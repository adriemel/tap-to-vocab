---
phase: 14-sentences-stats-fix
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/js/sentences.js
autonomous: true
requirements:
  - STAT-FIX-01

must_haves:
  truths:
    - "Each correct word tap in Build Sentences immediately increments the correct count by 1"
    - "Tapping an incorrect word increments the incorrect count by 1 (unchanged)"
    - "Completing a sentence with N words yields N correct count increments, not 1"
    - "Stats board totals reflect word-level interactions, consistent with Fill-in-Blank and Conjugation"
  artifacts:
    - path: "assets/js/sentences.js"
      provides: "SessionStats.record(true) called per correct word tap"
      contains: "SessionStats.record(true)"
  key_links:
    - from: "word btn onclick (correct-word branch)"
      to: "SessionStats.record(true)"
      via: "inline call immediately after placedWords.push(word)"
      pattern: "placedWords\\.push\\(word\\)[\\s\\S]{0,200}SessionStats\\.record\\(true\\)"
---

<objective>
Move `SessionStats.record(true)` from the sentence-completion block to the correct-word-tap branch in sentences.js, so each correct word tap counts individually rather than once per completed sentence.

Purpose: sentences.js currently records one correct interaction per completed sentence. Every other mode (fill-blank, conjugation, locations) records one per individual interaction. This inconsistency inflates the accuracy percentage shown in Build Sentences relative to actual effort.

Output: Modified `assets/js/sentences.js` where `SessionStats.record(true)` fires on every correct word tap.
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move SessionStats.record(true) to correct-word-tap branch</name>
  <files>assets/js/sentences.js</files>

  <read_first>
    - /home/desire/tap-to-vocab/assets/js/sentences.js — read entirely before making any change; understand lines 183-255 (the word btn onclick handler)
    - /home/desire/tap-to-vocab/assets/js/fill-blank.js — reference: see how line 70-88 records correct/incorrect at the individual choice level (not at sentence completion)
  </read_first>

  <action>
In `/home/desire/tap-to-vocab/assets/js/sentences.js`, make exactly two changes inside `initSentenceBuilder`:

**Change 1 — Add record(true) after the correct-word push (around line 207):**

Find this block (the correct-word branch, after the `if (word !== correctNextWord)` guard returns):

```javascript
          // Correct word - add to build area
          placedWords.push(word);
```

Insert the SessionStats call immediately after `placedWords.push(word);`:

```javascript
          // Correct word - add to build area
          placedWords.push(word);
          if (window.SessionStats) SessionStats.record(true);
```

**Change 2 — Remove record(true) from the sentence-completion block (around line 245):**

Find this block (inside `if (placedWords.length === words.length)` → `if (builtSentence === sentence.es)`):

```javascript
              if (window.CoinTracker) CoinTracker.addCoin();
              if (window.SessionStats) SessionStats.record(true);
              advanceTimer = setTimeout(() => {
```

Remove the `if (window.SessionStats) SessionStats.record(true);` line entirely, leaving:

```javascript
              if (window.CoinTracker) CoinTracker.addCoin();
              advanceTimer = setTimeout(() => {
```

No other lines change. The `record(false)` call (around line 196) and all other SessionStats calls remain untouched.
  </action>

  <verify>
    <automated>
      grep -n "SessionStats.record(true)" /home/desire/tap-to-vocab/assets/js/sentences.js
    </automated>
    Expected output: exactly ONE match on the line immediately following `placedWords.push(word);` (the correct-word branch) — NOT inside the `if (placedWords.length === words.length)` completion block.

    Confirm the completion block no longer has the call:
    grep -A5 "placedWords.length === words.length" /home/desire/tap-to-vocab/assets/js/sentences.js
    Expected: no `SessionStats.record` in the output.

    Confirm record(false) is unchanged:
    grep -n "SessionStats.record(false)" /home/desire/tap-to-vocab/assets/js/sentences.js
    Expected: exactly ONE match (around line 196, in the wrong-word branch).
  </verify>

  <acceptance_criteria>
    - `grep -c "SessionStats.record(true)" assets/js/sentences.js` returns `1`
    - `grep -c "SessionStats.record(false)" assets/js/sentences.js` returns `1`
    - The single `record(true)` occurrence is on the line directly after `placedWords.push(word);` — confirmed by grep -n showing the line number is 1-3 lines after the push
    - `grep -A10 "placedWords.length === words.length" assets/js/sentences.js` output does NOT contain `SessionStats.record`
    - `grep -B2 "SessionStats.record(true)" assets/js/sentences.js` shows `placedWords.push(word);` within the 2 preceding lines
  </acceptance_criteria>

  <done>sentences.js records one correct stat per correct word tap. Completing a 5-word sentence produces 5 correct increments in the stats panel, not 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This change is purely internal JS logic — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | SessionStats.record() | accept | SessionStats is an in-memory session object; no persistence, no server call; stat manipulation has zero security impact |
</threat_model>

<verification>
1. Serve the site locally: `cd /home/desire/tap-to-vocab && python3 -m http.server 8080`
2. Navigate to `/sentences.html`
3. Start a sentence with 4 words
4. Tap each word correctly, one by one
5. After each tap, open the stats panel (btn-stats) — correct count should increment by 1 per tap (1, 2, 3, 4) not jump to 1 at sentence completion
6. Tap a wrong word — incorrect count increments by 1 immediately (unchanged)
</verification>

<success_criteria>
- `SessionStats.record(true)` fires once per correct word tap, not once per sentence completion
- A 4-word sentence produces 4 correct stat increments
- Incorrect taps still produce 1 incorrect increment per tap (no regression)
- No other behavior in sentences.js changes
</success_criteria>

<output>
After completion, create `.planning/phases/14-sentences-stats-fix/14-01-SUMMARY.md` using the summary template.
</output>
