---
phase: 10-game-loop-and-integration
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - tap-to-vocab/locations.html
  - tap-to-vocab/index.html
  - tap-to-vocab/assets/css/styles.css
autonomous: false
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "User can open the Locations game from a full-width 'Locations' button on the home page, positioned below the 'Fill in' button"
    - "User can navigate to home from the Locations game page at any point"
    - "User can navigate back to the previous exercise using the Back button"
    - "The Locations page displays a prompt card showing the Spanish preposition and its German translation"
    - "The Locations page displays a coin counter badge and a progress badge in the header"
  artifacts:
    - path: "tap-to-vocab/locations.html"
      provides: "Complete game page shell: header, coin counter, progress badge, prompt card, scene (preserved), controls, feedback div, correct script load order, LocationsGame.startGame() call"
      contains: "LocationsGame.startGame()"
    - path: "tap-to-vocab/index.html"
      provides: "Locations entry button in .grid-two-col below .btn-fill-blank"
      contains: "btn-locations"
    - path: "tap-to-vocab/assets/css/styles.css"
      provides: ".btn-locations CSS rule (full-width, dark gradient, accent border)"
      contains: "btn-locations"
  key_links:
    - from: "tap-to-vocab/index.html .btn-locations"
      to: "tap-to-vocab/locations.html"
      via: "href=\"/locations.html\""
      pattern: "href.*locations\\.html"
    - from: "tap-to-vocab/locations.html <script>"
      to: "window.LocationsGame.startGame()"
      via: "LocationsGame.startGame() called after script load"
      pattern: "LocationsGame\\.startGame"
---

<objective>
Transform locations.html from a Phase 8/9 test scaffold into a full game page by adding the prompt card, header with coin+progress badges, navigation controls, and feedback div. Wire the correct script load order and call `LocationsGame.startGame()`. Add the Locations entry button to index.html and the matching CSS to styles.css.

Purpose: This is the navigation and UI shell layer. Plan 10-01 built the engine; this plan makes it driveable and discoverable.
Output: Playable locations.html with full game UI; Locations button visible on home page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-game-loop-and-integration/10-RESEARCH.md
@.planning/phases/10-game-loop-and-integration/10-01-SUMMARY.md
</context>

<interfaces>
<!-- fill-blank.html — authoritative pattern for header, controls, feedback (read before writing) -->
<!-- Key elements to mirror:
  - Header row: flex row, h1 + coin-badge (#coin-counter) + progress badge
  - Prompt display area: styled card div above the game area
  - Controls row: .controls class with back/skip/home buttons
  - Feedback: .error class div, display:none initially
  - Script order: coins.js → shared-utils.js → [game js]
-->

<!-- index.html .grid-two-col button pattern (from grep of file):
  Line 52: <a class="btn btn-fill-blank" href="/fill-blank.html">✏️ Fill in</a>
  Line 55: <button class="btn btn-games" id="btn-games">🎮 Play Games (10 ...)</button>
  New button goes BETWEEN these two lines (after Fill in, before Play Games)
-->

<!-- styles.css .btn-fill-blank pattern (to mirror for .btn-locations):
  .grid-two-col .btn-fill-blank { grid-column: 1 / -1; }
  New rule: .grid-two-col .btn-locations { grid-column: 1 / -1; ... }
-->

<!-- locations.html .scene div contents — MUST be preserved exactly as written by Plan 10-01:
  <div class="scene"> ... #caja-shadow, #draggable-shadow, #caja, 10 zone divs, #draggable ... </div>
  Do NOT restructure or replace .scene. Add elements OUTSIDE .scene only.
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild locations.html as a full game page</name>
  <files>tap-to-vocab/locations.html</files>
  <action>
Read the current locations.html (as modified by Plan 10-01) before writing. The file contains a `.scene` div with 10 zones, #draggable, and the 3D box. You must PRESERVE the entire `.scene` div and all inline `<style>` block CSS exactly. You are restructuring everything OUTSIDE `.scene`.

Replace the page shell (head, body structure, script tags) following this exact structure — while keeping all `.scene`-related styles and HTML intact:

**`<head>` changes:**
- Title: `Locations – Tap-to-Vocab`
- Keep `<link rel="stylesheet" href="/assets/css/styles.css">`
- Keep the entire existing `<style>` block unchanged (scene styles from Phase 9)
- No new `<style>` rules needed in head (styles.css handles page-level styles)

**`<body>` structure — replace the existing body content outside `.scene`:**
```html
<body>
  <div class="container">
    <div class="card">

      <!-- Header: title + coin badge + progress badge -->
      <div style="display:flex; align-items:center; justify-content:space-between; gap:10px; flex-wrap:wrap; margin-bottom:10px;">
        <h1 style="margin:0">&#128205; Locations</h1>
        <div style="display:flex; align-items:center; gap:8px;">
          <span class="badge coin-badge" id="coin-counter"><span class="coin-icon"></span> 0</span>
          <span class="badge" id="progress-badge">1 / 10</span>
        </div>
      </div>

      <!-- Prompt card: Spanish preposition + German translation -->
      <div id="prompt-card" class="sentence-target" style="text-align:center; margin-bottom:10px;">
        <div id="prompt-es" style="font-size:1.4rem; font-weight:700; color:var(--ink);">—</div>
        <div id="prompt-de" style="font-size:1rem; color:var(--muted); margin-top:4px;">—</div>
      </div>

      <!-- Scene: DO NOT CHANGE — preserved exactly from Phase 9 + Plan 10-01 gap fixes -->
      <div class="scene">
        [entire .scene contents from current file — copy verbatim]
      </div>

      <!-- Controls -->
      <div class="controls" style="margin-top:10px;">
        <button class="btn secondary" id="btn-back" disabled>&#8592; Back</button>
        <button class="btn secondary" id="btn-skip">Skip &#8594;</button>
        <button class="btn secondary" id="btn-home">&#127968; Home</button>
      </div>

      <!-- Feedback: error message for wrong drops -->
      <div class="error" id="feedback" style="display:none; margin-top:8px; text-align:center;"></div>

    </div>
  </div>

  <!-- Script load order: coins → shared-utils → locations (project convention) -->
  <script src="/assets/js/coins.js"></script>
  <script src="/assets/js/shared-utils.js"></script>
  <script src="/assets/js/locations.js"></script>
  <script>LocationsGame.startGame();</script>
</body>
```

IMPORTANT: The `.scene` div in your output must be the exact `.scene` div content from the current file — not a placeholder. Read the file, copy the `.scene` div contents literally.

Remove the old test script (`LocationsGame.init(...)` inline callback) and the old `<div id="result">` element. The `<script src="/assets/js/locations.js">` now comes AFTER coins.js and shared-utils.js.

Do not add game-init.js — it is explicitly out of scope (REQUIREMENTS.md).
  </action>
  <verify>
    <automated>grep -c 'data-zone\|btn-back\|btn-skip\|btn-home\|prompt-es\|coin-counter\|progress-badge\|LocationsGame.startGame\|coins.js\|shared-utils.js' /home/desire/tap-to-vocab/tap-to-vocab/locations.html</automated>
  </verify>
  <done>locations.html: has 10 data-zone divs; has #btn-back, #btn-skip, #btn-home buttons; has #prompt-es, #coin-counter, #progress-badge elements; has #feedback div with class="error"; script load order is coins.js → shared-utils.js → locations.js; final inline script calls LocationsGame.startGame(); old LocationsGame.init() test callback is gone; old #result div is gone</done>
</task>

<task type="auto">
  <name>Task 2: Add Locations button to index.html and styles.css</name>
  <files>tap-to-vocab/index.html, tap-to-vocab/assets/css/styles.css</files>
  <action>
**index.html:**
Read the file. Find the `.grid-two-col` div. The current order is:
1. Multiple category/game buttons
2. `.btn-fill-blank` (Fill in) — spans full width
3. `.btn-games` (Play Games) — spans full width

Insert a new Locations button immediately after the `.btn-fill-blank` line and before the `.btn-games` button:
```html
<a class="btn btn-locations" href="/locations.html">&#128205; Locations</a>
```

No other changes to index.html.

**styles.css:**
Read the file. Find the `.btn-fill-blank` CSS rule (it will be in the grid section — search for `btn-fill-blank`). Add the `.btn-locations` rule immediately after it:
```css
.grid-two-col .btn-locations {
  grid-column: 1 / -1;
  background: linear-gradient(135deg, #2a3a50 0%, #1a2535 100%);
  border: 2px solid var(--accent);
  font-weight: 700;
}
```

This makes the Locations button span the full two-column grid width, matching the visual weight of the Fill in and Play Games buttons.
  </action>
  <verify>
    <automated>grep -c 'btn-locations' /home/desire/tap-to-vocab/tap-to-vocab/index.html /home/desire/tap-to-vocab/tap-to-vocab/assets/css/styles.css</automated>
  </verify>
  <done>index.html contains `btn-locations` anchor with href="/locations.html"; styles.css contains `.btn-locations` rule with `grid-column: 1 / -1`; button is positioned after `.btn-fill-blank` and before `.btn-games` in the DOM</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify full game playthrough and home navigation</name>
  <files>tap-to-vocab/locations.html, tap-to-vocab/index.html</files>
  <action>
Serve the site locally and verify the complete Locations game loop and home navigation. Run: `cd /home/desire/tap-to-vocab/tap-to-vocab && python3 -m http.server 8001`

Steps to verify:
1. Open http://localhost:8001/index.html — confirm "Locations" button appears below "Fill in" button, spans full width, dark blue gradient, navigates to /locations.html on click
2. Open http://localhost:8001/locations.html — confirm: header shows title + coin badge + "1 / 10" progress badge; prompt card shows Spanish preposition and German translation; 3D box scene visible with orange cat in upper-left; 10 labeled zone blobs visible; Back (disabled), Skip, Home buttons visible below scene
3. Correct drop: Drag cat to zone matching the prompt. Verify: success sound, confetti, coin counter increments, progress badge advances to "2 / 10"
4. Wrong drop: Drag to different zone. Verify: "Falsch! Try again." appears in red, cat snaps back
5. Skip: Click Skip. Verify: progress badge advances, no coin, Back button enabled
6. Back: Click Back after skipping. Verify: returns to previous exercise
7. Home: Click Home. Verify: redirects to /
8. Mobile: DevTools iPhone 12 Pro (390px). Verify: scene fits, no horizontal scroll
  </action>
  <verify>
    <automated>MISSING — manual browser verification required for game interaction, sounds, confetti, and visual layout</automated>
  </verify>
  <done>All 8 verification steps pass: Locations button on home page navigates correctly; game loads with first prompt; correct drop awards coin and advances; wrong drop shows error message and snaps cat back; Skip advances without coin; Back navigates to prior exercise; Home redirects to /; 390px mobile layout has no horizontal scroll</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After completing automated tasks, before checkpoint:

1. Verify script order: `grep -n 'coins.js\|shared-utils.js\|locations.js' tap-to-vocab/locations.html` → coins first, shared-utils second, locations third
2. Verify startGame call: `grep 'LocationsGame.startGame' tap-to-vocab/locations.html` → must find exactly 1 match
3. Verify no old test code: `grep 'id="result"\|LocationsGame.init' tap-to-vocab/locations.html` → must find 0 matches
4. Verify Locations button in index: `grep 'btn-locations' tap-to-vocab/index.html` → 1 match
5. Verify CSS rule: `grep 'btn-locations' tap-to-vocab/assets/css/styles.css` → 1 match
6. Verify 10 zone labels: `grep 'zone-label' tap-to-vocab/locations.html | wc -l` → must be ≥10
</verification>

<success_criteria>
- locations.html is a fully playable game page (not a test scaffold)
- Script load order: coins.js → shared-utils.js → locations.js → LocationsGame.startGame()
- Old test code (init() callback, #result div) is gone
- Prompt card shows Spanish + German for each exercise
- Header displays coin counter and progress badge
- Controls: Back (disabled at start), Skip, Home all present
- Feedback div with class="error" present below controls
- index.html: Locations button after Fill in, before Play Games
- styles.css: .btn-locations with grid-column: 1/-1, dark gradient, accent border
- Human checkpoint: full game playthrough verified on desktop; visual check on 390px mobile viewport
</success_criteria>

<output>
After completion, create `.planning/phases/10-game-loop-and-integration/10-02-SUMMARY.md`
</output>
