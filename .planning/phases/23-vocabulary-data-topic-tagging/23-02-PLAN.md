---
phase: 23-vocabulary-data-topic-tagging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/words.tsv
autonomous: true
requirements: [DATA-05, TAG-01, TAG-03, TAG-04, TAG-05, TAG-06]

must_haves:
  truths:
    - "words.tsv has a 4th column named topics and every data row has exactly 4 tab-separated fields (D-01, DATA-05)"
    - "Not one row's category, es or de value changed — the first three columns are byte-identical to the pre-plan file (D-04, NAV-07)"
    - "All 133 rows in Colores, Animales, Numeros, Saludar and Casa_Familia carry topics equal to their own category name (D-05, TAG-03)"
    - "All 7 x-prefixed rows carry the un-prefixed topic name while keeping their x-prefixed category (D-05b)"
    - "Every one of the 118 Palabras headwords carries a topic — one of the 8 non-Palabras slugs or Palabras itself (D-08, TAG-01)"
    - "The 7 long Palabras example sentences keep a blank topics cell and are still present (D-06, D-08)"
    - "No Unidad row was touched by this plan — that is plans 03 and 04"
  artifacts:
    - path: "data/words.tsv"
      provides: "4-column TSV with legacy topics, x-rows and the Palabras partition tagged"
      contains: "topics"
  key_links:
    - from: "data/words.tsv"
      to: "assets/js/shared-utils.js"
      via: "loadWords reads the topics column by header name"
      pattern: "topics"
    - from: "data/words.tsv"
      to: "assets/js/tapvocab.js"
      via: "topic.html?cat=<category> still filters on the untouched category column"
      pattern: "category"
---

<objective>
Add the `topics` column to `data/words.tsv` and complete the first two tagging groups: the five
legacy topic-like categories (plus their 7 hidden `x` siblings) and the Palabras partition.

Purpose: TAG-01 (Palabras partitioned), TAG-03 (the five coherent topics tag themselves), and the
first members of TAG-04/05/06 (Calendario / Comida_Bebida / Escuela get their Palabras recruits).
Output: `data/words.tsv` becomes a 4-column file; 265 of its 708 rows carry a topic.

Source conflict resolved: REQUIREMENTS.md § Data Model shows the food topic as `Comida` and spells
topic names with spaces. CONTEXT.md D-01 overrides that. The nine values that may ever appear in
column 4 are exactly: `Colores`, `Animales`, `Numeros`, `Saludar`, `Casa_Familia`, `Palabras`,
`Calendario`, `Comida_Bebida`, `Escuela`. Do not "fix" them to the REQUIREMENTS.md spelling; the
pretty display labels are Phase 24 HTML.

D-14: there is NO approval gate on the tagging mapping. Apply it directly. The user reviews the git
diff and the running app afterwards.
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
</context>

<interfaces>
Target file shape after this plan. Header row, tab-separated:

`category` TAB `es` TAB `de` TAB `topics`

Every data row has exactly 4 tab-separated fields. An untagged row ends in a trailing tab followed
by nothing — the empty string, not a placeholder, not the word "none" (D-03).

The nine legal non-blank values in column 4: `Colores`, `Animales`, `Numeros`, `Saludar`,
`Casa_Familia`, `Palabras`, `Calendario`, `Comida_Bebida`, `Escuela`.

Verified pre-plan facts (measured, not assumed) — `data/words.tsv` is 709 lines: 1 header + 708 data
rows, all currently 3 fields. Category distribution: Unidad3 143, Palabras 125, Unidad5A 115,
Unidad4 108, Unidad2 77, Saludar 40, Casa_Familia 33, Numeros 32, Animales 15, Colores 13,
xAnimales 5, xCasa_Familia 2. Sentence-shaped rows (Spanish matching `/[.?!]$/`, the live regex from
`assets/js/sentences.js:53-58`) per category: Saludar 13, Casa_Familia 4, Palabras 7, Colores 2,
Unidad2 22, Unidad3 47, Unidad4 35, Unidad5A 43; zero in Animales, Numeros, xAnimales,
xCasa_Familia.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the topics column and tag the five legacy topics plus the x-prefixed rows</name>
  <files>data/words.tsv</files>
  <read_first>
    - data/words.tsv (whole file — you need the row inventory before editing it)
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-01, D-03, D-04, D-05, D-05b, D-06, D-07
    - .planning/codebase/CONVENTIONS.md § Naming (TSV categories are PascalCase) and § TSV Parsing Pattern
    - assets/js/sentences.js lines 53-58 — the live `/[.?!]$/` sentence regex that defines "sentence-shaped"
    - CLAUDE.md § Adding Vocabulary — the `x` prefix convention for hidden categories
  </read_first>
  <action>
    First, capture the baseline so the D-04 safety property is provable:
    `mkdir -p /tmp/gsd23` then
    `git show HEAD:data/words.tsv > /tmp/gsd23/words-prephase.tsv` and
    `awk -F'\t' '{print $1"\t"$2"\t"$3}' /tmp/gsd23/words-prephase.tsv > /tmp/gsd23/cols123-before.txt`.
    Do this before any edit. `data/words.tsv` is clean at plan start, so HEAD is the pre-phase file.

    Then make two changes to `data/words.tsv`:

    (a) Rewrite the header line to the four tab-separated names `category`, `es`, `de`, `topics`,
        and give EVERY one of the 708 data rows a 4th field. Rows you are not tagging in this task
        get an empty 4th field — i.e. the line gains a trailing tab and nothing after it. Do not
        write a placeholder value, `-`, `none`, or `Palabras` into untagged cells; D-03 makes blank
        the legitimate representation of "untagged".

    (b) Tag two groups, and only these two, in this task:

        Legacy topic-like categories (D-05, TAG-03) — for every row whose `category` is one of
        `Colores` (13 rows), `Animales` (15), `Numeros` (32), `Saludar` (40), `Casa_Familia` (33),
        set `topics` to that same string. All 133 rows get tagged, including the 19 sentence-shaped
        ones (Saludar 13, Casa_Familia 4, Colores 2). This is D-07 applied exactly as CONTEXT.md
        enumerates it: those 19 are set phrases and question-and-answer drill lines that belong in
        their topic — `¿Qué tal?`, `¿Como te llamas?`, `¿De donde eres?`, `¿Y tu?`,
        `¿De qué color es la tortuga?`, `Mi mascota se llama Tigo.` and the rest. The `category`
        value is NOT changed (D-04) — these rows keep working at `topic.html?cat=Colores` and
        friends.

        Hidden `x` rows (D-05b) — the 5 `xAnimales` rows (`el serpiente`, `el burro`, `la abeja`,
        `la tarantula`, `el mono`) get `topics` = `Animales`; the 2 `xCasa_Familia` rows
        (`el primo`, `la prima`) get `topics` = `Casa_Familia`. Their `category` keeps the leading
        `x` so they stay out of navigation; the topic tag is the deliberate un-hiding via the new
        axis.

    Leave `Palabras` (task 2 of this plan) and every `Unidad*` row (plans 03 and 04) with a blank
    4th field in this task. Overreaching here makes the per-plan gates below meaningless.

    You may do this by hand or with a throwaway script in `/tmp/gsd23`. If you script it, the script
    must not be written inside the repo, must use only shell/awk/node built-ins, and must not add a
    package, `package.json`, or build step — CLAUDE.md keeps this repo zero-dependency.

    Preserve exactly: the file's UTF-8 accented characters (`él`, `sábado`, `¿`, `¡`), its final
    newline, the single trailing space in the `el chico ` row, and the row order. Do not reformat,
    sort, deduplicate, or "clean up" anything.
  </action>
  <verify>
    <automated>head -1 data/words.tsv | od -c | head -3</automated>
    <automated>awk -F'\t' 'NF!=4' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' '{print $1"\t"$2"\t"$3}' data/words.tsv > /tmp/gsd23/cols123-after.txt; diff /tmp/gsd23/cols123-before.txt /tmp/gsd23/cols123-after.txt && echo COLS123_IDENTICAL</automated>
    <automated>awk -F'\t' 'NR>1 && $1 ~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia)$/ && $4!=$1' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $1 ~ /^x/ && $4 != substr($1,2)' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && ($1=="Palabras" || $1 ~ /^Unidad/) && $4!=""' data/words.tsv | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 data/words.tsv` is exactly `category`, `es`, `de`, `topics` separated by single tab characters (confirm with `od -c` that the separators are `\t`, not spaces)
    - `wc -l data/words.tsv` prints `709` — no row added, none removed
    - `awk -F'\t' 'NF!=4' data/words.tsv | wc -l` prints `0` — every line, header included, has exactly 4 fields
    - `diff /tmp/gsd23/cols123-before.txt /tmp/gsd23/cols123-after.txt` prints nothing and the command echoes `COLS123_IDENTICAL` — proves D-04/NAV-07: no category, es or de value changed anywhere
    - `awk -F'\t' 'NR>1 && $1 ~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia)$/ && $4!=$1' data/words.tsv | wc -l` prints `0` — all 133 legacy rows self-tagged, none blank
    - `awk -F'\t' 'NR>1 && $1 ~ /^x/ && $4 != substr($1,2)' data/words.tsv | wc -l` prints `0` — both x-categories tagged with their un-prefixed name
    - `awk -F'\t' 'NR>1 && $4!=""' data/words.tsv | wc -l` prints `140` (133 legacy + 7 hidden) at the end of this task
    - `awk -F'\t' 'NR>1 && ($1=="Palabras" || $1 ~ /^Unidad/) && $4!=""' data/words.tsv | wc -l` prints `0` — this task did not reach into other plans' rows
    - `awk -F'\t' 'NR>1{print $1}' data/words.tsv | sort | uniq -c | sort -rn` reproduces the exact pre-plan distribution: Unidad3 143, Palabras 125, Unidad5A 115, Unidad4 108, Unidad2 77, Saludar 40, Casa_Familia 33, Numeros 32, Animales 15, Colores 13, xAnimales 5, xCasa_Familia 2
    - `grep -c $'\t\t' data/words.tsv` prints `0` — no row lost its `es` or `de` value to an off-by-one field shift
  </acceptance_criteria>
  <done>`data/words.tsv` is a 4-column file, columns 1-3 byte-identical to the pre-plan version, with exactly 140 rows tagged: the 133 legacy rows self-tagged and the 7 x-rows tagged with their un-prefixed names.</done>
</task>

<task type="auto">
  <name>Task 2: Partition the 125 Palabras rows into topics</name>
  <files>data/words.tsv</files>
  <read_first>
    - data/words.tsv — read all 125 rows where column 1 is `Palabras` (`awk -F'\t' '$1=="Palabras"' data/words.tsv`); you cannot assign topics you have not read
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-01, D-06, D-08, D-12 and § Claude's Discretion
    - .planning/REQUIREMENTS.md § Data Model — the partitioned-vs-additive table and the expected topic sizes (Calendario ~33, Comida y Bebida ~17, Escuela ~16 across the whole file)
  </read_first>
  <action>
    Partition every `Palabras` row (TAG-01). The rule from the Data Model table: Palabras is a
    catch-all, not a school unit, so a Palabras word that fits a topic MOVES into it and leaves
    Palabras. Concretely, for each of the 118 Palabras rows whose Spanish does NOT match `/[.?!]$/`,
    write into column 4 exactly one of: `Colores`, `Animales`, `Numeros`, `Saludar`, `Casa_Familia`,
    `Calendario`, `Comida_Bebida`, `Escuela`, or `Palabras` if nothing fits better. Column 1 stays
    `Palabras` for every row (D-04) — the move is expressed only in column 4.

    Assignment guidance (the per-word calls are yours — CONTEXT.md § Claude's Discretion):
    - `Comida_Bebida` (TAG-05): food and drink nouns and the verbs of eating and drinking, e.g.
      `el tomate`, `el queso`.
    - `Escuela` (TAG-06): school, teaching, studying and language-of-instruction words, e.g.
      `estudiar`, `estudiar algo`, `el idioma`, `el alemán`, `el inglés`, `el español`,
      `el francés`, `la palabra`, `con acento`, `con dos puntos`, `Se escribe`.
    - `Calendario` (TAG-04): days, months, seasons, times of day and period words.
    - The five legacy topics: colour adjectives to `Colores`, animals to `Animales`, cardinal and
      ordinal numbers to `Numeros`, greeting and politeness phrases to `Saludar`, house/family/room
      words to `Casa_Familia`.
    - `Palabras` (the remainder): function words, connectives, question words, general adjectives
      and general verbs — `entonces`, `también`, `pero`, `quizás`, `fuerte`, `fácil`, `importante`,
      `trabajar`, `buscar algo` and their kind. D-12 makes `Palabras` the general catch-all bucket
      for any row with no better topic, not merely a label for rows whose category is Palabras.
    - Assign exactly one topic per row (D-02). A word that plausibly fits two gets its best fit and
      appears once; do not write a comma-separated pair.

    The 7 Palabras rows whose Spanish matches `/[.?!]$/` — the long example sentences such as
    `Yo estudio español en el instituto.`, `¿Hablas francés?`, `Hablo muy bien alemán.`,
    `¿Como se escribe?`, `¿Qué pasa?`, `Adelante!` — keep a BLANK 4th field (D-06). This is the
    deliberate refinement in D-08: ROADMAP success criterion 3 reads as "every Palabras HEADWORD
    either moved into one of the 8 non-Palabras topics or still carries `topics: Palabras`". Do not
    tag these 7 and do not treat their blankness as a defect. Note two of them are short —
    `¿Qué pasa?` and `Adelante!` — and D-07 permits tagging a short set phrase; if you tag either
    one to `Saludar`, the blank count below becomes 5 or 6 instead of 7, which is acceptable. What
    is NOT acceptable is tagging any Palabras row longer than 7 Spanish words.

    Do not change column 1, 2 or 3 of any row, do not reorder rows, do not touch any Unidad row.
  </action>
  <verify>
    <automated>awk -F'\t' 'NR>1 && $1=="Palabras" && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $4!="" && $4 !~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia|Palabras|Calendario|Comida_Bebida|Escuela)$/' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $1=="Palabras"{n=split($2,w," "); if($2 ~ /[.?!]$/ && n>7 && $4!="") print}' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' '{print $1"\t"$2"\t"$3}' data/words.tsv > /tmp/gsd23/cols123-after2.txt; diff /tmp/gsd23/cols123-before.txt /tmp/gsd23/cols123-after2.txt && echo COLS123_IDENTICAL</automated>
    <automated>awk -F'\t' 'NR>1 && $1=="Palabras"{c[$4]++} END{for(k in c) printf "%s=%d\n", (k==""?"(blank)":k), c[k]}' data/words.tsv | sort</automated>
    <automated>awk -F'\t' 'NR>1 && $1 ~ /^Unidad/ && $4!=""' data/words.tsv | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F'\t' 'NR>1 && $1=="Palabras" && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l` prints `0` — TAG-01 / ROADMAP criterion 3 as refined by D-08: every Palabras headword carries a topic
    - `awk -F'\t' 'NR>1 && $1=="Palabras" && $4!=""' data/words.tsv | wc -l` prints a number between `118` and `125` inclusive
    - `awk -F'\t' 'NR>1 && $1=="Palabras" && $4==""' data/words.tsv | wc -l` prints a number between `0` and `7` inclusive, and every such row's Spanish matches `/[.?!]$/`
    - The whitelist check `awk -F'\t' 'NR>1 && $4!="" && $4 !~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia|Palabras|Calendario|Comida_Bebida|Escuela)$/' data/words.tsv | wc -l` prints `0` — no typo, no space-separated variant, no `Comida`, no comma-joined pair
    - The long-sentence gate `awk -F'\t' 'NR>1 && $1=="Palabras"{n=split($2,w," "); if($2 ~ /[.?!]$/ && n>7 && $4!="") print}' data/words.tsv | wc -l` prints `0`
    - The Palabras distribution printout shows at least three distinct non-`Palabras` topics with a non-zero count, including `Comida_Bebida` and `Escuela` (TAG-05, TAG-06 recruits from Palabras)
    - `awk -F'\t' 'NR>1 && $1=="Palabras" && $4=="Palabras"' data/words.tsv | wc -l` prints a number `< 118` — the partition actually moved words out, it is not a blanket self-tag
    - `diff /tmp/gsd23/cols123-before.txt /tmp/gsd23/cols123-after2.txt` prints nothing and echoes `COLS123_IDENTICAL`
    - `awk -F'\t' 'NR>1 && $1 ~ /^Unidad/ && $4!=""' data/words.tsv | wc -l` still prints `0` — Unidad rows remain untouched for plans 03 and 04
    - `wc -l data/words.tsv` still prints `709` and `awk -F'\t' 'NF!=4' data/words.tsv | wc -l` still prints `0`
  </acceptance_criteria>
  <done>All 118 Palabras headwords carry exactly one of the nine slugs, the long Palabras sentences stay blank, columns 1-3 are unchanged, and no Unidad row has been tagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/words.tsv` → `loadWords` → DOM | Vocabulary text is author-committed and rendered via `textContent` in `tapvocab.js`; `sentences.js` uses `innerHTML` only with its own literal strings. This plan adds no new sink. |
| Repo content → public GitHub Pages site | Anything committed here is world-readable. D-05b intentionally makes 7 previously hidden `x` rows reachable via the Topics axis in Phase 24. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Denial of Service | Malformed TSV row (stray tab, lost field, dropped final newline) breaks `loadWords` for every learning page at once | mitigate | Hard gates in both tasks: `awk -F'\t' 'NF!=4'` must be 0, `wc -l` must stay 709, `grep -c $'\t\t'` must be 0, and columns 1-3 must diff clean against the pre-plan snapshot. |
| T-23-08 | Tampering | Accidental edit of `category`, `es` or `de` while bulk-editing column 4 — would silently orphan starred practice-list entries (they match on `es`+`de`) | mitigate | `/tmp/gsd23/cols123-before.txt` snapshot taken before the first edit and diffed after each task; must be empty (D-04, NAV-07). |
| T-23-09 | Information Disclosure | D-05b un-hides `xAnimales`/`xCasa_Familia` rows onto the Topics axis | accept | Reviewed: the 7 rows are `el serpiente`, `el burro`, `la abeja`, `la tarantula`, `el mono`, `el primo`, `la prima` — ordinary vocabulary, already committed to a public repo. Exposure is the intended effect of D-05b, not a leak. |
| T-23-SC | Tampering (supply chain) | npm/pip/cargo installs | mitigate | Zero packages installed. Any helper script lives in `/tmp/gsd23`, uses shell/awk/node built-ins only, and is never committed — CLAUDE.md keeps this repo zero-dependency and build-free. |
| T-23-10 | Spoofing / Repudiation / Elevation of Privilege | — | accept | Static GitHub Pages site: no server, no accounts, no sessions, no privilege model. These categories have no attack surface for a data-file edit. |
</threat_model>

<verification>
1. `awk -F'\t' 'NF!=4' data/words.tsv | wc -l` prints `0` and `wc -l data/words.tsv` prints `709`
2. Columns 1-3 diff clean against `/tmp/gsd23/cols123-before.txt`
3. Legacy self-tag gate and x-row gate both print `0`
4. Palabras headword gate prints `0` blanks
5. Slug whitelist gate prints `0`
6. Unidad rows still all blank (`0`)
7. Serve the site (`python3 -m http.server 8000`) and open `http://localhost:8000/topic.html?cat=Palabras` and `?cat=Colores` — both browse and quiz render exactly as before, with no console error; the extra column is inert until Phase 24
</verification>

<success_criteria>
- `data/words.tsv` is a 4-column, 709-line file with header `category`/`es`/`de`/`topics`
- 133 legacy rows self-tagged, 7 x-rows tagged with their un-prefixed names (TAG-03, D-05, D-05b)
- All 118 Palabras headwords carry exactly one of the nine slugs (TAG-01, and the Palabras half of TAG-04/05/06)
- Long Palabras example sentences remain blank and present (D-06, D-08)
- Columns 1-3 byte-identical to the pre-plan file (D-04, NAV-07)
- No Unidad row tagged yet; no dependency, package manager or build step added
</success_criteria>

<output>
Create `.planning/phases/23-vocabulary-data-topic-tagging/23-02-SUMMARY.md` when done.
Commit message prefix for this plan: `data(23-02):` — later plans locate the pre-phase blob by
filtering `git log -- data/words.tsv` for messages that do NOT contain `(23-`, so keep the prefix.
</output>
