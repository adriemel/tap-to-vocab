---
phase: 23-vocabulary-data-topic-tagging
plan: 03
type: execute
wave: 2
depends_on: ["23-02"]
files_modified:
  - data/words.tsv
autonomous: true
requirements: [TAG-02, TAG-04, TAG-05, TAG-06]

must_haves:
  truths:
    - "Every Unidad2, Unidad3, Unidad4 and Unidad5A headword carries exactly one topic while keeping its unidad category (D-11, TAG-02, ROADMAP criterion 4)"
    - "The 147 long example sentences in those four unidades keep a blank topics cell and remain present (D-06)"
    - "The Calendario, Comida_Bebida and Escuela topics are populated from unidad vocabulary, not only from Palabras (TAG-04, TAG-05, TAG-06)"
    - "Not one row's category, es or de value changed (D-04, NAV-07)"
  artifacts:
    - path: "data/words.tsv"
      provides: "additively tagged Unidad2/3/4/5A rows"
      contains: "Unidad5A"
  key_links:
    - from: "data/words.tsv"
      to: "assets/js/tapvocab.js"
      via: "topic.html?cat=Unidad3 still filters on the untouched category column"
      pattern: "category"
    - from: "data/words.tsv"
      to: "assets/js/sentences.js"
      via: "Build Sentences groups by category; blank topics must not disturb it"
      pattern: "category"
---

<objective>
Tag the four existing unidades additively: `Unidad2` (77 rows), `Unidad3` (143), `Unidad4` (108) and
`Unidad5A` (115). Each headword keeps its `category` and gains a topic, so the same word is findable
by both its school unit and its topic.

Purpose: TAG-02 and ROADMAP success criterion 4, plus the bulk of TAG-04 (Calendario), TAG-05
(Comida_Bebida) and TAG-06 (Escuela) — the days, months, food and school vocabulary mostly lives in
these unidades, not in Palabras.
Output: 271 more tagged rows in `data/words.tsv`.

Asymmetry to keep straight: Palabras was PARTITIONED in plan 02 (a word moves out). Unidades are
ADDITIVE (a word stays and gains a tag). The unidades must stay complete so a whole school unit can
be revised before a test.

D-14: no approval gate on the mapping. Apply it directly; the user reviews the git diff and the app.
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-PATTERNS.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-02-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
`data/words.tsv` is already a 4-column file after plan 02: `category` TAB `es` TAB `de` TAB `topics`,
709 lines, 708 data rows, every line exactly 4 tab-separated fields.

The nine legal non-blank values in column 4 (D-01, PascalCase with underscores — NOT the
space-separated spellings in REQUIREMENTS.md § Data Model): `Colores`, `Animales`, `Numeros`,
`Saludar`, `Casa_Familia`, `Palabras`, `Calendario`, `Comida_Bebida`, `Escuela`.

Measured row counts for this plan's scope, headwords vs sentence-shaped rows (Spanish matching
`/[.?!]$/`, the live regex at `assets/js/sentences.js:53-58`):
Unidad2 — 55 headwords, 22 sentences. Unidad3 — 96 headwords, 47 sentences.
Unidad4 — 73 headwords, 35 sentences. Unidad5A — 72 headwords, 43 sentences.
Total: 296 headwords to tag, 147 sentence rows to leave blank.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tag Unidad2 and Unidad3 additively</name>
  <files>data/words.tsv</files>
  <read_first>
    - data/words.tsv — read all 220 rows where column 1 is `Unidad2` or `Unidad3` (`awk -F'\t' '$1=="Unidad2" || $1=="Unidad3"' data/words.tsv`); you cannot assign topics you have not read
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-01, D-02, D-04, D-06, D-11, D-12 and § Claude's Discretion
    - .planning/REQUIREMENTS.md § Data Model — the additive rule for unidades and the expected topic sizes
    - .planning/phases/23-vocabulary-data-topic-tagging/23-02-SUMMARY.md — the Palabras assignments already made, so unidad words of the same kind land in the same topic
  </read_first>
  <action>
    Capture the baseline first, before any edit:
    `mkdir -p /tmp/gsd23` then
    `git show HEAD:data/words.tsv | awk -F'\t' '{print $1"\t"$2"\t"$3}' > /tmp/gsd23/p03-cols123-before.txt`.
    `data/words.tsv` is clean at plan start (plan 02 committed), so HEAD holds plan 02's output.

    For every `Unidad2` and `Unidad3` row whose Spanish does NOT match `/[.?!]$/` — 55 + 96 = 151
    rows — write exactly one of the nine slugs into column 4. Column 1 stays `Unidad2` / `Unidad3`
    for every row: this is the additive rule (D-11, TAG-02), the word appears under both its unidad
    and its topic.

    Assignment guidance (per-word calls are yours, per CONTEXT.md § Claude's Discretion):
    - `Calendario` (TAG-04): days of the week, months, seasons, times of day, and period words such
      as week/weekend/today/tomorrow/early/late.
    - `Comida_Bebida` (TAG-05): food, drink, meals, and the verbs of eating and drinking.
    - `Escuela` (TAG-06): school subjects, classroom objects, teaching/studying verbs, timetable
      words, and language-of-instruction words.
    - `Casa_Familia`: family members, rooms, furniture, house and home words.
    - `Colores` / `Animales` / `Numeros`: colour adjectives, animals, and cardinal or ordinal
      numbers respectively.
    - `Saludar`: greetings, farewells, politeness formulas, and introduce-yourself vocabulary.
    - `Palabras`: everything with no better home — function words, connectives, question words,
      general adjectives, general verbs, leisure and sport words. D-12 makes `Palabras` the general
      catch-all bucket for any row with no better topic, not merely a label for rows whose category
      is Palabras. A "Deportes y Ocio" topic was explicitly declined for this milestone (TAG-07,
      deferred), so sport and leisure words park in `Palabras`.
    - Exactly one topic per row (D-02). No comma-separated pairs; best fit wins.

    The 69 sentence-shaped rows in these two unidades (Unidad2 22, Unidad3 47) keep a BLANK 4th
    field (D-06) — topic screens stay clean word lists rather than mixing single words with long
    example sentences, and those sentences are already reachable under their unidad and in Build
    Sentences. The single exception D-07 allows: a sentence-shaped row of roughly five Spanish words
    or fewer that is plainly a set phrase (`¡Vale!`, `¿Qué tal?`-style) may be tagged. Anything
    longer than 7 Spanish words must stay blank — that is a hard gate below.

    Do not change column 1, 2 or 3 of any row. Do not reorder rows. Do not touch `Unidad4`,
    `Unidad5A`, `Palabras`, the legacy topic categories, or the `x` rows. A throwaway script in
    `/tmp/gsd23` is fine; it must not be committed and must not add a package or build step.
  </action>
  <verify>
    <automated>awk -F'\t' 'NR>1 && ($1=="Unidad2" || $1=="Unidad3") && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && ($1=="Unidad2" || $1=="Unidad3") && $2 ~ /[.?!]$/ {n=split($2,w," "); if(n>7 && $4!="") print}' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $4!="" && $4 !~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia|Palabras|Calendario|Comida_Bebida|Escuela)$/' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' '{print $1"\t"$2"\t"$3}' data/words.tsv > /tmp/gsd23/p03-cols123-after1.txt; diff /tmp/gsd23/p03-cols123-before.txt /tmp/gsd23/p03-cols123-after1.txt && echo COLS123_IDENTICAL</automated>
    <automated>awk -F'\t' 'NR>1 && ($1=="Unidad2" || $1=="Unidad3"){c[$4]++} END{for(k in c) printf "%s=%d\n",(k==""?"(blank)":k),c[k]}' data/words.tsv | sort</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F'\t' 'NR>1 && ($1=="Unidad2" || $1=="Unidad3") && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l` prints `0` — all 151 headwords tagged (TAG-02, ROADMAP criterion 4)
    - `awk -F'\t' 'NR>1 && ($1=="Unidad2" || $1=="Unidad3") && $4!=""' data/words.tsv | wc -l` prints a number between `151` and `165` inclusive (151 headwords plus at most a handful of D-07 set phrases)
    - The long-sentence gate prints `0` — no sentence over 7 Spanish words was tagged (D-06)
    - The slug whitelist gate prints `0` across the whole file
    - `awk -F'\t' 'NR>1{print $1}' data/words.tsv | sort | uniq -c` still shows `77 Unidad2` and `143 Unidad3` — categories untouched (D-04)
    - `diff /tmp/gsd23/p03-cols123-before.txt /tmp/gsd23/p03-cols123-after1.txt` prints nothing and echoes `COLS123_IDENTICAL`
    - The per-topic printout for these two unidades shows at least four distinct non-blank topics, and `Palabras` is not more than 70% of the tagged rows — the unidades genuinely fan out into topics rather than being blanket-tagged
    - `wc -l data/words.tsv` prints `709`; `awk -F'\t' 'NF!=4' data/words.tsv | wc -l` prints `0`
    - `awk -F'\t' 'NR>1 && ($1=="Unidad4" || $1=="Unidad5A") && $4!=""' data/words.tsv | wc -l` prints `0` — task 2's rows not touched yet
  </acceptance_criteria>
  <done>All 151 Unidad2/Unidad3 headwords carry exactly one slug, their categories are unchanged, their long sentences are blank, and Unidad4/Unidad5A are still untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Tag Unidad4 and Unidad5A additively</name>
  <files>data/words.tsv</files>
  <read_first>
    - data/words.tsv — read all 223 rows where column 1 is `Unidad4` or `Unidad5A` (`awk -F'\t' '$1=="Unidad4" || $1=="Unidad5A"' data/words.tsv`)
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-01, D-02, D-04, D-06, D-11, D-12
    - The topic assignments you made in Task 1 — the same kind of word must land in the same topic across unidades, or the Topics screens will look arbitrary
  </read_first>
  <action>
    For every `Unidad4` and `Unidad5A` row whose Spanish does NOT match `/[.?!]$/` — 73 + 72 = 145
    rows — write exactly one of the nine slugs into column 4, keeping column 1 unchanged (additive
    rule, D-11).

    Use the same assignment guidance as Task 1 and stay consistent with the calls made there.
    Unidad5A in particular carries the days-of-the-week and weekend vocabulary that populates
    `Calendario` (TAG-04) — `sábado` and its siblings are the canonical example in CONTEXT.md's row
    table. Unidad4 carries drink and food vocabulary such as `el zumo` for `Comida_Bebida`
    (TAG-05).

    The 78 sentence-shaped rows (Unidad4 35, Unidad5A 43) keep a BLANK 4th field (D-06). The D-07
    exception for short set phrases of roughly five Spanish words or fewer still applies; anything
    over 7 Spanish words must stay blank.

    Do not change column 1, 2 or 3 of any row, do not reorder rows, and do not revisit Unidad2,
    Unidad3, Palabras, the legacy categories or the `x` rows.
  </action>
  <verify>
    <automated>awk -F'\t' 'NR>1 && ($1=="Unidad4" || $1=="Unidad5A") && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $1 ~ /^Unidad/ && $2 ~ /[.?!]$/ {n=split($2,w," "); if(n>7 && $4!="") print}' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $4!="" && $4 !~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia|Palabras|Calendario|Comida_Bebida|Escuela)$/' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' '{print $1"\t"$2"\t"$3}' data/words.tsv > /tmp/gsd23/p03-cols123-after2.txt; diff /tmp/gsd23/p03-cols123-before.txt /tmp/gsd23/p03-cols123-after2.txt && echo COLS123_IDENTICAL</automated>
    <automated>awk -F'\t' 'NR>1 && $4!=""{c[$4]++} END{for(k in c) printf "%s=%d\n",k,c[k]}' data/words.tsv | sort</automated>
    <automated>awk -F'\t' 'NR>1 && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F'\t' 'NR>1 && ($1=="Unidad4" || $1=="Unidad5A") && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l` prints `0` — all 145 headwords tagged
    - `awk -F'\t' 'NR>1 && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l` prints `0` across the WHOLE file — every non-sentence row in `words.tsv` now carries a topic
    - The long-sentence gate over all `Unidad*` rows prints `0`
    - The slug whitelist gate prints `0`
    - `diff /tmp/gsd23/p03-cols123-before.txt /tmp/gsd23/p03-cols123-after2.txt` prints nothing and echoes `COLS123_IDENTICAL`
    - The whole-file topic distribution printout lists all nine slugs with a non-zero count, and `Calendario`, `Comida_Bebida` and `Escuela` each have at least `10` rows (TAG-04, TAG-05, TAG-06 — REQUIREMENTS.md estimates ~33 / ~17 / ~16)
    - `awk -F'\t' 'NR>1{print $1}' data/words.tsv | sort | uniq -c | sort -rn` still reproduces the exact original distribution: Unidad3 143, Palabras 125, Unidad5A 115, Unidad4 108, Unidad2 77, Saludar 40, Casa_Familia 33, Numeros 32, Animales 15, Colores 13, xAnimales 5, xCasa_Familia 2
    - `wc -l data/words.tsv` prints `709`; `awk -F'\t' 'NF!=4' data/words.tsv | wc -l` prints `0`; `grep -c $'\t\t' data/words.tsv` prints `0`
  </acceptance_criteria>
  <done>Every headword in the file — Palabras, legacy, hidden and all four existing unidades — carries exactly one of the nine slugs; sentences stay blank; columns 1-3 are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/words.tsv` → `loadWords` → DOM | Author-committed vocabulary text; rendered with `textContent` in `tapvocab.js`. This plan edits only column 4 and adds no new DOM sink. |
| Repo content → public GitHub Pages site | Everything committed is world-readable; this plan adds no new content, only classification of existing rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-11 | Denial of Service | A bulk edit over 443 rows shifting a field or dropping a tab breaks `loadWords` for every page | mitigate | Every task gates on `awk -F'\t' 'NF!=4'` = 0, `wc -l` = 709, `grep -c $'\t\t'` = 0, and a clean cols-1-3 diff against the snapshot taken before the first edit. |
| T-23-12 | Tampering | Silent mutation of `es`/`de` during the bulk pass would orphan starred practice-list entries, which match on `es`+`de` only | mitigate | `/tmp/gsd23/p03-cols123-before.txt` diffed after each task; must be empty (D-04, NAV-07). |
| T-23-13 | Tampering | An invented or misspelled slug (e.g. `Comida`, `Casa Familia`, `Calendario,Escuela`) would produce a topic screen that silently shows nothing in Phase 24 | mitigate | Whitelist gate rejects any column-4 value outside the nine D-01 slugs, and the no-comma rule enforces D-02. |
| T-23-SC | Tampering (supply chain) | npm/pip/cargo installs | mitigate | Zero packages installed; any helper script lives in `/tmp/gsd23`, uses shell/awk/node built-ins only, and is never committed. |
| T-23-14 | Spoofing / Repudiation / Information Disclosure / Elevation of Privilege | — | accept | Static site, no server, no accounts, no secrets, no privilege model. Reclassifying rows in a public vocabulary file creates no new exposure. |
</threat_model>

<verification>
1. Whole-file headword gate: `awk -F'\t' 'NR>1 && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l` prints `0`
2. Long-sentence gate over `Unidad*` prints `0`
3. Slug whitelist gate prints `0`
4. Columns 1-3 diff clean; `wc -l` is `709`; `NF!=4` count is `0`
5. Topic distribution lists all nine slugs, with Calendario, Comida_Bebida and Escuela each `>= 10`
6. Serve the site and open `http://localhost:8000/topic.html?cat=Unidad5A` and `?cat=Unidad3` — full word lists render exactly as before; `http://localhost:8000/sentences.html` still lists the same category checkboxes and plays sentences normally
</verification>

<success_criteria>
- All 296 Unidad2/3/4/5A headwords carry exactly one of the nine slugs while keeping their `category` (TAG-02, ROADMAP criterion 4)
- Calendario, Comida_Bebida and Escuela are populated from unidad vocabulary (TAG-04, TAG-05, TAG-06)
- Every non-sentence row in the entire file is now tagged
- Long example sentences remain blank and present (D-06)
- Columns 1-3 byte-identical to the pre-plan file (D-04, NAV-07)
- No dependency, package manager or build step added
</success_criteria>

<output>
Create `.planning/phases/23-vocabulary-data-topic-tagging/23-03-SUMMARY.md` when done. Record the
topic distribution printout in the summary so plan 05 can diff against it.
Commit message prefix for this plan: `data(23-03):`
</output>
