---
phase: 23-vocabulary-data-topic-tagging
plan: 05
type: execute
wave: 4
depends_on: ["23-01", "23-04"]
files_modified:
  - data/words.tsv
autonomous: false
requirements: [NAV-07, DATA-03, DATA-04, DATA-05, TAG-01, TAG-02, TAG-03, TAG-04, TAG-05, TAG-06]

must_haves:
  truths:
    - "Every Spanish+German pair that existed before Phase 23 still exists in words.tsv, so every starred practice-list entry still resolves (NAV-07, D-04, ROADMAP criterion 5)"
    - "Every non-sentence row in the whole file carries exactly one of the nine canonical slugs, and no column-4 cell anywhere contains a comma or a second slug (D-02, TAG-01 through TAG-06)"
    - "Untagged rows are exactly the long example sentences, and they still load (D-03, D-06, D-08)"
    - "topic.html browses and quizzes Unidad5B, and Build Sentences shows a Unidad5B checkbox enabled by default (DATA-03, DATA-04)"
    - "Every existing screen behaves exactly as it did before Phase 23"
  artifacts:
    - path: "data/words.tsv"
      provides: "final audited 4-column vocabulary file"
      contains: "topics"
    - path: ".planning/phases/23-vocabulary-data-topic-tagging/23-05-SUMMARY.md"
      provides: "the final topic distribution and the NAV-07 proof, for Phase 24 to build on"
  key_links:
    - from: "data/words.tsv"
      to: "assets/js/tapvocab.js"
      via: "practice list matches stored entries on es+de equality (tapvocab.js isMarked/toggleMark)"
      pattern: "w.es === word.es && w.de === word.de"
    - from: "data/words.tsv"
      to: "assets/js/shared-utils.js"
      via: "loadWords projects category, es, de, topics"
      pattern: "topics"
---

<objective>
Close Phase 23: audit the finished `data/words.tsv` against every phase gate at once, prove NAV-07
mechanically, and confirm in a browser that no existing screen changed behaviour.

Purpose: NAV-07 and ROADMAP success criterion 5, plus an end-to-end confirmation of DATA-03,
DATA-04, DATA-05 and TAG-01 through TAG-06 now that all the data is in place. The three new topics
(Calendario, Comida_Bebida, Escuela) span plans 02, 03 and 04, so their sizes can only be checked
here.
Output: a clean audit, any small corrections applied directly, and a summary recording the final
topic distribution for Phase 24.

D-14: this plan does NOT approve a proposed mapping — the mapping is already applied. The human
checkpoint at the end verifies the running app, which is exactly the review CONTEXT.md describes.
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-02-SUMMARY.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-03-SUMMARY.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-04-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Final expected state of `data/words.tsv`: header `category` TAB `es` TAB `de` TAB `topics`;
708 original data rows in their original order, columns 1-3 untouched; plus 42-46 appended
`Unidad5B` rows. Every line has exactly 4 tab-separated fields.

The nine legal non-blank column-4 values (D-01): `Colores`, `Animales`, `Numeros`, `Saludar`,
`Casa_Familia`, `Palabras`, `Calendario`, `Comida_Bebida`, `Escuela`. Note deliberately: NOT
`Comida`, NOT `Casa Familia`, NOT `Comida y Bebida` — REQUIREMENTS.md § Data Model uses the
space-separated display spellings, and CONTEXT.md D-01 overrides it for the data file.

Original category distribution that must still hold exactly: Unidad3 143, Palabras 125,
Unidad5A 115, Unidad4 108, Unidad2 77, Saludar 40, Casa_Familia 33, Numeros 32, Animales 15,
Colores 13, xAnimales 5, xCasa_Familia 2 — plus the new Unidad5B.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Whole-file audit against every Phase 23 gate</name>
  <files>data/words.tsv</files>
  <read_first>
    - data/words.tsv (header plus a scan of every category's tagged values — `awk -F'\t' 'NR>1{print $1"\t"$4}' data/words.tsv | sort | uniq -c`)
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-01 through D-14 (the full decision list — this task checks all of them)
    - .planning/ROADMAP.md § Phase 23 — the 5 success criteria, with criterion 3 read as refined by D-08
    - The three prior SUMMARY files, for the counts each plan reported
  </read_first>
  <action>
    Reconstruct the pre-phase file and run every gate.

    Reconstruct: `mkdir -p /tmp/gsd23` then
    `BASE=$(git log --format='%H %s' -- data/words.tsv | grep -v '(23-0' | head -1 | cut -d' ' -f1)`
    and `git show $BASE:data/words.tsv > /tmp/gsd23/words-prephase.tsv`. The `grep -v '(23-0'`
    filter skips this phase's own commits, which all use the `data(23-0N):` message prefix; the
    result is the last version of the file before Phase 23 touched it. Sanity-check it:
    `wc -l /tmp/gsd23/words-prephase.tsv` must print `709` and
    `awk -F'\t' 'NF!=3' /tmp/gsd23/words-prephase.tsv | wc -l` must print `0` (the pre-phase file is
    3 columns).

    Then run the eight gates listed in the acceptance criteria below and fix any violation you find
    by editing column 4 only. Typical fixes: a slug typo, a row that slipped through untagged, a
    stray comma-joined pair (D-02 forbids multi-values), or a long example sentence that got tagged
    (D-06). Do NOT fix a violation by touching columns 1-3 — that would break NAV-07; if a gate
    fails on columns 1-3, stop and report it, because it means an earlier plan corrupted a row.

    Two things that are NOT defects and must not be "fixed":
    - Blank `topics` cells on long example sentences. D-03 makes blank the legitimate untagged
      value, D-06 leaves sentences untagged, and D-08 explicitly refines ROADMAP criterion 3 so the
      handful of untagged Palabras sentences are not a gap.
    - The `x` prefix still sitting on `xAnimales` / `xCasa_Familia` categories while those rows
      carry real topic tags. That asymmetry is D-05b, deliberate.

    Record the final per-topic distribution — `awk -F'\t' 'NR>1 && $4!=""{c[$4]++} END{for(k in c) printf "%s=%d\n",k,c[k]}' data/words.tsv | sort` — in the plan summary; Phase 24 builds its nine Topics buttons from exactly these names.
  </action>
  <verify>
    <automated>wc -l data/words.tsv; head -1 data/words.tsv | od -c | head -3</automated>
    <automated>awk -F'\t' 'NF!=4' data/words.tsv | wc -l; grep -c $'\t\t' data/words.tsv</automated>
    <automated>awk -F'\t' 'NR>1 && $4!="" && $4 !~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia|Palabras|Calendario|Comida_Bebida|Escuela)$/' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $2 ~ /[.?!]$/ {n=split($2,w," "); if(n>7 && $4!="") print}' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $1 ~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia)$/ && $4!=$1' data/words.tsv | wc -l; awk -F'\t' 'NR>1 && $1 ~ /^x/ && $4 != substr($1,2)' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1{print $1}' data/words.tsv | sort | uniq -c | sort -rn</automated>
    <automated>awk -F'\t' 'NR>1 && $4!=""{c[$4]++} END{for(k in c) printf "%s=%d\n",k,c[k]}' data/words.tsv | sort</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 — structure: `awk -F'\t' 'NF!=4' data/words.tsv | wc -l` prints `0`; `grep -c $'\t\t' data/words.tsv` prints `0`; the header is exactly `category`/`es`/`de`/`topics` tab-separated (confirm separators with `od -c`); `wc -l data/words.tsv` prints between `751` and `755`
    - Gate 2 — slug whitelist: the whitelist command prints `0`. No `Comida`, no `Casa Familia`, no comma-joined value (D-01, D-02)
    - Gate 3 — headwords tagged: `awk -F'\t' 'NR>1 && $2 !~ /[.?!]$/ && $4==""' data/words.tsv | wc -l` prints `0` (TAG-01, TAG-02, ROADMAP criteria 3 and 4 as refined by D-08)
    - Gate 4 — sentences untagged: the long-sentence gate prints `0`, and `awk -F'\t' 'NR>1 && $4==""' data/words.tsv | wc -l` prints a number between `150` and `200`, every one of which matches `/[.?!]$/`
    - Gate 5 — legacy self-tag: `awk -F'\t' 'NR>1 && $1 ~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia)$/ && $4!=$1' data/words.tsv | wc -l` prints `0`, and the x-row gate prints `0` (TAG-03, D-05, D-05b)
    - Gate 6 — categories intact: the category distribution shows exactly Unidad3 143, Palabras 125, Unidad5A 115, Unidad4 108, Unidad2 77, Saludar 40, Casa_Familia 33, Numeros 32, Animales 15, Colores 13, xAnimales 5, xCasa_Familia 2, plus Unidad5B between 42 and 46 — and no thirteenth original category (D-04)
    - Gate 7 — new topics populated: the topic distribution lists all nine slugs, with `Calendario >= 20`, `Comida_Bebida >= 12` and `Escuela >= 10` (TAG-04, TAG-05, TAG-06; REQUIREMENTS.md estimates ~33 / ~17 / ~16)
    - Gate 8 — partition happened: `awk -F'\t' 'NR>1 && $1=="Palabras" && $4!="Palabras" && $4!=""' data/words.tsv | wc -l` prints a number `>= 20` — Palabras words genuinely moved into topics rather than being blanket self-tagged (TAG-01)
    - Any violation found was fixed by editing column 4 only; `git diff` for this task shows no change in columns 1-3
  </acceptance_criteria>
  <done>All eight gates pass on the finished file, and the final per-topic distribution is captured for the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Prove NAV-07 — every pre-phase word still resolves</name>
  <files>data/words.tsv</files>
  <read_first>
    - assets/js/tapvocab.js — the `isMarked` / `toggleMark` pair (the practice list matches stored entries with `w.es === word.es && w.de === word.de`; `category` is never part of the match key and `topics` never will be)
    - assets/js/shared-utils.js — the `loadWords` projection changed in plan 01
    - .planning/phases/23-vocabulary-data-topic-tagging/23-PATTERNS.md § Practice-list storage shape
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-04
  </read_first>
  <action>
    NAV-07 says a word already starred for practice before the re-tagging must still be present and
    openable afterwards. `topic.html?cat=practice` reads the stored objects from the `practiceList`
    localStorage key and matches them against loaded rows by Spanish text plus German text only.
    So the requirement reduces to a provable set property: every `es`+`de` pair that existed before
    Phase 23 must still exist in `words.tsv`.

    Prove it with the real loader rather than by eyeballing the diff. Write a throwaway node script
    to `/tmp/gsd23/nav07-check.js` (never inside the repo, node built-ins only, no package install).
    The script must: set `global.window` to an empty object; set `global.fetch` to an async stub that
    returns an object with `ok: true` and a `text()` returning `require("fs").readFileSync(path,
    "utf8")`; `require` the real `assets/js/shared-utils.js` by absolute path so the IIFE publishes
    `window.SharedUtils`; call `window.SharedUtils.loadWords` twice — once on
    `/tmp/gsd23/words-prephase.tsv` (the pre-phase blob reconstructed in Task 1) and once on
    `data/words.tsv`; build a Set of `es + "|" + de` keys from the new file; report every pre-phase
    row whose key is absent; and exit non-zero if that list is not empty. This is the exact
    predicate `isMarked` uses, run over all 708 pre-phase rows instead of a sample.

    Then take three concrete spot entries and record them in the summary so a human can retrace the
    check by hand — pick one row from `Palabras`, one from a re-tagged unidad, and one from
    `xAnimales` (for example `el mono` / `der Affe`) — and confirm each still appears exactly once
    via `grep -c` on its `es` TAB `de` pair.

    If the script reports any lost pair, do NOT paper over it by re-adding a row: it means an
    earlier plan mutated column 2 or 3, which violates D-04. Restore the exact original `es`/`de`
    text from `/tmp/gsd23/words-prephase.tsv` for that row and re-run.
  </action>
  <verify>
    <automated>node /tmp/gsd23/nav07-check.js</automated>
    <automated>node -e 'global.window={};global.fetch=async p=>({ok:true,text:async()=>require("fs").readFileSync(p,"utf8")});require(process.cwd()+"/assets/js/shared-utils.js");(async()=>{const r=await window.SharedUtils.loadWords("data/words.tsv");const bad=r.filter(x=>typeof x.topics!=="string");console.log("rows",r.length,"badTopics",bad.length,"sample",JSON.stringify(r[0]));process.exit(bad.length===0?0:1)})()'</automated>
    <automated>awk -F'\t' 'NR==FNR && FNR>1{k[$2"|"$3];next} FNR>1{seen[$2"|"$3]} END{n=0; for(x in k) if(!(x in seen)) n++; print "lost_pairs="n}' /tmp/gsd23/words-prephase.tsv data/words.tsv</automated>
  </verify>
  <acceptance_criteria>
    - `node /tmp/gsd23/nav07-check.js` exits 0 and prints `lost 0` — all 708 pre-phase `es`+`de` pairs are still present under the real `loadWords` projection (NAV-07, ROADMAP criterion 5)
    - The pure-awk cross-check prints `lost_pairs=0` (independent of the node harness, in case the loader itself is at fault)
    - The `loadWords` smoke command exits 0: every row of the finished file has `typeof row.topics === "string"` and the printed sample row shows four keys `category`, `es`, `de`, `topics` (DATA-05)
    - Three named spot entries — one `Palabras`, one unidad, one `xAnimales` — are recorded in the summary with their exact `es` and `de` values, and `grep -c` finds each pair exactly once in `data/words.tsv`
    - `/tmp/gsd23/nav07-check.js` exists only under `/tmp`; `git status --porcelain` shows no new file inside the repo
  </acceptance_criteria>
  <done>Every pre-phase Spanish+German pair provably survives, so no starred practice-list entry can have been orphaned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the running app</name>
  <files>none — verification only, no file is modified by this task</files>
  <action>
    Start the static server from the repo root with `python3 -m http.server 8000` (CLAUDE.md: the
    site uses absolute asset paths, so `file://` will not work), leave it running in the background,
    then hand the seven verification steps below to the user and stop. Do not proceed, do not
    self-approve, and do not edit `data/words.tsv` while waiting. If the user reports a topic
    assignment they disagree with, apply that correction to column 4 only and re-run the Task 1
    gates before finishing — per D-14 corrections are ordinary follow-up edits, not a re-planning
    event.
  </action>
  <what-built>
    `data/words.tsv` now has a 4th `topics` column with every headword tagged into one of nine
    topics, Unidad 5B has been added from the two source images, and `SharedUtils.loadWords` projects
    the new column. Nothing about navigation or filtering changed — Phase 24 owns that. Every check
    that can be automated has already run and passed: field counts, slug whitelist, category
    preservation, and a full proof that every pre-existing Spanish+German pair survived.
  </what-built>
  <how-to-verify>
    Start a local server from the repo root: `python3 -m http.server 8000`. Then:

    1. Open http://localhost:8000/topic.html?cat=Unidad5B — Browse shows the new 5B cards. Tap
       through several. The speaker reads the Spanish without saying anything like "abrir Klammer
       ue" — the stem-change hints were stripped (D-10). Expected: ~27 headwords plus their example
       sentences.
    2. Switch to the Quiz tab on the same page — cards flip and score normally, and a correct answer
       still adds a coin.
    3. Open http://localhost:8000/sentences.html and open the sentence manager (settings) — a
       `Unidad5B` checkbox is present and already ticked. Untick everything except `Unidad5B`, save,
       and confirm 5B sentences are what you get.
    4. Open http://localhost:8000/topic.html?cat=Colores and ?cat=Palabras — these look and behave
       exactly as they did before this phase. Old bookmarks still work.
    5. Star a word (⭐) on any page, then open Practice from the home screen — it appears. If you had
       words starred on this device before today, they must all still be there and still open.
    6. Open the browser console on any of these pages — no new errors or warnings.
    7. Skim `git diff` for `data/words.tsv` — every changed line should differ only by the text after
       the last tab, plus the appended Unidad5B block at the end. Flag any topic assignment you
       disagree with; corrections are ordinary follow-up edits (D-14).
  </how-to-verify>
  <verify>
    <human-check>All seven steps above behave as described; any disputed topic assignments are noted for a follow-up edit</human-check>
  </verify>
  <acceptance_criteria>
    - Unidad5B browses and quizzes at `topic.html?cat=Unidad5B` with speech reading clean Spanish (DATA-03)
    - A `Unidad5B` checkbox appears in Build Sentences, ticked by default (DATA-04)
    - `topic.html?cat=Colores` and `?cat=Palabras` behave exactly as before the phase
    - Previously starred practice words are all still present and openable (NAV-07)
    - No new console errors or warnings on any page
    - The user has either approved the topic assignments or listed the ones to change
  </acceptance_criteria>
  <resume-signal>Type "approved", or list the topic assignments you want changed.</resume-signal>
  <done>The user has exercised the app and confirmed nothing regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/words.tsv` → `loadWords` → DOM | Author-committed vocabulary rendered with `textContent` in `tapvocab.js`; `sentences.js` uses `innerHTML` only with its own literal strings. |
| browser `localStorage` → `topic.html?cat=practice` | Stored `practiceList` entries are matched against loaded rows by `es`+`de`. Entries saved before this phase carry a stale `category` and no `topics` field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-20 | Denial of Service | A single corrupted row anywhere in the 750-row file breaks `loadWords` and blanks every learning page | mitigate | Gate 1 checks field counts and the header across the whole finished file; Task 2 loads the file through the real loader; Task 3 opens four pages in a browser. |
| T-23-21 | Tampering | Silent loss or mutation of an `es`/`de` pair orphans a starred practice-list entry with no error message anywhere | mitigate | Task 2 proves set inclusion for all 708 pre-phase pairs using the exact `isMarked` predicate, plus an independent awk cross-check that does not depend on the loader being correct. |
| T-23-22 | Tampering | A stale `topics`-less entry already in a user's `practiceList` is read by future code that assumes the field exists | accept | Phase 23 ships no reader of `topics`. Recorded for Phase 24: any consumer must tolerate `undefined` on practice-list entries. Migrating the practice list to stable IDs is deferred pre-existing debt. |
| T-23-23 | Information Disclosure | The `x`-prefixed rows become reachable through the Topics axis | accept | Deliberate under D-05b; the 7 rows are ordinary animal and family vocabulary already public in this repo. |
| T-23-SC | Tampering (supply chain) | npm/pip/cargo installs | mitigate | Zero packages installed across the entire phase. The audit harness uses node built-ins only and lives in `/tmp/gsd23`; `git status --porcelain` is checked so nothing leaks into the repo. |
| T-23-24 | Spoofing / Repudiation / Elevation of Privilege | — | accept | Static GitHub Pages site: no server, no accounts, no sessions, no privilege model. |
</threat_model>

<verification>
1. All eight gates in Task 1 pass
2. `node /tmp/gsd23/nav07-check.js` exits 0 printing `lost 0`; the awk cross-check prints `lost_pairs=0`
3. The `loadWords` smoke command exits 0 and shows a four-key row object
4. The human checkpoint is approved
5. `git status --porcelain` shows no stray helper script or fixture inside the repo
</verification>

<success_criteria>
- ROADMAP criterion 1: Unidad 5B headwords and sentences present, browsable/quizzable, and visible as a Build Sentences checkbox
- ROADMAP criterion 2: `loadWords` reads `topics` by header name; a file without the column, or with it blank, still loads with no crash
- ROADMAP criterion 3 (as refined by D-08): every Palabras headword either moved into one of the 8 non-Palabras topics or still carries `topics: Palabras`; no row lost
- ROADMAP criterion 4: every Unidad2/3/4/5A/5B row keeps its original `category` and additionally carries a topic
- ROADMAP criterion 5: every word starred before the tagging pass is still present and openable (proved mechanically for all 708 pre-phase pairs)
- No dependency, package manager or build step added anywhere in the phase
</success_criteria>

<output>
Create `.planning/phases/23-vocabulary-data-topic-tagging/23-05-SUMMARY.md` when done. It must
record: the final per-topic distribution (the nine slug counts), the final `Unidad5B` row count, the
three NAV-07 spot entries, and a note for Phase 24 that practice-list entries saved before this
phase have no `topics` field and any reader must tolerate `undefined`.
Commit message prefix for this plan: `data(23-05):`
</output>
