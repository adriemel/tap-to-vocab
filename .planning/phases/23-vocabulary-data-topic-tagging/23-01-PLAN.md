---
phase: 23-vocabulary-data-topic-tagging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/js/shared-utils.js
  - tap-to-vocab-walkthrough.md
autonomous: true
requirements: [DATA-05]

must_haves:
  truths:
    - "loadWords returns a topics string on every row when words.tsv carries a topics column (D-01)"
    - "loadWords returns topics as the empty string and drops zero rows when the TSV has no topics header (D-03, DATA-05)"
    - "loadWords returns the topics cell as a single unsplit string — it never splits on commas and never returns an array (D-02)"
    - "loadWords returns topics as the empty string for a row whose topics cell is blank, and keeps that row (D-03, D-06, D-08)"
    - "topic.html browse/quiz and sentences.html behave exactly as they did before this change"
  artifacts:
    - path: "assets/js/shared-utils.js"
      provides: "loadWords projects the topics column by header name"
      contains: "header.indexOf(\"topics\")"
    - path: "tap-to-vocab-walkthrough.md"
      provides: "plain-English description of the 4th words.tsv column and the loader change (code-walkthrough skill)"
      contains: "topics"
  key_links:
    - from: "assets/js/shared-utils.js"
      to: "assets/js/tapvocab.js"
      via: "SharedUtils.loadWords consumed at tapvocab.js:466"
      pattern: "loadWords"
    - from: "assets/js/shared-utils.js"
      to: "assets/js/sentences.js"
      via: "SharedUtils.loadWords consumed at sentences.js:318"
      pattern: "loadWords"
---

<objective>
Teach `SharedUtils.loadWords` to project a 4th `topics` field, read by header name, tolerant of the
column being absent or blank. This is the ONLY code change in Phase 23.

Purpose: DATA-05. Phase 24's Topics screens will filter on this field. The tolerance is what makes
the data migration (plans 02-04) safe in any order and what keeps blank-topic rows (example
sentences, D-06/D-08) alive.
Output: A two-line change in `loadWords`, plus the updated plain-English walkthrough required by the
`code-walkthrough` skill.

Source conflict resolved: REQUIREMENTS.md § Data Model shows the food topic as `Comida` and writes
topic names with spaces. CONTEXT.md D-01 overrides that — slugs are PascalCase with underscores
(`Comida_Bebida`, `Casa_Familia`). Display labels with spaces and emoji are Phase 24's HTML. Do not
"fix" slugs back to the REQUIREMENTS.md spelling.
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@assets/js/shared-utils.js
</context>

<interfaces>
The contract this plan establishes, consumed by Phase 24 and by plans 02-04's verification:

`SharedUtils.loadWords(tsvPath)` resolves to an array of objects with exactly four string keys:
`category`, `es`, `de`, `topics`. `topics` is `""` when the column is missing from the file, when
the cell is empty, or when the row is short a field. It is never `undefined`, never `null`, and is
never split on commas (D-02: exactly one topic per row).

`SharedUtils.loadTSV(tsvPath)` is already fully header-keyed (shared-utils.js:41-56) and picks the
new column up automatically. It must not be touched.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Project the topics column in loadWords</name>
  <files>assets/js/shared-utils.js</files>
  <read_first>
    - assets/js/shared-utils.js lines 19-56 (both `loadWords` and the `loadTSV` sibling that shows the tolerant idiom)
    - .planning/phases/23-vocabulary-data-topic-tagging/23-PATTERNS.md § `assets/js/shared-utils.js` — has the verbatim current function body and the exact change
    - .planning/codebase/CONVENTIONS.md § TSV Parsing Pattern — the `(cols[i] || "").trim()` guard rule
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-01, D-02, D-03
  </read_first>
  <action>
    Edit `loadWords` in assets/js/shared-utils.js (lines 19-38) and nothing else in the file.

    Two additions, both inside `loadWords`:
    1. In the `idx` object literal that currently holds `category`, `es`, `de`, add a fourth entry
       named `topics` whose value is `header.indexOf("topics")`. The header string must be exactly
       `topics`, lowercase — that is the column name D-01 puts in the TSV.
    2. In the object literal returned by the `.map` callback, add a fourth key named exactly
       `topics`, built with the identical ternary-guard idiom already used by the other three keys:
       take `cols[idx.topics]` when `idx.topics >= 0`, otherwise the empty string, then `.trim()`.
       When the file has no `topics` header, `header.indexOf("topics")` is -1, so the guard yields
       `""` — this is the DATA-05 tolerance and the reason no `if` statement, fallback value, or
       `console.warn` is needed or allowed.

    Explicit prohibitions, each with its reason:
    - Do NOT add `topics` to the trailing `.filter(r => r.category && r.es && r.de)`. Adding it
      would silently drop every blank-topic row — the 173 example-sentence rows (D-06) and the 7
      long Palabras sentences (D-08) must survive.
    - Do NOT split the value on commas or any other separator. D-02 fixes exactly one topic per row
      and Phase 24 does a plain equality match; comma support is deferred as TAG-09.
    - Do NOT default a blank topic to `"Palabras"` or any other slug. D-03: blank means untagged,
      full stop, and no console warning.
    - Do NOT modify `loadTSV` (lines 41-56). It is already header-keyed and needs zero changes.
    - Do NOT add a dependency, package.json entry, or build step. This repo is zero-dependency by
      CLAUDE.md; the verification harness below runs on node built-ins only and lives in /tmp.

    Before running the harness, create the throwaway fixture and directory:
    `mkdir -p /tmp/gsd23` and write a 2-line TSV at `/tmp/gsd23/legacy3col.tsv` whose header row is
    the three tab-separated names `category`, `es`, `de` and whose single data row is the three
    tab-separated values `Colores`, `negro`, `schwarz`. This fixture is the "old file, no topics
    column" case from DATA-05. It must never be written inside the repo.
  </action>
  <verify>
    <automated>grep -c 'header.indexOf("topics")' assets/js/shared-utils.js</automated>
    <automated>node -e 'global.window={};global.fetch=async p=>({ok:true,text:async()=>require("fs").readFileSync(p.replace(/^\/(data|assets)\//,"$1/"),"utf8")});require("./assets/js/shared-utils.js");(async()=>{const r=await window.SharedUtils.loadWords("data/words.tsv");const bad=r.filter(x=>typeof x.topics!=="string");const n=require("fs").readFileSync("data/words.tsv","utf8").split(/\r?\n/).filter(Boolean).length-1;console.log("rows",r.length,"expected",n,"badTopics",bad.length);process.exit(r.length===n&&bad.length===0?0:1)})()'</automated>
    <automated>node -e 'global.window={};global.fetch=async p=>({ok:true,text:async()=>require("fs").readFileSync(p,"utf8")});require("./assets/js/shared-utils.js");(async()=>{const r=await window.SharedUtils.loadWords("/tmp/gsd23/legacy3col.tsv");console.log(JSON.stringify(r));process.exit(r.length===1&&r[0].topics===""&&r[0].es==="negro"?0:1)})()'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'header.indexOf("topics")' assets/js/shared-utils.js` prints exactly `1`
    - `sed -n '19,38p' assets/js/shared-utils.js | grep -c 'console'` prints exactly `0` (no warning added, per D-03)
    - `grep -c 'filter(r => r.category && r.es && r.de)' assets/js/shared-utils.js` prints exactly `1` and that line contains no occurrence of `topics`
    - `git diff -U0 assets/js/shared-utils.js | grep -c '^+[^+]'` prints `4` or less — two genuinely new lines plus at most two comma-only rewrites of the lines above them; nothing outside `loadWords` lines 19-38 changes
    - `git diff -U0 assets/js/shared-utils.js | grep -c 'loadTSV'` prints exactly `0` — the sibling loader is untouched
    - Harness against the real `data/words.tsv` exits 0: every returned row has `typeof row.topics === "string"`, and the returned row count equals the file's non-empty line count minus 1 (708 today) — no row dropped
    - Harness against `/tmp/gsd23/legacy3col.tsv` (a 3-column TSV with no `topics` header) exits 0: 1 row returned, `row.topics === ""`, `row.es === "negro"` — proves DATA-05 tolerance
    - `git status --porcelain` shows no new file inside the repo other than the two files this plan owns
  </acceptance_criteria>
  <done>`loadWords` returns `{category, es, de, topics}` with `topics` always a string, `loadTSV` is byte-identical to before, and both harness commands exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Update the plain-English walkthrough for the new column</name>
  <files>tap-to-vocab-walkthrough.md</files>
  <read_first>
    - /home/desire/.claude/skills/code-walkthrough-skill/SKILL.md (the required document structure and tone)
    - tap-to-vocab-walkthrough.md lines 55-95 (the directory listing and the `#### words.tsv — The Vocabulary List` section)
    - tap-to-vocab-walkthrough.md lines 110-125 (the `### assets/js/shared-utils.js` section and its `#### TSV Loader` subsection)
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-01, D-03, D-04, D-06
  </read_first>
  <action>
    The repo already has a single whole-app walkthrough at the root, `tap-to-vocab-walkthrough.md`,
    organised as one section per file. Update that document — do NOT create a new
    `shared-utils-walkthrough.md`; a second competing document would fragment the one place a
    non-programmer maintainer looks.

    Three edits, all in the existing voice (plain English, no jargon, aimed at someone who is not a
    programmer):

    1. The directory-listing line near line 67 currently reads
       `words.tsv  ← Vocabulary: category, Spanish, German`. Extend it so it names all four columns
       including `topics`.
    2. The `#### words.tsv — The Vocabulary List` section (near line 82): add a short paragraph
       explaining the fourth column. It must state, in plain English: the column is called `topics`;
       each row carries at most one topic; the nine topic names written in the file are `Colores`,
       `Animales`, `Numeros`, `Saludar`, `Casa_Familia`, `Palabras`, `Calendario`, `Comida_Bebida`,
       `Escuela` (underscores instead of spaces, matching how `Casa_Familia` already works); the
       prettier labels with emoji live in the home-page buttons, not in the data; an empty topics
       cell is normal and deliberate — example sentences are left empty on purpose so topic lists
       stay short word lists; and the older `category` column was not changed at all, so every
       existing link and the starred practice list keep working (D-04).
    3. The `#### TSV Loader` subsection under `### assets/js/shared-utils.js` (near line 112):
       its worked example currently turns the row `Colores | negro | schwarz` into a three-key
       object. Update that example to a four-column row and a four-key object including `topics`,
       and add one sentence explaining that if a data file has no topics column at all, the loader
       hands back an empty topic rather than failing — which is why old copies of the file still
       work.

    Do not restructure or re-order the rest of the document. Do not describe Phase 24 navigation as
    if it already exists — this phase changes data and one loader only.
  </action>
  <verify>
    <automated>grep -c 'topics' tap-to-vocab-walkthrough.md</automated>
    <automated>for s in Colores Animales Numeros Saludar Casa_Familia Palabras Calendario Comida_Bebida Escuela; do grep -q "$s" tap-to-vocab-walkthrough.md || { echo "MISSING $s"; exit 1; }; done; echo ALL_SLUGS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'topics' tap-to-vocab-walkthrough.md` prints a number `>= 4`
    - The slug loop prints `ALL_SLUGS_PRESENT` — all nine D-01 slugs are named in the document
    - `grep -c 'de: "schwarz"}' tap-to-vocab-walkthrough.md` prints `0` — the stale three-key loader example is gone
    - `grep -n 'words.tsv' tap-to-vocab-walkthrough.md` still matches the directory-listing line and that line names four columns
    - `git diff --stat tap-to-vocab-walkthrough.md` (or `git status` for a first commit) shows changes confined to this one document
    - The document contains no code fences added beyond the existing example blocks, and no sentence claims a Topics home screen exists yet
  </acceptance_criteria>
  <done>`tap-to-vocab-walkthrough.md` explains the `topics` column and the loader's tolerance in plain English; all nine slugs are named; the stale three-key example is replaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages static host → browser | All HTML/CSS/JS/TSV is author-controlled and committed to git. Nothing user-supplied crosses this boundary. |
| `data/*.tsv` → DOM | TSV field values are rendered into pages. `tapvocab.js` uses `textContent` for word text (32 occurrences); `sentences.js` uses `innerHTML` only with its own literal strings, never with TSV content. |
| browser `localStorage` → app | `practiceList`, `enabledSentences`, `tapvocab_coins`. Same-origin, single-user, no privilege attached. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | `loadWords` projection of the new `topics` field | mitigate | Value is read with the existing `(cols[i] \|\| "").trim()` guard, stored as a plain string property, never split, never `eval`'d, never assigned to `innerHTML`. No new sink is introduced by this plan. |
| T-23-02 | Denial of Service | `loadWords` is the single load path for every learning page | mitigate | Fixture harness proves a 3-column TSV (no `topics` header) still returns all rows; the required-fields filter is unchanged, so no row class can be silently dropped and no page can be blanked by the change. |
| T-23-SC | Tampering (supply chain) | npm/pip/cargo installs | mitigate | This plan installs zero packages. The verification harness uses node built-ins (`fs`) only and writes its fixture to `/tmp/gsd23`, outside the repo. Any package install would violate CLAUDE.md's zero-dependency rule and is out of scope for this phase. |
| T-23-03 | Spoofing | — | accept | No accounts, no sessions, no server. There is no identity to spoof on a static GitHub Pages site. |
| T-23-04 | Repudiation | — | accept | No server-side actions and no audit surface. Git history is the only record and is unaffected. |
| T-23-05 | Information Disclosure | — | accept | The repo is public and contains only vocabulary data. No secrets, no PII, no credentials touched by this change. |
| T-23-06 | Elevation of Privilege | — | accept | No privilege model exists in the app; every visitor has identical capability. |
</threat_model>

<verification>
1. `grep -c 'header.indexOf("topics")' assets/js/shared-utils.js` prints `1`
2. Both node harness commands in Task 1 exit 0
3. `git diff -U0 assets/js/shared-utils.js | grep -c '^+[^+]'` prints 4 or less, and `git diff assets/js/shared-utils.js` shows no hunk outside lines 19-38
4. `python3 -m http.server 8000` from the repo root, then open `http://localhost:8000/topic.html?cat=Colores` — cards render and flip as before; open `http://localhost:8000/sentences.html` — sentences load and the settings panel lists the same categories as before
5. Browser console shows no new errors or warnings on either page
</verification>

<success_criteria>
- `SharedUtils.loadWords` returns `{category, es, de, topics}` for every row, with `topics` always a string
- A `words.tsv` without a `topics` column still loads every row, `topics === ""` (DATA-05, ROADMAP criterion 2)
- `loadTSV` is unchanged
- No row is dropped for having a blank topic
- `tap-to-vocab-walkthrough.md` documents the column and the tolerance in plain English
- Zero dependencies, zero build steps, zero new files inside the repo
</success_criteria>

<output>
Create `.planning/phases/23-vocabulary-data-topic-tagging/23-01-SUMMARY.md` when done.
Commit message prefix for this plan: `feat(23-01):`
</output>
