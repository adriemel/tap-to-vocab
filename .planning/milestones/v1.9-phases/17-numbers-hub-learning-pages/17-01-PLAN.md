---
phase: 17-numbers-hub-learning-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/js/numbers-data.js
  - index.html
  - assets/css/styles.css
autonomous: true
requirements:
  - NUM-01

must_haves:
  truths:
    - "window.NUMBERS is defined and contains exactly 100 entries (n: 1 through n: 100)"
    - "Each entry has a correct Spanish word for its numeral (e.g. {n:1, es:'uno'}, {n:16, es:'dieciséis'}, {n:100, es:'cien'})"
    - "Home page shows a 'Qué número es?' button between the Locations button and Play Games button"
    - "The button spans both columns of the two-column grid (full width) with a distinct dark-blue gradient style"
    - "Tapping the button navigates to /numbers.html"
  artifacts:
    - path: "assets/js/numbers-data.js"
      provides: "NUMBERS constant — all 100 Spanish numerals"
      exports: ["window.NUMBERS"]
      contains: "window.NUMBERS"
    - path: "index.html"
      provides: "Home page with numbers entry button"
      contains: "btn-numbers"
    - path: "assets/css/styles.css"
      provides: "CSS rule for numbers button"
      contains: ".btn-numbers"
  key_links:
    - from: "index.html"
      to: "/numbers.html"
      via: "<a class='btn btn-numbers' href='/numbers.html'>"
      pattern: "btn-numbers.*numbers\\.html"
    - from: "assets/js/numbers-data.js"
      to: "window.NUMBERS"
      via: "IIFE assignment"
      pattern: "window\\.NUMBERS\\s*="
---

<objective>
Create the numbers data module (window.NUMBERS constant with all 100 Spanish numerals) and wire the entry point on the home page.

Purpose: Plans 02 depends on numbers-data.js existing. The home page button (NUM-01) is self-contained and can be added here in Wave 1 alongside the data module.

Output:
- assets/js/numbers-data.js — IIFE module exporting window.NUMBERS[100 entries]
- index.html — "Qué número es?" button added between btn-locations and btn-games
- assets/css/styles.css — .btn-numbers CSS rule added after .btn-locations block
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/desire/tap-to-vocab/.planning/PROJECT.md
@/home/desire/tap-to-vocab/.planning/ROADMAP.md
@/home/desire/tap-to-vocab/.planning/STATE.md

<interfaces>
<!-- From assets/js/coins.js — the IIFE pattern to replicate exactly -->
(function () {
  // internal code
  window.CoinTracker = { addCoin: addCoin, getCoins: getCoins, ... };
})();

<!-- From index.html lines 52-56 — insertion context for new button -->
<!-- 📍 Locations - spans both columns -->
<a class="btn btn-locations" href="/locations.html">&#128205; Locations</a>

<!-- 🎮 Games - spans both columns, always visible -->
<button class="btn btn-games" id="btn-games">🎮 Play Games (10 <span class="coin-icon"></span>)</button>

<!-- From assets/css/styles.css lines 79-88 — CSS blocks to follow -->
.grid-two-col .btn-locations {
  grid-column: 1 / -1;
  background: linear-gradient(135deg, #3c4f6a 0%, #2a364a 100%);
  border: 2px solid var(--accent);
  font-weight: 700;
}

.grid-two-col .btn-games {
  grid-column: 1 / -1;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assets/js/numbers-data.js with complete NUMBERS constant</name>
  <read_first>
    - /home/desire/tap-to-vocab/assets/js/coins.js (IIFE pattern to replicate — read lines 1-10 and last 5 lines)
  </read_first>
  <files>assets/js/numbers-data.js</files>
  <action>
Create /home/desire/tap-to-vocab/assets/js/numbers-data.js as a new file using the project's IIFE pattern (same as coins.js: single wrapping IIFE, window assignment as only side effect, no "use strict", no ES module syntax).

The file must assign window.NUMBERS to an array of 100 objects, one per integer from 1 to 100. Each object has two fields: `n` (integer) and `es` (Spanish word string).

Use exactly these spellings — every entry listed explicitly (no "// ... pattern continues" shortcuts):

{ n: 1,   es: "uno" },
{ n: 2,   es: "dos" },
{ n: 3,   es: "tres" },
{ n: 4,   es: "cuatro" },
{ n: 5,   es: "cinco" },
{ n: 6,   es: "seis" },
{ n: 7,   es: "siete" },
{ n: 8,   es: "ocho" },
{ n: 9,   es: "nueve" },
{ n: 10,  es: "diez" },
{ n: 11,  es: "once" },
{ n: 12,  es: "doce" },
{ n: 13,  es: "trece" },
{ n: 14,  es: "catorce" },
{ n: 15,  es: "quince" },
{ n: 16,  es: "dieciséis" },
{ n: 17,  es: "diecisiete" },
{ n: 18,  es: "dieciocho" },
{ n: 19,  es: "diecinueve" },
{ n: 20,  es: "veinte" },
{ n: 21,  es: "veintiuno" },
{ n: 22,  es: "veintidós" },
{ n: 23,  es: "veintitrés" },
{ n: 24,  es: "veinticuatro" },
{ n: 25,  es: "veinticinco" },
{ n: 26,  es: "veintiséis" },
{ n: 27,  es: "veintisiete" },
{ n: 28,  es: "veintiocho" },
{ n: 29,  es: "veintinueve" },
{ n: 30,  es: "treinta" },
{ n: 31,  es: "treinta y uno" },
{ n: 32,  es: "treinta y dos" },
{ n: 33,  es: "treinta y tres" },
{ n: 34,  es: "treinta y cuatro" },
{ n: 35,  es: "treinta y cinco" },
{ n: 36,  es: "treinta y seis" },
{ n: 37,  es: "treinta y siete" },
{ n: 38,  es: "treinta y ocho" },
{ n: 39,  es: "treinta y nueve" },
{ n: 40,  es: "cuarenta" },
{ n: 41,  es: "cuarenta y uno" },
{ n: 42,  es: "cuarenta y dos" },
{ n: 43,  es: "cuarenta y tres" },
{ n: 44,  es: "cuarenta y cuatro" },
{ n: 45,  es: "cuarenta y cinco" },
{ n: 46,  es: "cuarenta y seis" },
{ n: 47,  es: "cuarenta y siete" },
{ n: 48,  es: "cuarenta y ocho" },
{ n: 49,  es: "cuarenta y nueve" },
{ n: 50,  es: "cincuenta" },
{ n: 51,  es: "cincuenta y uno" },
{ n: 52,  es: "cincuenta y dos" },
{ n: 53,  es: "cincuenta y tres" },
{ n: 54,  es: "cincuenta y cuatro" },
{ n: 55,  es: "cincuenta y cinco" },
{ n: 56,  es: "cincuenta y seis" },
{ n: 57,  es: "cincuenta y siete" },
{ n: 58,  es: "cincuenta y ocho" },
{ n: 59,  es: "cincuenta y nueve" },
{ n: 60,  es: "sesenta" },
{ n: 61,  es: "sesenta y uno" },
{ n: 62,  es: "sesenta y dos" },
{ n: 63,  es: "sesenta y tres" },
{ n: 64,  es: "sesenta y cuatro" },
{ n: 65,  es: "sesenta y cinco" },
{ n: 66,  es: "sesenta y seis" },
{ n: 67,  es: "sesenta y siete" },
{ n: 68,  es: "sesenta y ocho" },
{ n: 69,  es: "sesenta y nueve" },
{ n: 70,  es: "setenta" },
{ n: 71,  es: "setenta y uno" },
{ n: 72,  es: "setenta y dos" },
{ n: 73,  es: "setenta y tres" },
{ n: 74,  es: "setenta y cuatro" },
{ n: 75,  es: "setenta y cinco" },
{ n: 76,  es: "setenta y seis" },
{ n: 77,  es: "setenta y siete" },
{ n: 78,  es: "setenta y ocho" },
{ n: 79,  es: "setenta y nueve" },
{ n: 80,  es: "ochenta" },
{ n: 81,  es: "ochenta y uno" },
{ n: 82,  es: "ochenta y dos" },
{ n: 83,  es: "ochenta y tres" },
{ n: 84,  es: "ochenta y cuatro" },
{ n: 85,  es: "ochenta y cinco" },
{ n: 86,  es: "ochenta y seis" },
{ n: 87,  es: "ochenta y siete" },
{ n: 88,  es: "ochenta y ocho" },
{ n: 89,  es: "ochenta y nueve" },
{ n: 90,  es: "noventa" },
{ n: 91,  es: "noventa y uno" },
{ n: 92,  es: "noventa y dos" },
{ n: 93,  es: "noventa y tres" },
{ n: 94,  es: "noventa y cuatro" },
{ n: 95,  es: "noventa y cinco" },
{ n: 96,  es: "noventa y seis" },
{ n: 97,  es: "noventa y siete" },
{ n: 98,  es: "noventa y ocho" },
{ n: 99,  es: "noventa y nueve" },
{ n: 100, es: "cien" }

The file structure must be:
```javascript
(function () {
  window.NUMBERS = [
    { n: 1,   es: "uno" },
    // ... all 100 entries ...
    { n: 100, es: "cien" }
  ];
})();
```

No trailing comma after the last entry. No comments inside the array (data only). A short file-level comment at the top is acceptable: `// Spanish numerals 1–100 — shared constant for numbers pages`.
  </action>
  <verify>
    <automated>grep -c '{ n:' /home/desire/tap-to-vocab/assets/js/numbers-data.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '{ n:' /home/desire/tap-to-vocab/assets/js/numbers-data.js` outputs `100`
    - `grep 'window\.NUMBERS' /home/desire/tap-to-vocab/assets/js/numbers-data.js` returns a match
    - `grep 'dieciséis' /home/desire/tap-to-vocab/assets/js/numbers-data.js` returns `{ n: 16,  es: "dieciséis" }` (accent on é)
    - `grep '"cien"' /home/desire/tap-to-vocab/assets/js/numbers-data.js` returns `{ n: 100, es: "cien" }` (not "ciento")
    - `grep 'veintiuno' /home/desire/tap-to-vocab/assets/js/numbers-data.js` returns `{ n: 21,  es: "veintiuno" }`
    - File starts with `(function ()` and ends with `})();`
  </acceptance_criteria>
  <done>assets/js/numbers-data.js exists with all 100 entries, correct spellings, IIFE pattern, window.NUMBERS export</done>
</task>

<task type="auto">
  <name>Task 2: Add "Qué número es?" button to index.html and CSS rule to styles.css</name>
  <read_first>
    - /home/desire/tap-to-vocab/index.html (read in full — identify the btn-locations line and btn-games line as insertion context)
    - /home/desire/tap-to-vocab/assets/css/styles.css (read lines 79-90 — the .btn-locations and .btn-games blocks as insertion context)
  </read_first>
  <files>index.html, assets/css/styles.css</files>
  <action>
**index.html modification:**

Locate the comment `<!-- 📍 Locations - spans both columns -->` and the line immediately after it:
```html
<a class="btn btn-locations" href="/locations.html">&#128205; Locations</a>
```

Insert the following block AFTER the btn-locations line and BEFORE the next comment block (`<!-- 🎮 Games`):
```html

        <!-- 🔢 Numbers - spans both columns -->
        <a class="btn btn-numbers" href="/numbers.html">🔢 Qué número es?</a>
```

The result in index.html must be (in order):
1. `<a class="btn btn-locations" href="/locations.html">...` (existing)
2. (blank line)
3. `<!-- 🔢 Numbers - spans both columns -->`
4. `<a class="btn btn-numbers" href="/numbers.html">🔢 Qué número es?</a>`
5. (blank line)
6. `<!-- 🎮 Games - spans both columns, always visible -->` (existing)
7. `<button class="btn btn-games" ...>` (existing)

**styles.css modification:**

Locate the `.grid-two-col .btn-locations` block (lines 79-84):
```css
.grid-two-col .btn-locations {
  grid-column: 1 / -1;
  background: linear-gradient(135deg, #3c4f6a 0%, #2a364a 100%);
  border: 2px solid var(--accent);
  font-weight: 700;
}
```

Insert the following block AFTER the closing `}` of `.btn-locations` and BEFORE the `.grid-two-col .btn-games` block:
```css

.grid-two-col .btn-numbers {
  grid-column: 1 / -1;
  background: linear-gradient(135deg, #2a3a5a 0%, #1a2540 100%);
  border: 2px solid var(--accent);
  font-weight: 700;
}
```

Note: The numbers button uses `#2a3a5a` / `#1a2540` — a noticeably darker blue than the locations button (`#3c4f6a` / `#2a364a`) to visually differentiate the two adjacent full-width buttons.

Do NOT use `border: 2px solid var(--warn)` (that is the Practice button's color). Use `var(--accent)` to match the numbers/learning palette.
  </action>
  <verify>
    <automated>grep -n 'btn-numbers' /home/desire/tap-to-vocab/index.html && grep -n 'btn-numbers' /home/desire/tap-to-vocab/assets/css/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'btn-numbers' /home/desire/tap-to-vocab/index.html` returns `<a class="btn btn-numbers" href="/numbers.html">🔢 Qué número es?</a>`
    - `grep -A1 'btn-locations' /home/desire/tap-to-vocab/index.html` shows btn-locations BEFORE btn-numbers
    - `grep -A1 'btn-numbers' /home/desire/tap-to-vocab/index.html` (in context) shows btn-numbers BEFORE btn-games
    - `grep 'btn-numbers' /home/desire/tap-to-vocab/assets/css/styles.css` returns `.grid-two-col .btn-numbers {`
    - `grep -A4 'btn-numbers' /home/desire/tap-to-vocab/assets/css/styles.css` shows `grid-column: 1 / -1;` and `background: linear-gradient(135deg, #2a3a5a 0%, #1a2540 100%);`
    - Visual check: open index.html in browser, confirm "Qué número es?" appears as a full-width button between Locations and Play Games
  </acceptance_criteria>
  <done>index.html has btn-numbers anchor linking to /numbers.html, positioned between btn-locations and btn-games. styles.css has .grid-two-col .btn-numbers rule with grid-column: 1/-1 and dark-blue gradient.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No significant trust boundaries | This plan creates a static data file and modifies existing static HTML/CSS — no user input, no server, no dynamic data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | assets/js/numbers-data.js | accept | Static file served over HTTPS (GitHub Pages); no runtime mutation of NUMBERS constant needed; constant is read-only by consuming pages |
| T-17-01-02 | Information Disclosure | index.html btn-numbers href | accept | href="/numbers.html" is a public navigation link — no sensitive information in the URL |
</threat_model>

<verification>
After both tasks complete:
1. `python3 -m http.server 8000` from /home/desire/tap-to-vocab
2. Open http://localhost:8000 — confirm "Qué número es?" button appears between Locations and Play Games, full-width, dark-blue gradient
3. Tap/click the button — confirm browser navigates to /numbers.html (will 404 until Plan 02 creates it — this is expected at Plan 01 stage)
4. In browser console: `window.NUMBERS.length` should return 100 after loading a page that includes numbers-data.js
5. `grep -c '{ n:' assets/js/numbers-data.js` outputs 100
</verification>

<success_criteria>
- assets/js/numbers-data.js exists with exactly 100 entries from n:1 to n:100 using IIFE pattern
- window.NUMBERS[0] = {n:1, es:"uno"}, window.NUMBERS[15] = {n:16, es:"dieciséis"}, window.NUMBERS[99] = {n:100, es:"cien"}
- index.html contains the btn-numbers anchor in correct position (after Locations, before Games)
- styles.css contains .grid-two-col .btn-numbers with grid-column: 1 / -1
</success_criteria>

<output>
After completion, create `.planning/phases/17-numbers-hub-learning-pages/17-01-SUMMARY.md`
</output>
