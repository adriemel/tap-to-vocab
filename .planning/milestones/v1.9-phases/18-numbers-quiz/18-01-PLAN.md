---
plan: 18-01
phase: 18
wave: 1
title: "Implement Numbers Quiz flip-card grid with TTS"
depends_on: []
files_modified:
  - assets/css/styles.css
  - numbers-quiz.html
requirements: [NUM-05, NUM-06, NUM-07]
autonomous: true
must_haves:
  truths:
    - "Quiz page shows all numbers in the selected range as a 4-column grid of face-down cards with the numeral visible on the front face"
    - "Tapping an unflipped card flips it to reveal the Spanish word on the back face"
    - "Each flip triggers Web Speech API to speak the Spanish word aloud in a Spanish voice"
    - "Already-flipped cards stay flipped; re-tapping re-speaks TTS without flipping back"
  artifacts:
    - path: "assets/css/styles.css"
      provides: "Compact number-quiz card CSS classes"
      contains: ".nq-grid"
    - path: "numbers-quiz.html"
      provides: "Inline IIFE quiz logic: URL param parsing, grid render, flip + TTS handler"
      contains: "quiz-grid"
  key_links:
    - from: "numbers-quiz.html inline script"
      to: "window.NUMBERS"
      via: "numbers-data.js loaded before script tag"
      pattern: "window\\.NUMBERS"
    - from: "card click handler"
      to: "speakSpanish()"
      via: "addEventListener click on .nq-card"
      pattern: "speakSpanish"
---

<objective>
Implement the numbers flip-card quiz in numbers-quiz.html.

The page already has an empty `#quiz-grid` div and `numbers-data.js` loaded. This plan adds:

1. Compact card CSS classes to styles.css (`.nq-grid`, `.nq-card`, `.nq-card-inner`, `.nq-card-front`, `.nq-card-back`, `.nq-card.flipped`) sized for 4-per-row mobile fit without scroll.
2. An inline IIFE script in numbers-quiz.html that parses `?range=lo-hi`, filters `window.NUMBERS`, renders flip cards into `#quiz-grid`, and handles tap-to-flip + TTS.

Purpose: Completes the v1.9 milestone — users can now tap through any number range and hear the Spanish pronunciation.
Output: A fully functional numbers-quiz.html with 4-column flip-card grid and TTS.
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/desire/tap-to-vocab/.planning/ROADMAP.md
@/home/desire/tap-to-vocab/.planning/phases/18-numbers-quiz/18-CONTEXT.md

<interfaces>
<!-- Key patterns the executor must follow. Extracted from codebase. -->

From assets/js/numbers-data.js:
  window.NUMBERS — array of { n: number, es: string }, 1–100.
  Already loaded via <script src="/assets/js/numbers-data.js"> in numbers-quiz.html stub before the inline script.

From assets/js/tapvocab.js lines 13–53 (TTS block — copy verbatim):
  let _cachedSpanishVoice = null;
  let _voicesLoaded = false;

  function getSpanishVoice() {
    const voices = window.speechSynthesis.getVoices();
    if (!voices.length) return null;
    const normalize = s => (s || "").normalize("NFD").replace(/[̀-ͯ]/g, "").toLowerCase();
    const monica = voices.find(v => normalize(v.name).includes("monica") && v.lang.toLowerCase().startsWith("es"));
    if (monica) { _cachedSpanishVoice = monica; return monica; }
    const preferred = voices.filter(v => v.lang && v.lang.toLowerCase().startsWith("es"));
    _cachedSpanishVoice = preferred[0] || voices[0] || null;
    return _cachedSpanishVoice;
  }

  if (typeof speechSynthesis !== "undefined") {
    var _initialVoices = speechSynthesis.getVoices();
    if (_initialVoices.length) { _voicesLoaded = true; getSpanishVoice(); }
    if (speechSynthesis.onvoiceschanged !== undefined) {
      speechSynthesis.addEventListener("voiceschanged", function () {
        _voicesLoaded = true;
        getSpanishVoice();
      });
    }
  }

  function speakSpanish(text) {
    try {
      const u = new SpeechSynthesisUtterance(text);
      u.lang = "es-ES";
      const v = getSpanishVoice();
      if (v) u.voice = v;
      u.rate = 0.95; u.pitch = 1.0;
      window.speechSynthesis.cancel();
      window.speechSynthesis.speak(u);
    } catch (e) { console.warn("Speech synthesis failed:", e); }
  }

From numbers-learn.html inline script (URL param parsing — copy verbatim):
  var params = new URLSearchParams(location.search);
  var range = params.get("range") || "1-20";
  var parts = range.split("-");
  var lo = parseInt(parts[0], 10);
  var hi = parseInt(parts[1], 10);
  if (isNaN(lo) || isNaN(hi) || lo < 1 || hi > 100 || lo > hi) {
    lo = 1; hi = 20; range = "1-20";
  }

CSS variables from styles.css :root (use these — do NOT hardcode hex where a variable exists):
  --bg: #152238
  --card: #121831
  --ink: #e8ecff
  --muted: #aeb6d9
  --ok: #3ddc97
  --warn: #ffcc66

Reference image (WhichNumberQuizz.png):
  Dark navy card background (~#0d1740), bold yellow/gold numeral text, 4-column grid,
  roughly square cards with rounded corners, subtle navy border between card and background.
</interfaces>
</context>

<tasks>

<task id="18-01-T01" title="Add compact number-quiz flip-card CSS to styles.css">
  <objective>Add 6 new CSS rule blocks (.nq-grid, .nq-card, .nq-card-inner, .nq-card-front, .nq-card-back, .nq-card.flipped) to the END of assets/css/styles.css. These must produce cards that fit 4-per-row at 375px mobile viewport without triggering vertical scroll, using the same 3D flip mechanism as the existing .flip-card block.</objective>

  <read_first>
    - file: assets/css/styles.css
      why: Read lines 389–465 to understand the existing flip-card 3D CSS machinery (perspective, transform-style, backface-visibility, rotateY). The new compact variant reuses the exact same 3D transform approach — only the dimensions and colors change.
    - file: /home/desire/tap-to-vocab/WhichNumberQuizz.png
      why: Visual reference for card dimensions, color, and density — dark navy background, gold/yellow numeral, ~square cards, 4-column tight grid.
  </read_first>

  <action>
Append the following CSS block to the END of assets/css/styles.css (after the last existing rule).

Exact class names and property values to use:

1. `.nq-grid` — the grid container:
   ```css
   .nq-grid {
     display: grid;
     grid-template-columns: repeat(4, 1fr);
     gap: 8px;
     margin-bottom: 16px;
   }
   ```

2. `.nq-card` — the perspective wrapper (per-card):
   ```css
   .nq-card {
     perspective: 600px;
     cursor: pointer;
     -webkit-tap-highlight-color: transparent;
   }
   ```

3. `.nq-card-inner` — the rotating layer:
   ```css
   .nq-card-inner {
     position: relative;
     width: 100%;
     aspect-ratio: 1 / 1;
     transition: transform 0.5s cubic-bezier(0.4, 0.2, 0.2, 1);
     transform-style: preserve-3d;
   }
   ```
   Note: `aspect-ratio: 1 / 1` makes cards square regardless of content. This is the key sizing rule for mobile 4-per-row fit — no min-height needed.

4. `.nq-card.flipped .nq-card-inner` — the flipped state:
   ```css
   .nq-card.flipped .nq-card-inner {
     transform: rotateY(180deg);
   }
   ```

5. `.nq-card-front` and `.nq-card-back` — shared base:
   ```css
   .nq-card-front,
   .nq-card-back {
     position: absolute;
     inset: 0;
     backface-visibility: hidden;
     -webkit-backface-visibility: hidden;
     border-radius: 10px;
     display: flex;
     align-items: center;
     justify-content: center;
   }
   ```

6. `.nq-card-front` — front face (number, dark navy + gold per D-01):
   ```css
   .nq-card-front {
     background: #0d1740;
     border: 2px solid #1e2d6b;
   }
   ```
   And for the numeral text element inside (selector `.nq-num`):
   ```css
   .nq-num {
     font-size: clamp(1.2rem, 5vw, 1.8rem);
     font-weight: 800;
     color: #f5d800;
     line-height: 1;
     text-align: center;
   }
   ```

7. `.nq-card-back` — back face (green border per D-03, ink text per D-04):
   ```css
   .nq-card-back {
     background: #0d1740;
     border: 2px solid var(--ok);
     transform: rotateY(180deg);
     padding: 4px;
   }
   ```
   And for the Spanish word text element inside (selector `.nq-word`):
   ```css
   .nq-word {
     font-size: clamp(0.65rem, 2.8vw, 1rem);
     font-weight: 700;
     color: var(--ink);
     text-align: center;
     line-height: 1.2;
     word-break: break-word;
   }
   ```

All 8 rule blocks (including `.nq-num` and `.nq-word`) must be appended as a contiguous section with the comment:
```css
/* ── Numbers Quiz flip cards ─────────────────────────────── */
```
  </action>

  <acceptance_criteria>
    - grep -n "\.nq-grid" assets/css/styles.css returns a line with "grid-template-columns: repeat(4, 1fr)"
    - grep -n "\.nq-card-inner" assets/css/styles.css returns a line with "aspect-ratio: 1 / 1"
    - grep -n "\.nq-card\.flipped" assets/css/styles.css returns a line with "rotateY(180deg)"
    - grep -n "\.nq-card-front" assets/css/styles.css returns a line with "background: #0d1740"
    - grep -n "\.nq-card-back" assets/css/styles.css returns a line with "border: 2px solid var(--ok)"
    - grep -n "\.nq-num" assets/css/styles.css returns a line with "color: #f5d800"
    - grep -n "\.nq-word" assets/css/styles.css returns a line with "color: var(--ink)"
    - grep -n "backface-visibility: hidden" assets/css/styles.css shows at least one line in the new nq-* block
    - grep -n "Numbers Quiz flip cards" assets/css/styles.css returns the section comment line
  </acceptance_criteria>
</task>

<task id="18-01-T02" title="Implement inline quiz IIFE in numbers-quiz.html">
  <objective>Replace the numbers-quiz.html stub content with a fully functional page: remove the "Quiz coming soon" placeholder, update the `#quiz-grid` div to use class `nq-grid`, add a `<style>` block for `#quiz-grid` to accept the nq-grid layout, and insert an inline IIFE `<script>` tag after the numbers-data.js script tag. The IIFE must: (1) copy the TTS block from tapvocab.js verbatim, (2) parse `?range=lo-hi` using the numbers-learn.html pattern verbatim, (3) render cards into `#quiz-grid` using createElement+textContent exclusively, (4) handle tap-to-flip (add .flipped class) + TTS on click, (5) re-speak TTS on re-tap of already-flipped card (D-05).</objective>

  <read_first>
    - file: numbers-quiz.html
      why: The current stub — understand what to keep (nav header, #quiz-grid div, numbers-data.js script tag) and what to remove ("Quiz coming soon" paragraph).
    - file: assets/js/tapvocab.js
      why: Read lines 1–53 — copy the TTS block (_cachedSpanishVoice, _voicesLoaded, getSpanishVoice, voiceschanged listener, speakSpanish) verbatim into the IIFE. Do not modify this block.
    - file: numbers-learn.html
      why: Copy the URL param parsing block (lines 65–76) verbatim — same validation logic.
  </read_first>

  <action>
Rewrite numbers-quiz.html as follows. Preserve the existing head, nav header div, and numbers-data.js script tag exactly. Make these changes:

1. Remove the `<p style="color:var(--muted)...">Quiz coming soon</p>` line.

2. Add `class="nq-grid"` to `<div id="quiz-grid">` so it becomes:
   `<div id="quiz-grid" class="nq-grid"></div>`

3. Add a `<script>` tag after the existing `<script src="/assets/js/numbers-data.js"></script>` tag with an IIFE structured as follows:

```html
<script>
(function () {
  /* ── TTS (verbatim copy from tapvocab.js:13–53) ── */
  let _cachedSpanishVoice = null;
  let _voicesLoaded = false;

  function getSpanishVoice() {
    const voices = window.speechSynthesis.getVoices();
    if (!voices.length) return null;
    const normalize = s => (s || "").normalize("NFD").replace(/[̀-ͯ]/g, "").toLowerCase();
    const monica = voices.find(v => normalize(v.name).includes("monica") && v.lang.toLowerCase().startsWith("es"));
    if (monica) { _cachedSpanishVoice = monica; return monica; }
    const preferred = voices.filter(v => v.lang && v.lang.toLowerCase().startsWith("es"));
    _cachedSpanishVoice = preferred[0] || voices[0] || null;
    return _cachedSpanishVoice;
  }

  if (typeof speechSynthesis !== "undefined") {
    var _initialVoices = speechSynthesis.getVoices();
    if (_initialVoices.length) { _voicesLoaded = true; getSpanishVoice(); }
    if (speechSynthesis.onvoiceschanged !== undefined) {
      speechSynthesis.addEventListener("voiceschanged", function () {
        _voicesLoaded = true;
        getSpanishVoice();
      });
    }
  }

  function speakSpanish(text) {
    try {
      const u = new SpeechSynthesisUtterance(text);
      u.lang = "es-ES";
      const v = getSpanishVoice();
      if (v) u.voice = v;
      u.rate = 0.95; u.pitch = 1.0;
      window.speechSynthesis.cancel();
      window.speechSynthesis.speak(u);
    } catch (e) { console.warn("Speech synthesis failed:", e); }
  }

  /* ── URL param parsing (verbatim copy from numbers-learn.html) ── */
  var params = new URLSearchParams(location.search);
  var range = params.get("range") || "1-20";
  var parts = range.split("-");
  var lo = parseInt(parts[0], 10);
  var hi = parseInt(parts[1], 10);
  if (isNaN(lo) || isNaN(hi) || lo < 1 || hi > 100 || lo > hi) {
    lo = 1; hi = 20; range = "1-20";
  }

  /* ── Update page title ── */
  document.querySelector("h1").textContent = "🔢 " + lo + "–" + hi;

  /* ── Render cards ── */
  var nums = window.NUMBERS.filter(function (item) {
    return item.n >= lo && item.n <= hi;
  });
  var grid = document.getElementById("quiz-grid");

  nums.forEach(function (item) {
    // Outer perspective wrapper
    var card = document.createElement("div");
    card.className = "nq-card";

    // Inner rotating layer
    var inner = document.createElement("div");
    inner.className = "nq-card-inner";

    // Front face
    var front = document.createElement("div");
    front.className = "nq-card-front";
    var numEl = document.createElement("span");
    numEl.className = "nq-num";
    numEl.textContent = String(item.n);
    front.appendChild(numEl);

    // Back face
    var back = document.createElement("div");
    back.className = "nq-card-back";
    var wordEl = document.createElement("span");
    wordEl.className = "nq-word";
    wordEl.textContent = item.es;
    back.appendChild(wordEl);

    inner.appendChild(front);
    inner.appendChild(back);
    card.appendChild(inner);

    // Click handler: flip on first tap; re-speak on subsequent taps (D-05)
    card.addEventListener("click", function () {
      var alreadyFlipped = card.classList.contains("flipped");
      if (!alreadyFlipped) {
        card.classList.add("flipped");
      }
      // Always speak — whether first flip or re-tap
      speakSpanish(item.es);
    });

    grid.appendChild(card);
  });
})();
</script>
```

Constraints:
- NEVER use innerHTML to insert item.n or item.es — always textContent.
- The TTS block must be a character-for-character copy from tapvocab.js lines 13–53. Do not rename variables, reorder statements, or change values.
- The click handler checks `card.classList.contains("flipped")` before adding the class — this implements D-05 (re-tap re-speaks but does not flip back).
- The emoji in the title update uses unicode escape `🔢` for the 🔢 emoji and `–` for the en-dash — avoids encoding issues. Alternatively, use the literal characters if the file encoding is UTF-8 (it is — `<meta charset="utf-8" />`).
  </action>

  <acceptance_criteria>
    - grep -n "nq-grid" numbers-quiz.html returns at least 1 line (the div with id="quiz-grid" class="nq-grid")
    - grep -n "nq-card" numbers-quiz.html returns lines including "nq-card", "nq-card-inner", "nq-card-front", "nq-card-back"
    - grep -n "speakSpanish" numbers-quiz.html returns at least 2 lines (function definition + call in click handler)
    - grep -n "getSpanishVoice" numbers-quiz.html returns at least 2 lines (function definition + call inside speakSpanish)
    - grep -n "classList.contains" numbers-quiz.html returns a line with "flipped"
    - grep -n "classList.add" numbers-quiz.html returns a line with "flipped"
    - grep -n "Quiz coming soon" numbers-quiz.html returns nothing (placeholder removed)
    - grep -n "innerHTML" numbers-quiz.html returns nothing (DOM creation uses textContent only)
    - grep -n "window.NUMBERS" numbers-quiz.html returns a line using .filter()
    - grep -n "URLSearchParams" numbers-quiz.html returns a line
    - grep -n "isNaN(lo)" numbers-quiz.html returns a line (validation guard present)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → JS | `?range=lo-hi` param is user-controlled and parsed into integers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | URL `?range` param | mitigate | parseInt + isNaN guard + bounds check (lo >= 1, hi <= 100, lo <= hi) — verbatim from numbers-learn.html. Malformed input falls back to range "1-20". |
| T-18-02 | Information Disclosure | textContent vs innerHTML | mitigate | All DOM insertion uses textContent exclusively — no script injection possible from NUMBERS data or URL param values. |
| T-18-03 | Denial of Service | speechSynthesis.speak() without cancel() | accept | cancel() is called before every speak() (copied from tapvocab.js). Low-value target, no PII involved. |
</threat_model>

<verification>
  <must_haves>
    <item>numbers-quiz.html contains class="nq-grid" on the #quiz-grid div</item>
    <item>assets/css/styles.css contains .nq-grid with grid-template-columns: repeat(4, 1fr)</item>
    <item>assets/css/styles.css contains .nq-card.flipped .nq-card-inner with transform: rotateY(180deg)</item>
    <item>assets/css/styles.css contains .nq-card-back with border: 2px solid var(--ok)</item>
    <item>assets/css/styles.css contains .nq-num with color: #f5d800</item>
    <item>numbers-quiz.html contains speakSpanish() function body with u.rate = 0.95</item>
    <item>numbers-quiz.html contains card.classList.contains("flipped") in click handler</item>
    <item>numbers-quiz.html contains no innerHTML assignments with variable content</item>
    <item>numbers-quiz.html contains URLSearchParams and isNaN validation guard</item>
  </must_haves>
  <manual_checks>
    - Open /numbers-quiz.html?range=1-20 in browser — confirm 20 cards render in 4 columns, all face-down showing numerals in yellow/gold on dark navy
    - Tap a card — confirm it flips with 3D animation and reveals the Spanish word in white text with green border; TTS speaks the word
    - Tap the same flipped card again — confirm card stays flipped (does NOT flip back); TTS speaks again
    - Open /numbers-quiz.html?range=81-100 — confirm 20 cards, numerals 81–100, "noventa y nueve" etc. on back
    - Open /numbers-quiz.html (no param) — confirm falls back to range 1-20, 20 cards shown
    - Open /numbers-quiz.html?range=invalid — confirm falls back to range 1-20, no JS error in console
  </manual_checks>
</verification>

<success_criteria>
1. numbers-quiz.html?range=X-Y displays all numbers in [X,Y] as a 4-column grid of face-down cards showing numerals on the front — matching WhichNumberQuizz.png layout (NUM-05)
2. Tapping any card flips it to show the Spanish word on the back face with green border (NUM-06)
3. Each flip triggers TTS speech of the Spanish word via Web Speech API (NUM-07)
4. Previously flipped cards remain flipped; re-tapping re-speaks without flipping back (D-05)
5. No JS errors in console for valid ranges, invalid ranges, or missing range param
6. Cards fit 4-per-row at 375px viewport width without horizontal scroll
</success_criteria>

## Source Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Users can test number knowledge by flipping cards in 4-column grid that reveal Spanish word and speak it aloud | T01 (CSS grid + card styles) + T02 (render + click handler) |
| REQ NUM-05 | Quiz page displays all numbers in selected range as tap-to-flip card grid, 4 columns | T01 (nq-grid 4-col) + T02 (filter + render loop) |
| REQ NUM-06 | Tapping a number card flips it to reveal the Spanish word | T02 (click handler adds .flipped class) |
| REQ NUM-07 | Card flip triggers Spanish TTS voice speaking the word | T02 (speakSpanish() in click handler) |
| CONTEXT D-01 | Front face: yellow/gold number text on dark navy background | T01 (.nq-card-front background: #0d1740, .nq-num color: #f5d800) |
| CONTEXT D-02 | New compact card CSS, not reusing .flip-card | T01 (all-new .nq-* classes, aspect-ratio: 1/1) |
| CONTEXT D-03 | Back face: green border var(--ok) | T01 (.nq-card-back border: 2px solid var(--ok)) |
| CONTEXT D-04 | Back face shows only Spanish word | T02 (back face has only .nq-word span with item.es) |
| CONTEXT D-05 | Re-tap already-flipped card re-speaks TTS, stays flipped | T02 (classList.contains check before add, speakSpanish always called) |
| CONTEXT D-06 | Inline IIFE script in numbers-quiz.html, not separate .js file | T02 (inline <script> IIFE block) |

<output>
After completion, create `.planning/phases/18-numbers-quiz/18-01-SUMMARY.md`
</output>
