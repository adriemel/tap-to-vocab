---
phase: 23-vocabulary-data-topic-tagging
plan: 04
type: execute
wave: 3
depends_on: ["23-03"]
files_modified:
  - data/words.tsv
autonomous: true
requirements: [DATA-03, DATA-04, TAG-02]

must_haves:
  truths:
    - "All 27 Unidad 5B headwords from the two source images exist in words.tsv with category Unidad5B (D-09, DATA-03)"
    - "Textbook stem-change hints are stripped while the algo / hacer algo pattern is kept (D-10)"
    - "Every 5B headword carries a topic while keeping category Unidad5B (D-11, D-12, D-13, TAG-02)"
    - "The 5B example sentences are present with a blank topics cell and are numerous enough to make Build Sentences show a Unidad5B checkbox (D-06, DATA-04)"
    - "The 708 pre-existing rows are byte-identical — only appended lines changed (D-04, NAV-07)"
  artifacts:
    - path: "data/words.tsv"
      provides: "Unidad5B vocabulary, transcribed and tagged"
      contains: "Unidad5B"
  key_links:
    - from: "data/words.tsv"
      to: "assets/js/sentences.js"
      via: "categories derived from s.category at runtime (sentences.js:75-95); new category defaults to enabled at line 89"
      pattern: "Unidad5B"
    - from: "data/words.tsv"
      to: "assets/js/tapvocab.js"
      via: "topic.html?cat=Unidad5B filters on category (tapvocab.js:472)"
      pattern: "Unidad5B"
---

<objective>
Transcribe Unidad 5B (Bloque B) from the two source images into `data/words.tsv` as 4-column rows,
with `category` = `Unidad5B` and a topic on every headword.

Purpose: DATA-03 (browsable/quizzable 5B vocabulary), DATA-04 (a `Unidad5B` checkbox in Build
Sentences — which needs no code change: `sentences.js:75-95` derives its category list from
`s.category` at runtime and line 89 defaults an unseen category to enabled), and TAG-02 for the new
rows.
Output: ~44 appended rows (27 headwords + ~17 example sentences).

D-14: no approval gate on the topic mapping. Apply it directly; the user reviews the git diff and
the running app.
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-PATTERNS.md
@.planning/phases/23-vocabulary-data-topic-tagging/23-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Row shape (tab-separated, 4 fields): `category` TAB `es` TAB `de` TAB `topics`.
`category` for every new row is exactly `Unidad5B` — no space, matching the existing `Unidad5A`
convention (D-09).

The nine legal non-blank values in column 4 (D-01): `Colores`, `Animales`, `Numeros`, `Saludar`,
`Casa_Familia`, `Palabras`, `Calendario`, `Comida_Bebida`, `Escuela`.

German-side convention already used in the file: `;` separates alternatives, e.g.
`el lápiz; (Pl.) los lápices`. Follow the book's wording (CONTEXT.md § Claude's Discretion).

Existing file state at plan start: 709 lines (1 header + 708 data rows), every line 4 fields, all
non-sentence rows tagged. New rows are APPENDED at the end; do not interleave them.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe and tag the 27 Unidad 5B headwords</name>
  <files>data/words.tsv</files>
  <read_first>
    - new-vocab/unidad5b-1.jpeg — Bloque B page 1, `el fin de semana` through `¡Qué pena!` (read the image; the German column is the right-hand column)
    - new-vocab/unidad5b-2.jpeg — Bloque B page 2, `poder (-ue-) hacer algo` through `la silla de ruedas`
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-09, D-10, D-11, D-12, D-13 and § Specifics
    - data/words.tsv — the last 20 `Unidad5A` rows, to copy the existing formatting habits (article usage, `;` alternatives, `algo` pattern)
    - CLAUDE.md § Adding Vocabulary
  </read_first>
  <action>
    Baseline first, before any edit: `mkdir -p /tmp/gsd23` then
    `git show HEAD:data/words.tsv > /tmp/gsd23/p04-before.tsv`.

    Append 27 headword rows to the END of `data/words.tsv`. The bold entries in the two images are
    the headwords; the indented, non-bold lines beneath them are example sentences and belong to
    Task 2. Two traps in the source: under `la escalada` there is a circled-synonym cross-reference
    line reading `escalar` / `klettern` — that is a synonym marker, not a 28th headword, and
    `escalar` already appears earlier; and at the foot of page 1 `¡Qué pena!` is repeated as the
    closing line of a mini-dialogue — one headword row only.

    The complete Spanish-side checklist, already normalised per D-10 (write these exact strings into
    column 2):
    `el fin de semana`, `la semana`, `bailar`, `ir a correr`, `escalar`, `esquiar`, `hacer surf`,
    `jugar a algo`, `el baloncesto`, `el curso`, `la escalada`, `tener ganas de hacer algo`,
    `probar algo`, `la idea`, `¿Qué tal...?`, `por la mañana`, `¡Qué pena!`, `poder hacer algo`,
    `temprano`, `No pasa nada.`, `por la tarde`, `si`, `querer hacer algo`, `ordenar algo`,
    `¡Buena idea!`, `la disculpa`, `la silla de ruedas`.

    D-10 in full — strip the textbook's stem-change hints, keep the `algo` / `hacer algo` pattern:
    `esquiar (-í-)` becomes `esquiar`; `jugar (-ue-) a algo` becomes `jugar a algo`;
    `poder (-ue-) hacer algo` becomes `poder hacer algo`; `querer (-ie-) (hacer) algo` becomes
    `querer hacer algo`; `probar (-ue-) algo` becomes `probar algo`. Reason: the parenthesised hints
    wreck the text-to-speech reading and clutter the flip cards, and the irregularity already lives
    in `verbs.tsv` for conjugation practice. Keeping `algo` matches existing rows like `usar algo`
    and `desayunar (algo)`.

    German side: transcribe from the images, following the book. Use `;` to separate alternatives,
    as in `das Wochenende; am Wochenende`, `joggen gehen; laufen gehen`,
    `etw. tun können; etw. tun dürfen`, `die Entschuldigung; die Ausrede`, `wenn; falls`,
    `(am) Morgen; (am) Vormittag`.

    Topic assignment (D-11 additive — `category` stays `Unidad5B` on every row AND a topic is set):
    - `Palabras` for the 8 leisure and sport headwords, per D-12: `bailar`, `ir a correr`,
      `escalar`, `esquiar`, `hacer surf`, `jugar a algo`, `el baloncesto`, `la escalada`. A
      "Deportes y Ocio" topic was explicitly declined for this milestone (TAG-07, deferred), and
      D-12 makes `Palabras` the general catch-all for any row with no better topic.
    - `Calendario` for `el fin de semana`, `la semana`, `por la mañana`, `por la tarde`,
      `temprano` (D-13).
    - `Escuela` for `el curso` (D-13).
    - `Saludar` for the conversational set phrases where they genuinely read as
      greeting/politeness/reaction formulas — `¡Qué pena!`, `No pasa nada.`, `¡Buena idea!`,
      `¿Qué tal...?`, `la disculpa`, `tener ganas de hacer algo` — otherwise `Palabras` (D-13
      leaves this call to you).
    - `Palabras` for everything else: `probar algo`, `la idea`, `poder hacer algo`, `si`,
      `querer hacer algo`, `ordenar algo`, `la silla de ruedas`.
    - Exactly one topic per row (D-02); no comma-separated pairs.

    Four of these headwords end in `.`, `?` or `!` — `¡Qué pena!`, `No pasa nada.`, `¡Buena idea!`,
    `¿Qué tal...?`. They are still headwords and still get a topic (D-07). Known and accepted side
    effect from CONTEXT.md § Specifics: `sentences.js` will also pick them up as 2-3 word Build
    Sentences scrambles, exactly as it already does for `¿Qué tal?` in Saludar. Do not add code to
    suppress that.

    Preserve the file's trailing newline, do not reorder or touch any existing row, and do not add
    any dependency, package.json, or build step.

    Write the 27-string checklist to `/tmp/gsd23/u5b-headwords.txt`, one string per line, for the
    gates below.
  </action>
  <verify>
    <automated>awk -F'\t' '$1=="Unidad5B"' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR==FNR{ok[$0];next} $1=="Unidad5B" && !($2 in ok)' /tmp/gsd23/u5b-headwords.txt data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR==FNR{ok[$0];next} $1=="Unidad5B" && ($2 in ok){seen[$2]++} END{n=0; for(k in ok) if(seen[k]!=1){print "BAD_COUNT", k, seen[k]+0; n++} print "missing_or_dupe="n}' /tmp/gsd23/u5b-headwords.txt data/words.tsv</automated>
    <automated>awk -F'\t' '$1=="Unidad5B" && ($2 ~ /\(-/ || $3=="" || $4=="")' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR>1 && $4!="" && $4 !~ /^(Colores|Animales|Numeros|Saludar|Casa_Familia|Palabras|Calendario|Comida_Bebida|Escuela)$/' data/words.tsv | wc -l</automated>
    <automated>head -709 data/words.tsv | diff - /tmp/gsd23/p04-before.tsv && echo EXISTING_ROWS_UNTOUCHED</automated>
    <automated>awk -F'\t' 'NF!=4' data/words.tsv | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F'\t' '$1=="Unidad5B"' data/words.tsv | wc -l` prints exactly `27` at the end of this task
    - The checklist cross-check prints `missing_or_dupe=0` — each of the 27 exact Spanish strings appears exactly once as column 2 of a `Unidad5B` row, and no `Unidad5B` row has a column-2 value outside the checklist
    - `awk -F'\t' '$1=="Unidad5B" && ($2 ~ /\(-/ || $3=="" || $4=="")' data/words.tsv | wc -l` prints `0` — no `(-ue-)`, `(-í-)` or `(-ie-)` hint survived (D-10), no empty German, no untagged headword
    - `awk -F'\t' '$1=="Unidad5B" && $4=="Palabras"' data/words.tsv | wc -l` prints a number `>= 8` — the D-12 leisure/sport eight are parked in `Palabras`
    - `awk -F'\t' '$1=="Unidad5B" && $4=="Calendario"' data/words.tsv | wc -l` prints `5` and `awk -F'\t' '$1=="Unidad5B" && $4=="Escuela"' data/words.tsv | wc -l` prints `1` (D-13)
    - The slug whitelist gate over the whole file prints `0`
    - `head -709 data/words.tsv | diff - /tmp/gsd23/p04-before.tsv` prints nothing and echoes `EXISTING_ROWS_UNTOUCHED` — every pre-existing row byte-identical (D-04, NAV-07)
    - `awk -F'\t' 'NF!=4' data/words.tsv | wc -l` prints `0` and `wc -l data/words.tsv` prints `736`
    - `grep -c $'\t\t' data/words.tsv` prints `0`
  </acceptance_criteria>
  <done>27 Unidad5B headword rows appended, each with normalised Spanish, book German, and exactly one topic; every pre-existing row untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Transcribe the Unidad 5B example sentences</name>
  <files>data/words.tsv</files>
  <read_first>
    - new-vocab/unidad5b-1.jpeg and new-vocab/unidad5b-2.jpeg — the indented, non-bold lines under each headword
    - .planning/phases/23-vocabulary-data-topic-tagging/23-CONTEXT.md § D-06, D-09 and § Specifics
    - data/words.tsv — existing `Unidad5A` sentence rows, for punctuation and capitalisation habits
    - assets/js/sentences.js lines 53-58 and 75-95 — the sentence filter and the runtime category derivation that makes DATA-04 work with no code change
  </read_first>
  <action>
    Append the indented example sentences from both images as `Unidad5B` rows with a BLANK 4th field
    (D-06: example sentences are never tagged; they stay reachable under their unidad and in Build
    Sentences, and keeping them out of the topic lists is what keeps topic quizzes clean word
    lists).

    The sentences to transcribe, in image order (roughly 17 rows, Spanish then German from the
    right-hand column):
    page 1 — the `ir a correr` example about running in the park; the `esquiar` example about
    skiing in one's free time; the `el baloncesto` example about playing basketball on Saturdays;
    the two-line `tener ganas` mini-dialogue (the invitation question and the `¡Vale!` reply); the
    `probar algo` example about the ham; the `la idea` example; the two `¿Qué tal...?` follow-up
    questions about having time and about after school; the `por la mañana` example about the
    shopping centre on Saturday morning; and the `¡Qué pena!` mini-dialogue line about not going to
    the party.
    page 2 — the `poder hacer algo` example about the guitar lesson; the `temprano` example about
    mother having breakfast very early; the `por la tarde` example about doing sport in the
    afternoon; the `si` example about coming to your house; the `querer hacer algo` example about
    going by bicycle; and the `ordenar algo` example about tidying the room on Saturdays.

    Rules:
    - Each sentence is one row: `Unidad5B` TAB Spanish TAB German TAB (empty).
    - Do NOT repeat the closing `¡Qué pena!` line from the page-1 dialogue — it is already a
      headword row from Task 1. Its dialogue partner (the "aren't you going to the party?" line) IS
      a sentence row.
    - Drop the leading dialogue dashes from the two-line exchanges; keep the Spanish punctuation
      (`¿ ¡`) and the accents exactly as printed.
    - Do not tag any of these rows. Do not touch any existing row, including the 27 from Task 1.
  </action>
  <verify>
    <automated>awk -F'\t' '$1=="Unidad5B"' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR==FNR{ok[$0];next} $1=="Unidad5B" && !($2 in ok) && $4!=""' /tmp/gsd23/u5b-headwords.txt data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' 'NR==FNR{ok[$0];next} $1=="Unidad5B" && ($2 in ok) && $4==""' /tmp/gsd23/u5b-headwords.txt data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' '$1=="Unidad5B" && $2 ~ /[.?!]$/ && $2 !~ /\.\.$/' data/words.tsv | wc -l</automated>
    <automated>awk -F'\t' '$1=="Unidad5B" && ($3=="" || $2=="")' data/words.tsv | wc -l</automated>
    <automated>head -709 data/words.tsv | diff - /tmp/gsd23/p04-before.tsv && echo EXISTING_ROWS_UNTOUCHED</automated>
    <automated>awk -F'\t' 'NF!=4' data/words.tsv | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F'\t' '$1=="Unidad5B"' data/words.tsv | wc -l` prints a number between `42` and `46` inclusive (27 headwords + 15-19 sentences), satisfying ROADMAP criterion 1's "~24 headwords and ~15 example sentences"
    - `awk -F'\t' 'NR==FNR{ok[$0];next} $1=="Unidad5B" && !($2 in ok) && $4!=""' /tmp/gsd23/u5b-headwords.txt data/words.tsv | wc -l` prints `0` — no example sentence got tagged (D-06)
    - `awk -F'\t' 'NR==FNR{ok[$0];next} $1=="Unidad5B" && ($2 in ok) && $4==""' /tmp/gsd23/u5b-headwords.txt data/words.tsv | wc -l` prints `0` — no headword lost its tag
    - `awk -F'\t' '$1=="Unidad5B" && $2 ~ /[.?!]$/ && $2 !~ /\.\.$/' data/words.tsv | wc -l` prints a number `>= 18` — enough rows pass the live `sentences.js` filter that a `Unidad5B` checkbox must appear in Build Sentences (DATA-04); this counts the 4 punctuation-ending headwords plus the example sentences
    - `awk -F'\t' '$1=="Unidad5B" && ($3=="" || $2=="")' data/words.tsv | wc -l` prints `0` — no half-transcribed row
    - `head -709 data/words.tsv | diff - /tmp/gsd23/p04-before.tsv` prints nothing and echoes `EXISTING_ROWS_UNTOUCHED`
    - `awk -F'\t' 'NF!=4' data/words.tsv | wc -l` prints `0`; `grep -c $'\t\t' data/words.tsv` prints `0`
    - `awk -F'\t' 'NR>1{print $1}' data/words.tsv | sort | uniq -c | sort -rn` shows the 12 original categories at their original counts plus a new `Unidad5B` row count
  </acceptance_criteria>
  <done>Unidad 5B is complete in `data/words.tsv`: 27 tagged headwords plus its example sentences with blank topics, appended without disturbing a single pre-existing row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scanned textbook images → `data/words.tsv` → DOM | New author-transcribed content enters the repo. It is rendered with `textContent` in `tapvocab.js`; `sentences.js` uses `innerHTML` only with its own literal strings, so transcribed text never reaches an HTML sink. |
| `data/words.tsv` → `sentences.js` category derivation | A new `category` value silently creates a new Build Sentences checkbox, enabled by default (`sentences.js:89`). This is the intended DATA-04 mechanism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-15 | Tampering | Transcription introduces a literal tab, stray quote, or line break inside a Spanish or German value, shifting fields for that row and every reader of it | mitigate | Gates: `NF!=4` count 0, `grep -c $'\t\t'` 0, no empty `es`/`de`, and the checklist cross-check that pins every headword string exactly. |
| T-23-16 | Tampering | An append that accidentally rewrites earlier rows (editor reflow, lost final newline) would orphan starred practice-list entries | mitigate | `head -709 data/words.tsv \| diff - /tmp/gsd23/p04-before.tsv` must be empty before the plan is considered done (D-04, NAV-07). |
| T-23-17 | Denial of Service | A malformed appended row breaks `loadWords` and therefore every learning page at once | mitigate | Same field-count and non-empty-field gates, plus the browser smoke test in plan 05 before the phase closes. |
| T-23-18 | Information Disclosure | Transcribing copyrighted textbook material into a public repo | accept | Same practice as every prior unidad already in `data/words.tsv` (Unidad2-5A); this is a personal vocabulary list of individual words and short example sentences, not a reproduction of the book. Explicitly chosen by the user in D-09. |
| T-23-SC | Tampering (supply chain) | npm/pip/cargo installs | mitigate | Zero packages installed. No OCR library, no image tooling, no `package.json` — the images are read directly by the executing agent, and any helper script lives in `/tmp/gsd23`. |
| T-23-19 | Spoofing / Repudiation / Elevation of Privilege | — | accept | Static site, no server, no accounts, no privilege model. |
</threat_model>

<verification>
1. `awk -F'\t' '$1=="Unidad5B"' data/words.tsv | wc -l` prints 42-46
2. Checklist cross-check prints `missing_or_dupe=0`; the two "tagged/untagged" gates both print `0`
3. `awk -F'\t' '$1=="Unidad5B" && $2 ~ /\(-/' data/words.tsv | wc -l` prints `0`
4. `head -709 data/words.tsv | diff - /tmp/gsd23/p04-before.tsv` is empty
5. `awk -F'\t' 'NF!=4' data/words.tsv | wc -l` prints `0`
6. Serve the site (`python3 -m http.server 8000`) and open `http://localhost:8000/topic.html?cat=Unidad5B` — Browse shows the 5B cards, Quiz flips them, speech reads the Spanish without reading any stem-change hint (DATA-03)
7. Open `http://localhost:8000/sentences.html`, open the sentence manager — a `Unidad5B` checkbox is listed and is checked by default (DATA-04)
</verification>

<success_criteria>
- 27 Unidad 5B headwords present, D-10-normalised, each with exactly one of the nine slugs (DATA-03, TAG-02)
- 15-19 Unidad 5B example sentences present with blank topics (D-06)
- Every pre-existing row byte-identical (D-04, NAV-07)
- `topic.html?cat=Unidad5B` browses and quizzes; Build Sentences shows a `Unidad5B` checkbox (DATA-04)
- No dependency, package manager or build step added
</success_criteria>

<output>
Create `.planning/phases/23-vocabulary-data-topic-tagging/23-04-SUMMARY.md` when done. Record the
final `Unidad5B` row count and its topic breakdown.
Commit message prefix for this plan: `data(23-04):`
</output>
