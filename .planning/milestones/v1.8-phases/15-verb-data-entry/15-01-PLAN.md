---
phase: 15-verb-data-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tap-to-vocab/data/verbs.tsv
autonomous: false
requirements:
  - DATA-02

must_haves:
  truths:
    - "User opens conjugation.html and sees saber, hacer, beber, vivir, entender, and comer as selectable verbs in the Verb Manager"
    - "For each of the 6 new verbs, the Practice (tap-to-fill) mode shows a complete 6-row conjugation table (yo, tú, él, nosotros, vosotros, ellos) with correct forms"
    - "For each of the 6 new verbs, the Show mode displays all 6 conjugation forms pre-filled"
    - "All 13 pre-existing verbs (estar, ser, tener, hablar, escuchar, buscar, trabajar, tocar, estudiar, grabar (algo), practicar (algo), mirar (algo), quedar) are still present and unchanged"
  artifacts:
    - path: "tap-to-vocab/data/verbs.tsv"
      provides: "Verb conjugation data source loaded by conjugation.js"
      contains: "saber\thacer\tbeber\tvivir\tentender\tcomer — one row per verb with 8 tab-separated columns matching the header"
      min_lines: 20
  key_links:
    - from: "tap-to-vocab/data/verbs.tsv"
      to: "tap-to-vocab/assets/js/conjugation.js"
      via: "SharedUtils.loadTSV('/data/verbs.tsv') returns header-keyed rows; conjugation.js reads v.infinitive, v.de, v.yo, v.tu, v['él'], v.nosotros, v.vosotros, v.ellos"
      pattern: "loadTSV.*verbs\\.tsv"
---

<objective>
Add 6 new verbs (saber, hacer, beber, vivir, entender, comer) with complete present-indicative conjugations to `data/verbs.tsv` so users can practice them immediately in the Conjugation game.

Purpose: Expand the pool of practicable verbs from 13 to 19. Each new verb satisfies the three success criteria in the phase goal: (1) appears as a selectable option in the Verb Manager, (2) displays a complete and correct conjugation table, (3) no existing rows are lost or corrupted during the edit.

Output: Updated `tap-to-vocab/data/verbs.tsv` containing the 6 new rows appended after the existing 13 verb rows, preserving the existing CRLF line endings and UTF-8 accent characters.
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Current verbs.tsv header and format (verified from the file). -->
<!-- File encoding: UTF-8, line endings: CRLF (\r\n), column separator: TAB (\t) -->

Header (row 1):
```
infinitive	de	yo	tu	él	nosotros	vosotros	ellos
```

The `él` column header uses NFC U+00E9 (the pre-composed é character).

Row shape (8 TAB-separated columns):
```
<infinitive>	<german>	<yo-form>	<tu-form>	<el-form>	<nosotros-form>	<vosotros-form>	<ellos-form>
```

How conjugation.js consumes the data:
```javascript
// conjugation.js line 29-36 — PRONOUNS array defines column keys
const PRONOUNS = [
  { key: "yo", label: "yo" },
  { key: "tu", label: "tú" },
  { key: "él", label: "él/ella" },           // <-- matches "él" column in TSV
  { key: "nosotros", label: "nosotros/as" },
  { key: "vosotros", label: "vosotros/as" },
  { key: "ellos", label: "ellos/ellas" }
];

// conjugation.js line 307
const allVerbs = (await SharedUtils.loadTSV("/data/verbs.tsv"))
  .filter(v => v.infinitive && v.de);
```

`loadTSV` in `shared-utils.js` parses the header row and creates row objects keyed by header names, so adding rows with the same 8-column shape is automatically picked up — no JS changes required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 6 verb rows to verbs.tsv</name>
  <files>tap-to-vocab/data/verbs.tsv</files>
  <read_first>
    - tap-to-vocab/data/verbs.tsv (MUST read to confirm current content, line endings, and exact spelling of existing rows before modifying)
    - tap-to-vocab/assets/js/conjugation.js (confirm the PRONOUNS column keys match the TSV header: yo, tu, él, nosotros, vosotros, ellos)
  </read_first>
  <action>
Append exactly 6 new rows to the end of `tap-to-vocab/data/verbs.tsv`, after the last existing row (`quedar	sich verabreden, sich treffen	quedo	quedas	queda	quedamos	quedáis	quedan`).

CRITICAL FILE-FORMAT RULES:
1. The existing file uses **CRLF** line endings (`\r\n`). Every new row MUST end with `\r\n`, including the final row (so the file ends with `\r\n` after the `comer` row, mirroring existing behavior).
2. Columns are separated by a single **TAB** (`\t`) character — not spaces, not multiple tabs.
3. All accented characters (á, é, í, ó, ú, ü, ö) MUST be written as their pre-composed (NFC) UTF-8 code points, matching the existing rows (e.g. `á` = U+00E1, `é` = U+00E9).
4. Do NOT modify, reorder, or delete any of the 14 existing lines (1 header + 13 verb rows).
5. Do NOT add a blank line between the existing last row and the new rows.

Use the Python heredoc below (or an equivalent Edit) to append the rows atomically while preserving CRLF endings:

```python
# Run via Bash:  python3 <<'PY'
rows = [
    "saber\twissen (Sachverhalt), kennen (Fakten)\tsé\tsabes\tsabe\tsabemos\tsabéis\tsaben",
    "hacer\tmachen, tun\thago\thaces\thace\thacemos\thacéis\thacen",
    "beber\ttrinken\tbebo\tbebes\tbebe\tbebemos\tbebéis\tbeben",
    "vivir\tleben, wohnen\tvivo\tvives\tvive\tvivimos\tvivís\tviven",
    "entender\tverstehen\tentiendo\tentiendes\tentiende\tentendemos\tentendéis\tentienden",
    "comer\tessen\tcomo\tcomes\tcome\tcomemos\tcoméis\tcomen",
]
path = "tap-to-vocab/data/verbs.tsv"
with open(path, "rb") as f:
    existing = f.read()
# Ensure the existing file ends with CRLF; if not, normalize before appending.
if not existing.endswith(b"\r\n"):
    existing = existing.rstrip(b"\r\n") + b"\r\n"
addition = ("\r\n".join(rows) + "\r\n").encode("utf-8")
with open(path, "wb") as f:
    f.write(existing + addition)
PY
```

EXPECTED FINAL 6 ROWS (tabs shown as `→`, CRLF implicit at line end):
```
saber→wissen (Sachverhalt), kennen (Fakten)→sé→sabes→sabe→sabemos→sabéis→saben
hacer→machen, tun→hago→haces→hace→hacemos→hacéis→hacen
beber→trinken→bebo→bebes→bebe→bebemos→bebéis→beben
vivir→leben, wohnen→vivo→vives→vive→vivimos→vivís→viven
entender→verstehen→entiendo→entiendes→entiende→entendemos→entendéis→entienden
comer→essen→como→comes→come→comemos→coméis→comen
```

Conjugation reference (present indicative, standard Castilian — DO NOT deviate):

| infinitive | de | yo | tú | él | nosotros | vosotros | ellos |
|------------|-----|------|-------|-------|-----------|-----------|---------|
| saber | wissen (Sachverhalt), kennen (Fakten) | sé | sabes | sabe | sabemos | sabéis | saben |
| hacer | machen, tun | hago | haces | hace | hacemos | hacéis | hacen |
| beber | trinken | bebo | bebes | bebe | bebemos | bebéis | beben |
| vivir | leben, wohnen | vivo | vives | vive | vivimos | vivís | viven |
| entender | verstehen | entiendo | entiendes | entiende | entendemos | entendéis | entienden |
| comer | essen | como | comes | come | comemos | coméis | comen |

Notes on specific forms (do not second-guess these; they are the correct standard forms):
- `saber` → 1st person singular is `sé` (with accent, irregular)
- `hacer` → 1st person singular is `hago` (c→g irregular)
- `vivir` → vosotros form is `vivís` (with accent on í, -ir verb pattern)
- `entender` → e→ie stem change in yo/tú/él/ellos (entiendo, entiendes, entiende, entienden); nosotros/vosotros keep the regular stem (entendemos, entendéis)
- All others are fully regular -er / -ir patterns.
  </action>
  <verify>
    <automated>cd /home/desire/tap-to-vocab && python3 -c "
import sys
path = 'tap-to-vocab/data/verbs.tsv'
with open(path, 'rb') as f:
    raw = f.read()
# 1. CRLF preserved
assert raw.endswith(b'\r\n'), 'File must end with CRLF'
assert b'\r\n' in raw, 'File must contain CRLF line endings'
# 2. Parse lines
lines = raw.decode('utf-8').rstrip('\r\n').split('\r\n')
assert len(lines) == 20, f'Expected 20 lines (1 header + 19 verbs), got {len(lines)}'
# 3. Header intact
assert lines[0] == 'infinitive\tde\tyo\ttu\tél\tnosotros\tvosotros\tellos', f'Header tampered: {lines[0]!r}'
# 4. Existing verbs intact (check 3 anchors)
assert 'estar\tsein, sich befinden\testoy\testás\testá\testamos\testáis\testán' in lines, 'estar row missing/corrupted'
assert 'tener\thaben\ttengo\ttienes\ttiene\ttenemos\ttenéis\ttienen' in lines, 'tener row missing/corrupted'
assert 'quedar\tsich verabreden, sich treffen\tquedo\tquedas\tqueda\tquedamos\tquedáis\tquedan' in lines, 'quedar row missing/corrupted'
# 5. New rows present with exact forms
expected_new = [
    'saber\twissen (Sachverhalt), kennen (Fakten)\tsé\tsabes\tsabe\tsabemos\tsabéis\tsaben',
    'hacer\tmachen, tun\thago\thaces\thace\thacemos\thacéis\thacen',
    'beber\ttrinken\tbebo\tbebes\tbebe\tbebemos\tbebéis\tbeben',
    'vivir\tleben, wohnen\tvivo\tvives\tvive\tvivimos\tvivís\tviven',
    'entender\tverstehen\tentiendo\tentiendes\tentiende\tentendemos\tentendéis\tentienden',
    'comer\tessen\tcomo\tcomes\tcome\tcomemos\tcoméis\tcomen',
]
for row in expected_new:
    assert row in lines, f'Missing/incorrect row: {row!r}'
# 6. Each new row has exactly 8 tab-separated columns
for row in expected_new:
    cols = row.split('\t')
    assert len(cols) == 8, f'Row {cols[0]} has {len(cols)} cols, expected 8'
# 7. New verbs appended after existing ones (last 6 lines are the new verbs in order)
new_infinitives = [l.split('\t')[0] for l in lines[-6:]]
assert new_infinitives == ['saber','hacer','beber','vivir','entender','comer'], f'Order wrong: {new_infinitives}'
print('OK: verbs.tsv validated — 20 lines, CRLF, 6 new verbs appended correctly')
"</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l tap-to-vocab/data/verbs.tsv` reports exactly 20 lines (1 header + 13 existing verbs + 6 new verbs)
    - `file tap-to-vocab/data/verbs.tsv` still reports "with CRLF line terminators" (NOT converted to LF)
    - `grep -c $'^saber\t' tap-to-vocab/data/verbs.tsv` returns 1
    - `grep -c $'^hacer\t' tap-to-vocab/data/verbs.tsv` returns 1
    - `grep -c $'^beber\t' tap-to-vocab/data/verbs.tsv` returns 1
    - `grep -c $'^vivir\t' tap-to-vocab/data/verbs.tsv` returns 1
    - `grep -c $'^entender\t' tap-to-vocab/data/verbs.tsv` returns 1
    - `grep -c $'^comer\t' tap-to-vocab/data/verbs.tsv` returns 1
    - `grep -P $'^saber\twissen \\(Sachverhalt\\), kennen \\(Fakten\\)\tsé\tsabes\tsabe\tsabemos\tsabéis\tsaben\r$' tap-to-vocab/data/verbs.tsv` matches one line
    - `grep -P $'^hacer\tmachen, tun\thago\thaces\thace\thacemos\thacéis\thacen\r$' tap-to-vocab/data/verbs.tsv` matches one line
    - `grep -P $'^beber\ttrinken\tbebo\tbebes\tbebe\tbebemos\tbebéis\tbeben\r$' tap-to-vocab/data/verbs.tsv` matches one line
    - `grep -P $'^vivir\tleben, wohnen\tvivo\tvives\tvive\tvivimos\tvivís\tviven\r$' tap-to-vocab/data/verbs.tsv` matches one line
    - `grep -P $'^entender\tverstehen\tentiendo\tentiendes\tentiende\tentendemos\tentendéis\tentienden\r$' tap-to-vocab/data/verbs.tsv` matches one line
    - `grep -P $'^comer\tessen\tcomo\tcomes\tcome\tcomemos\tcoméis\tcomen\r$' tap-to-vocab/data/verbs.tsv` matches one line
    - `grep -c $'^estar\t' tap-to-vocab/data/verbs.tsv` still returns 1 (existing row not corrupted)
    - `grep -c $'^quedar\t' tap-to-vocab/data/verbs.tsv` still returns 1 (existing row not corrupted)
    - First line of the file exactly equals `infinitive\tde\tyo\ttu\tél\tnosotros\tvosotros\tellos` (header preserved, including NFC `él`)
    - Every new row has exactly 8 tab-separated columns (verified by `awk -F'\t' 'NR>14 {print NF}' tap-to-vocab/data/verbs.tsv | sort -u` → prints only `8`)
  </acceptance_criteria>
  <done>
    verbs.tsv contains 20 lines (1 header + 19 verbs) with CRLF line endings, UTF-8 NFC accents, and each of the 6 new rows exactly matching the reference table. All existing rows are bit-for-bit unchanged. Automated verify script passes and every acceptance_criteria grep returns the expected match count.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke test in the Conjugation game</name>
  <files>tap-to-vocab/data/verbs.tsv, tap-to-vocab/conjugation.html, tap-to-vocab/assets/js/conjugation.js</files>
  <read_first>
    - tap-to-vocab/data/verbs.tsv (eyeball the 6 new rows as written to disk)
    - tap-to-vocab/assets/js/conjugation.js (confirm no JS edit was required by this plan)
  </read_first>
  <action>
Pause execution and hand the session back to the user for a live in-browser smoke test. This checkpoint has NO code actions — it verifies that the Task 1 data change produces the expected end-user behavior in the Conjugation game. The executor should print the "how-to-verify" instructions verbatim and wait for the resume signal; do not advance automatically.
  </action>
  <what-built>
    6 new verbs appended to `data/verbs.tsv`: saber, hacer, beber, vivir, entender, comer. No JS/HTML/CSS changes — conjugation.js already loads whatever verbs.tsv contains via SharedUtils.loadTSV.
  </what-built>
  <how-to-verify>
    1. Start a local server from the project root: `cd tap-to-vocab && python3 -m http.server 8000`
    2. Open http://localhost:8000/conjugation.html in a browser (hard-refresh with Ctrl+Shift+R to bypass any cached TSV).
    3. Click the "Select Verbs" / verb-manager button. Expected: the list now includes 6 new entries — "saber — wissen (Sachverhalt), kennen (Fakten)", "hacer — machen, tun", "beber — trinken", "vivir — leben, wohnen", "entender — verstehen", "comer — essen". All 13 previously existing verbs should still be present above them.
    4. Close the manager, then switch to "Show" mode and click "Next" through the verbs until each of the 6 new verbs is displayed. For each one, verify all 6 rows are pre-filled and match this table:
       - saber: yo sé / tú sabes / él sabe / nosotros sabemos / vosotros sabéis / ellos saben
       - hacer: yo hago / tú haces / él hace / nosotros hacemos / vosotros hacéis / ellos hacen
       - beber: yo bebo / tú bebes / él bebe / nosotros bebemos / vosotros bebéis / ellos beben
       - vivir: yo vivo / tú vives / él vive / nosotros vivimos / vosotros vivís / ellos viven
       - entender: yo entiendo / tú entiendes / él entiende / nosotros entendemos / vosotros entendéis / ellos entienden
       - comer: yo como / tú comes / él come / nosotros comemos / vosotros coméis / ellos comen
    5. Switch to "Practice" mode. Skip through the verb sequence (using the Skip button) until one of the 6 new verbs appears. Tap the word-bank forms in correct order (yo → ellos) and confirm the table fills correctly, the success animation plays, and no console errors appear.
    6. Open DevTools console and confirm NO errors from `conjugation.js` or `shared-utils.js` loading or parsing the TSV.
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; no automated check replaces the live browser smoke test. Task 1's automated verify script already proves the data file is correct; this task proves the data renders correctly in the Conjugation UI.</automated>
  </verify>
  <acceptance_criteria>
    - User explicitly types "approved" (or equivalent) as the resume signal
    - User confirms all 6 new verbs appear in the Verb Manager list alongside the 13 existing verbs
    - User confirms Show mode renders each of the 6 new verbs with the exact forms listed in step 4
    - User confirms Practice mode accepts the correct tap sequence for at least one new verb without errors
    - User confirms the DevTools console shows no errors from conjugation.js or shared-utils.js during verb loading
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 6 verbs display correctly in both Show and Practice mode, or describe any form that is wrong so Task 1 can be re-run with corrections.</resume-signal>
  <done>
    User confirms: (a) all 6 new verbs are listed in the Verb Manager alongside the 13 existing ones, (b) Show mode renders each new verb's 6 conjugation rows with the exact forms above, (c) Practice mode accepts the correct sequence for at least one new verb without errors, (d) no console errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static-site file-system → browser | TSV file is served as a static asset and fetched by client-side JS; no server-side code, no user input reaches the TSV |
| Developer edit → committed TSV | The only "input" is this plan's hand-authored row additions; trust boundary is the developer / reviewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | tap-to-vocab/data/verbs.tsv existing rows | mitigate | Acceptance criteria include `grep -c` checks for every existing anchor row (estar, tener, quedar) and a line-count assertion of exactly 20 lines. The automated verify script re-reads the file and asserts all 13 pre-existing rows are present verbatim. Prevents accidental corruption during the edit. |
| T-15-02 | Tampering | tap-to-vocab/data/verbs.tsv line endings / encoding | mitigate | Verify script asserts the file still ends with `\r\n` and contains CRLF markers; `file` command acceptance criterion confirms "CRLF line terminators". Prevents editors from silently converting to LF, which would work on first load but produce diff noise and could confuse future parsers. |
| T-15-03 | Information Disclosure | TSV contents | accept | No PII, no secrets — file contains only Spanish/German vocabulary that is already public on tapvocab.fun. ASVS L1 does not require mitigation for public educational content. |
| T-15-04 | Denial of Service | conjugation.html load path | accept | Adding 6 rows to a 14-line file cannot plausibly degrade performance (final file < 2 KB). No network/compute threat. |
| T-15-05 | Repudiation | Data edit history | mitigate | Change is committed via git with a descriptive message, providing authoritative audit trail. No additional logging needed. |
| T-15-06 | Spoofing / Elevation of Privilege | — | n/a | No authentication or authorization in this static asset; no privilege boundary crossed by this change. |
| T-15-07 | Injection (XSS) via TSV content | conjugation.js rendering of verb forms | mitigate | conjugation.js uses `textContent` (not `innerHTML`) when writing verb forms into the DOM (see lines 134-136, 148, 169, 264-280), so any accidental HTML-like characters in TSV values cannot become executable script. The 6 new rows contain only plain Spanish/German letters and punctuation, so the risk is latent; the existing code pattern is the mitigation and must not be regressed. ASVS L1 V5.3.3 (output encoding) is satisfied. |
</threat_model>

<verification>
- Automated verify script in Task 1 passes (line count, CRLF, existing-row integrity, new-row exact match, column count, append order).
- Every acceptance_criteria grep in Task 1 matches exactly once.
- Human smoke test in Task 2 confirms all 6 verbs appear in both Show and Practice modes with correct conjugations and no console errors.
</verification>

<success_criteria>
1. `tap-to-vocab/data/verbs.tsv` contains exactly 20 lines (1 header + 19 verb rows) with CRLF line endings and UTF-8 NFC accents.
2. The 6 new verbs (saber, hacer, beber, vivir, entender, comer) each occupy one row with 8 tab-separated columns and the exact conjugation forms specified in the task.
3. All 13 pre-existing verb rows are present bit-for-bit unchanged.
4. Reloading `/conjugation.html` lists all 6 new verbs in the Verb Manager, shows a complete 6-row table for each in Show mode, and accepts correct word-bank taps in Practice mode.
</success_criteria>

<output>
After completion, create `.planning/phases/15-verb-data-entry/15-01-SUMMARY.md` documenting:
- The 6 rows added (verbatim)
- Confirmation that CRLF and existing rows were preserved
- Verification output from the automated Python script
- Checkpoint observation (any verb-manager or display issues seen during smoke test)
</output>
