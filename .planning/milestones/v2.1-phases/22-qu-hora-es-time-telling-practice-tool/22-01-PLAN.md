---
phase: 22-qu-hora-es-time-telling-practice-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/js/hora-phrase.js
  - hora-phrase.test.js
  - index.html
  - assets/css/styles.css
autonomous: true
requirements: [HORA-01, HORA-06, HORA-07]
user_setup: []

must_haves:
  truths:
    - "A Spanish time phrase can be produced for any of the 288 reachable (hour, minute) dial states"
    - "Phrases use traditional forms (Es la una / Son las..., y cuarto, y media, menos cuarto, menos veinte) — never literal digit reading"
    - "Every phrase ends with de la mañana / de la tarde / de la noche derived from the raw 24h hour, not the spoken hour"
    - "A user on the home screen sees a 'Qué hora es?' button directly below 'Quién soy yo'"
  artifacts:
    - path: "assets/js/hora-phrase.js"
      provides: "Pure buildTimePhrase(hour24, minute) with dual window/module export"
      contains: "buildTimePhrase"
      min_lines: 40
    - path: "hora-phrase.test.js"
      provides: "Zero-dependency Node assert script covering the RESEARCH worked-example table"
      contains: "require"
      min_lines: 30
    - path: "index.html"
      provides: "Home screen entry point to /hora.html"
      contains: "btn-hora"
    - path: "assets/css/styles.css"
      provides: ".grid-two-col .btn-hora full-width home button styling"
      contains: "btn-hora"
  key_links:
    - from: "hora-phrase.test.js"
      to: "assets/js/hora-phrase.js"
      via: "require of the module's CommonJS export"
      pattern: "require\\(.*hora-phrase"
    - from: "index.html"
      to: "hora.html"
      via: "anchor href"
      pattern: "href=\"/hora\\.html\""
---

<objective>
Build the Spanish time-phrase grammar engine as a pure, dual-exported module with an executable Node test, and add the home-screen entry point that opens the clock page.

Purpose: The grammar function is the highest-correctness-risk surface of this phase (RESEARCH.md flags two pitfalls that produce fluent-but-wrong Spanish). Isolating it as a pure function with a real automated test means every later UI plan builds on a proven core. The home button is bundled here because it is small, independent, and shares the `styles.css` write.
Output: `assets/js/hora-phrase.js`, `hora-phrase.test.js`, an entry button in `index.html`, and `.grid-two-col .btn-hora` in `assets/css/styles.css`.
</objective>

<execution_context>
@/home/desire/tap-to-vocab/.claude/get-shit-done/workflows/execute-plan.md
@/home/desire/tap-to-vocab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-qu-hora-es-time-telling-practice-tool/22-CONTEXT.md
@.planning/phases/22-qu-hora-es-time-telling-practice-tool/22-RESEARCH.md
@.planning/phases/22-qu-hora-es-time-telling-practice-tool/22-PATTERNS.md
@.planning/phases/22-qu-hora-es-time-telling-practice-tool/22-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contract this plan CREATES. Plan 03 consumes it from hora.html. -->

Global (browser): `window.HoraPhrase.buildTimePhrase(hour24, minute) -> string`
CommonJS (node):  `require('./assets/js/hora-phrase.js').buildTimePhrase(hour24, minute) -> string`

- `hour24`: integer 0–23
- `minute`: integer, one of 0, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55
- returns: a single-line Spanish phrase string, no leading/trailing whitespace, no HTML

Existing CSS custom properties available (assets/css/styles.css `:root`, lines 1–10):
`--bg #152238`, `--card #121831`, `--ink #e8ecff`, `--muted #aeb6d9`, `--accent #6ca8ff`, `--ok #3ddc97`, `--warn #ffcc66`, `--error #ff6b9d`
</interfaces>

<locked_decisions>
These grammar judgment calls were flagged `[ASSUMED]` in RESEARCH.md's Assumptions Log and are LOCKED for this phase. Implement exactly; do not re-litigate.

- **A1 — period boundaries:** hour 0–11 → `de la mañana`; 12–17 → `de la tarde`; 18–23 → `de la noche`. No `madrugada` bucket.
- **A2 — no special words:** 00:00 and 12:00 use `Son las doce ... de la mañana` / `de la tarde`. Do NOT emit `Es medianoche` / `Es mediodía` (they cannot carry the period suffix HORA-07 requires).
- **A4 — exact hours:** minute 0 appends ` en punto` (not a bare hour phrase).
- **D-01/D-02/D-03/D-04** from CONTEXT.md apply to plans 02 and 03, not to this plan.
</locked_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create buildTimePhrase module and its Node test</name>
  <files>hora-phrase.test.js, assets/js/hora-phrase.js</files>
  <read_first>
    - `.planning/phases/22-qu-hora-es-time-telling-practice-tool/22-RESEARCH.md` lines 96–192 — the full grammar mapping, the minute-suffix table, the hour-shift rule, the reference implementation, and the worked-example table. This is the source of truth for every rule below.
    - `.planning/phases/22-qu-hora-es-time-telling-practice-tool/22-RESEARCH.md` lines 225–235 — Pitfall 1 (period from wrong hour) and Pitfall 2 (Es la una vs Son las after the menos shift). Both are directly exercised by the test cases.
    - `assets/js/coins.js` — smallest existing example of this project's IIFE-assigns-to-`window` module convention (per CLAUDE.md "All JS is IIFE pattern, export to `window`").
    - `CLAUDE.md` — zero-dependency constraint: no npm, no package.json, no test framework.
  </read_first>
  <behavior>
    Write `hora-phrase.test.js` FIRST, run it (it must fail because the module does not exist yet), then implement `assets/js/hora-phrase.js` until it passes.

    Test uses only Node built-ins: `require('node:assert/strict')` and `require('./assets/js/hora-phrase.js')`. No framework, no package.json.

    Exact-output cases (input → expected string, byte-for-byte including accents):
    - (1, 0)   → "Es la una en punto de la mañana"
    - (7, 5)   → "Son las siete y cinco de la mañana"
    - (13, 0)  → "Es la una en punto de la tarde"
    - (14, 15) → "Son las dos y cuarto de la tarde"
    - (17, 30) → "Son las cinco y media de la tarde"
    - (0, 0)   → "Son las doce en punto de la mañana"
    - (12, 0)  → "Son las doce en punto de la tarde"
    - (18, 45) → "Son las siete menos cuarto de la noche"
    - (23, 40) → "Son las doce menos veinte de la noche"
    - (0, 40)  → "Es la una menos veinte de la mañana"
    - (12, 40) → "Es la una menos veinte de la tarde"
    - (11, 35) → "Son las doce menos veinticinco de la mañana"

    Exhaustive invariant case: loop all 24 hours × the 12 minute steps (288 states) and assert for each result that it (a) is a non-empty string, (b) starts with either "Es la " or "Son las ", (c) ends with exactly one of " de la mañana", " de la tarde", " de la noche", (d) contains no digit characters.

    On success the script prints a line containing "ALL PASS" and exits 0; on any assertion failure Node exits non-zero.
  </behavior>
  <action>
    Create `assets/js/hora-phrase.js` as a single IIFE that defines a pure `buildTimePhrase(hour24, minute)` and dual-exports it: assign `window.HoraPhrase = { buildTimePhrase: buildTimePhrase }` when `typeof window !== 'undefined'`, and assign `module.exports = { buildTimePhrase: buildTimePhrase }` when `typeof module !== 'undefined' && module.exports`. Both guards are required — the browser page and the Node test load the same file. The function must touch no DOM, no globals, no I/O.

    Implement exactly these rules (from RESEARCH.md Pattern 1):

    1. `MINUTE_WORDS` lookup keyed by minute: 0→'' , 5→'cinco', 10→'diez', 15→'cuarto', 20→'veinte', 25→'veinticinco', 30→'media', 35→'veinticinco', 40→'veinte', 45→'cuarto', 50→'diez', 55→'cinco'.
    2. `HOUR_WORDS` array indexed 1–12: una, dos, tres, cuatro, cinco, seis, siete, ocho, nueve, diez, once, doce. (Index 0 is a filler 'doce' and is never reached.)
    3. `isPast = minute <= 30`.
    4. `h12 = hour24 % 12; if (h12 === 0) h12 = 12;`
    5. `spokenHour = isPast ? h12 : (h12 % 12) + 1` — the menos branch counts down to the NEXT hour and wraps 12→1.
    6. Verb/article: `spokenHour === 1` → `'Es la'`, otherwise `'Son las'`. This check MUST run on `spokenHour` (post-shift), never on `h12` — Pitfall 2. Case (0, 40) and (12, 40) both prove this.
    7. Minute suffix: minute 0 → `' en punto'`; else if `isPast` → `' y ' + MINUTE_WORDS[minute]`; else → `' menos ' + MINUTE_WORDS[minute]`.
    8. Period suffix computed from the RAW `hour24` only, in its own separate small function or clearly separated block — never from `h12` or `spokenHour` (Pitfall 1). Ranges per locked decision A1: `hour24 < 12` → 'de la mañana', `hour24 < 18` → 'de la tarde', else 'de la noche'. Case (18, 45) proves this: spoken hour is 7 but the period is noche.
    9. Return `verb + ' ' + HOUR_WORDS[spokenHour] + minuteSuffix + ' ' + period`.

    Do NOT add `mediodía`/`medianoche` branching (locked A2). Do NOT emit "y quince"/"y treinta"/"y cuarenta y cinco" (HORA-06 forbids literal digit reading). Write the file as UTF-8 so ñ and á round-trip through both Node and the browser.

    Create `hora-phrase.test.js` at the repository root (so the quick-run command matches `.planning/phases/22-.../22-VALIDATION.md`: `node hora-phrase.test.js`).
  </action>
  <verify>
    <automated>node hora-phrase.test.js</automated>
    <automated>node -e "const m=require('./assets/js/hora-phrase.js'); const a=require('node:assert/strict'); a.equal(m.buildTimePhrase(18,45),'Son las siete menos cuarto de la noche'); a.equal(m.buildTimePhrase(0,40),'Es la una menos veinte de la mañana'); console.log('SPOT_OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node hora-phrase.test.js` exits 0 and its stdout contains `ALL PASS`.
    - The spot-check `node -e` command prints `SPOT_OK`.
    - `grep -c "buildTimePhrase" assets/js/hora-phrase.js` returns a value >= 3 (definition + both exports).
    - `grep -q "module.exports" assets/js/hora-phrase.js` succeeds AND `grep -q "window.HoraPhrase" assets/js/hora-phrase.js` succeeds — both export paths present.
    - `grep -qE "mediod|medianoche" assets/js/hora-phrase.js` FAILS (exit 1) — locked decision A2 respected.
    - `grep -qE "y quince|y treinta|cuarenta y cinco" assets/js/hora-phrase.js` FAILS (exit 1) — HORA-06 respected.
    - `grep -c "document\.\|window\.speech\|localStorage\|fetch(" assets/js/hora-phrase.js` returns 0 — the module is pure and has no DOM/IO dependency.
    - `ls package.json 2>/dev/null` returns nothing — no package manager was introduced.
  </acceptance_criteria>
  <done>`node hora-phrase.test.js` prints ALL PASS and exits 0; all 12 exact cases and the 288-state invariant sweep hold; the module is DOM-free and dual-exported.</done>
</task>

<task type="auto">
  <name>Task 2: Add the home-screen "Qué hora es?" entry button</name>
  <files>index.html, assets/css/styles.css</files>
  <read_first>
    - `index.html` lines 40–63 — the `.grid-two-col` full-width button stack. The new button goes on the line directly below the `.btn-quien-soy` anchor (currently line 59) and above the `.btn-games` button (currently line 62).
    - `assets/css/styles.css` lines 1345–1356 — the `.grid-two-col .btn-quien-soy` rule whose gradient/border/weight combo must be reused verbatim, plus the section-header comment style used to open a new component block.
    - `assets/css/styles.css` lines 134–152 — the base `.btn` rule and `.btn.secondary` modifier, so the new class only overrides what it needs.
    - `.planning/phases/22-.../22-UI-SPEC.md` lines 80–90 (Copywriting Contract) and 129–131 (New home-page button class).
  </read_first>
  <action>
    In `index.html`, insert immediately after the existing `💬 Quién soy yo` anchor (and before the `🎮 Play Games` button) a comment line `<!-- 🕐 Qué hora es? - spans both columns -->` followed by the anchor `<a class="btn btn-hora" href="/hora.html">🕐 Qué hora es?</a>`. Copy label text exactly as specified by UI-SPEC (clock emoji U+1F550, then a space, then `Qué hora es?`). Keep the existing blank-line-between-buttons formatting used by its neighbours. Change nothing else in `index.html`.

    In `assets/css/styles.css`, append a new section at the end of the file opened by a banner comment in the same style as the existing `Quién Soy Yo — Chat Simulator  (qs-*)` banner, reading `Qué Hora Es? — Time-Telling (hora-*)`. Under a `/* ── Home button (index.html) ─── */` sub-comment add the rule `.grid-two-col .btn-hora` with exactly: `grid-column: 1 / -1;`, `background: linear-gradient(135deg, #2a1f5a 0%, #1a1440 100%);`, `border: 2px solid var(--accent);`, `font-weight: 700;`. These four declarations are copied verbatim from `.grid-two-col .btn-quien-soy` per UI-SPEC's explicit "reuse that gradient/border combo verbatim rather than inventing a new one" instruction — do not invent a different gradient, do not add a hover rule, do not introduce any new hex colour beyond the two already in that gradient.

    Leave the rest of the new `hora-*` CSS (clock card, reels, phrase card) for plan 02 — this task adds the home button rule only.
  </action>
  <verify>
    <automated>grep -c 'class="btn btn-hora" href="/hora.html"' index.html</automated>
    <automated>grep -n "grid-two-col .btn-hora" assets/css/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class="btn btn-hora" href="/hora.html"' index.html` returns exactly 1.
    - `grep -n "btn-quien-soy\|btn-hora\|btn-games" index.html` shows `btn-quien-soy` before `btn-hora` before `btn-games` in ascending line order (button sits below Quién soy yo, above Play Games — HORA-01 placement).
    - `sed -n '/grid-two-col .btn-hora/,/}/p' assets/css/styles.css | grep -c "grid-column: 1 / -1;\|linear-gradient(135deg, #2a1f5a 0%, #1a1440 100%)\|border: 2px solid var(--accent)\|font-weight: 700" returns 4.
    - `grep -c "Qué Hora Es? — Time-Telling (hora-\*)" assets/css/styles.css` returns 1 — the new section banner exists for plan 02 to extend.
    - No other anchor in `index.html` changed: `git diff --stat index.html` reports 1 file changed with no deletions other than whitespace.
  </acceptance_criteria>
  <done>The home screen shows a full-width `🕐 Qué hora es?` button between "Quién soy yo" and "Play Games", styled identically to the Quién soy yo button, linking to `/hora.html`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed in this plan | `buildTimePhrase` receives only integers produced by in-page dial state (plan 03); there is no network input, no user free-text, no query-string parsing, and no persistence. This project is a static site with no backend, no auth, and no cookies. |
| repo → GitHub Pages CDN | Every file committed at repo root is publicly served, including `hora-phrase.test.js`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `buildTimePhrase` return value later injected into the DOM | mitigate | Function returns a plain string assembled solely from two hard-coded word tables plus fixed literals; it never concatenates caller-supplied strings. Consumers are required to render it via `textContent` (enforced in plans 02/03 acceptance criteria), so no markup path exists. |
| T-22-02 | Information Disclosure | `hora-phrase.test.js` served publicly by GitHub Pages | accept | Contains only Spanish grammar assertions and a `require` of a public asset. No secrets, no credentials, no infrastructure detail, no internal paths beyond an already-public asset path. |
| T-22-03 | Elevation of Privilege | Module loading in `index.html` / `hora.html` | accept | No `eval`, no `new Function`, no dynamic `import()`, no inline event-handler attributes are introduced. The module is a plain static script with no privileged capability to elevate. |
| T-22-04 | Spoofing | New `index.html` anchor target | mitigate | `href="/hora.html"` is a root-relative, same-origin path with no `target="_blank"` and no user-controlled component — no `rel="noopener"` gap and no open-redirect surface. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — this phase installs zero packages (RESEARCH.md "Package Legitimacy Audit: Not applicable"). CLAUDE.md forbids external CDN/library dependencies; no `package.json` exists and none is created. Acceptance criteria explicitly assert `package.json` absence, so any accidental dependency introduction fails the task. |
</threat_model>

<verification>
1. `node hora-phrase.test.js` exits 0 with `ALL PASS`.
2. `node -e "console.log(require('./assets/js/hora-phrase.js').buildTimePhrase(23,40))"` prints `Son las doce menos veinte de la noche`.
3. Serve the site (`python3 -m http.server 8000`) and load `http://localhost:8000/` — the `🕐 Qué hora es?` button renders full-width below "Quién soy yo" with the same gradient and accent border as the Quién soy yo button. (It will 404 until plan 02 creates `hora.html`; that is expected at this wave.)
4. `git status --porcelain` shows only the four intended files changed/added.
</verification>

<success_criteria>
- HORA-06: `buildTimePhrase` emits cuarto / media / menos cuarto / menos veinte forms and never a literal digit reading — proven by 12 exact-string assertions plus the no-digits invariant across all 288 states.
- HORA-07: every one of the 288 outputs ends with `de la mañana`, `de la tarde`, or `de la noche`, derived from the raw 24h hour — proven by the (18,45) and (23,40) cases where the spoken hour and the period hour diverge.
- HORA-01: home screen has a full-width `🕐 Qué hora es?` entry button positioned directly below "Quién soy yo" pointing at `/hora.html`.
- Zero new dependencies: no `package.json`, no CDN link, no framework.
</success_criteria>

<output>
Create `.planning/phases/22-qu-hora-es-time-telling-practice-tool/22-01-SUMMARY.md` when done.
</output>
