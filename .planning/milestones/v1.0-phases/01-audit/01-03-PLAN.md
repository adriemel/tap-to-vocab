---
phase: 01-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - AUDT-01

must_haves:
  truths:
    - "CONCERNS.md has a labelled audit section for games.html, coin-dash.html, jungle-run.html, and tower-stack.html"
    - "Each entry has a severity rating"
    - "The open question about jungle-run.html music scheduling is answered (same scheduleMusic-in-loop pattern as coin-dash, or different?)"
    - "The sessionStorage guard UX gap is documented for each game page (silent redirect with no user feedback)"
    - "Whether coin-dash, jungle-run, and tower-stack all share the same lives-decrement code duplication is documented"
  artifacts:
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Audit entries for all 4 games cluster pages"
      contains: "## Audit — Games Cluster"
  key_links:
    - from: "games/coin-dash.html sessionStorage guard"
      to: ".planning/codebase/CONCERNS.md"
      via: "Entry confirming direct-URL redirect behavior and UX gap"
    - from: "jungle-run.html game loop"
      to: ".planning/codebase/CONCERNS.md"
      via: "Entry confirming or denying scheduleMusic-on-every-frame pattern"
---

<objective>
Audit the four games cluster pages (games.html, coin-dash.html, jungle-run.html, tower-stack.html) by reading their source files and recording all issues in CONCERNS.md.

Purpose: The games cluster has zero documented entries in CONCERNS.md for jungle-run and tower-stack. The sessionStorage fragility pattern is documented at a high level but not page-by-page. The scheduleMusic bug is confirmed for coin-dash but unknown for other games. This audit closes those gaps and gives Phase 2 (Bug Fixes) a complete picture of the games cluster state.

Output: A new "## Audit — Games Cluster" section in CONCERNS.md with one sub-section per page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-audit/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit games.html and coin-dash.html</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    Read the following files in full:
    - tap-to-vocab/tap-to-vocab/games.html
    - tap-to-vocab/tap-to-vocab/games/coin-dash.html

    Apply the 9-point audit checklist from RESEARCH.md to each page:
    1. Functionality — game mechanics, coin collection, lives decrement, game-over state
    2. Navigation — is there a way back to index.html from each page? From the game-over screen?
    3. Data loading — any TSV fetches? Web Audio API initialization?
    4. Mobile layout — game canvas or layout at 375px? Overflow?
    5. Tap targets — game controls (jump, etc.) at least 44px?
    6. Visual consistency — CSS variables used? Or game-specific styles?
    7. Script dependencies — load order correct?
    8. Error states — what happens if sessionStorage lives = 0 or missing?
    9. Console errors — would normal play produce errors?

    Confirm the following known issues for games.html and coin-dash.html:
    - games.html: redirects to `/` if `sessionStorage.game_lives <= 0` — no user explanation shown (UX gap)
    - coin-dash.html: same sessionStorage guard, same silent redirect
    - coin-dash.html: `scheduleMusic()` called inside the animation loop (every frame). Find the exact line and record it.
    - Game lives decrement logic: identify the pattern in coin-dash.html. Is it the same code as in games.html and the other game files? (Documenting duplication for Phase 3)

    Additional checks for coin-dash.html:
    - Does the game correctly call `CoinTracker.addCoin()` when the player collects a coin in-game? Or does it have its own coin tracking separate from the CoinTracker system?
    - Is `CoinTracker` loaded (coins.js script tag present) on this page?
    - Does the game-over screen have a "Play Again" button and a "Home" button? Are both functional?

    Write a new section at the bottom of .planning/codebase/CONCERNS.md:

    ```
    ## Audit — Games Cluster
    *(Phase 1 audit — 2026-03-10)*

    ### games.html
    [entries]

    ### games/coin-dash.html
    [entries]
    ```

    Use the standard format:
    ```
    ### [Issue Title]
    **Severity: [Critical/High/Medium/Low]**

    [Description of broken behavior from user's perspective]

    File: [path:line if applicable]
    ```

    For the sessionStorage guard entries: note severity as High (feature broken for common path — user who bookmarks a game URL is silently redirected with no feedback).
    For scheduleMusic-every-frame: note severity as Low (performance issue, not a crash).
    For duplication of lives logic: note severity as Low (code quality / technical debt).

    Do NOT fix any code. Write only to CONCERNS.md.
  </action>
  <verify>
    <automated>MISSING — manual check: open .planning/codebase/CONCERNS.md and verify "## Audit — Games Cluster" section exists with "### games.html" and "### games/coin-dash.html" sub-sections, each with at least one entry with a severity rating.</automated>
  </verify>
  <done>
    CONCERNS.md has "## Audit — Games Cluster" with sub-sections for games.html and coin-dash.html. The scheduleMusic line number is recorded. The sessionStorage UX gap is documented with High severity. The CoinTracker integration (or lack thereof) in coin-dash is documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit jungle-run.html and tower-stack.html</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    Read the following files in full:
    - tap-to-vocab/tap-to-vocab/games/jungle-run.html
    - tap-to-vocab/tap-to-vocab/games/tower-stack.html

    Apply the 9-point audit checklist from RESEARCH.md to each.

    For BOTH files, answer the open question from RESEARCH.md:
    - Does jungle-run.html call `scheduleMusic()` inside its animation/game loop? Find the function name for the game loop (e.g., `loop()`, `gameLoop()`, `update()`), locate the music call, and record whether it's called every frame or only on state change.
    - Does tower-stack.html have the same pattern?
    - Compare the sessionStorage guard in both files to the one in coin-dash.html — is it identical copy-pasted code, or is the pattern different?

    Confirm for BOTH files:
    - sessionStorage guard present: `parseInt(sessionStorage.getItem("game_lives") || "0", 10) <= 0` → `location.replace("/")`
    - Silent redirect UX gap: no message shown before redirect
    - Lives decrement on game-over: find where lives are decremented and note if it mirrors coin-dash pattern exactly
    - CoinTracker integration: is `coins.js` loaded? Are coins awarded during gameplay?

    Additional checks:
    - tower-stack.html: Is this a canvas-based game like coin-dash and jungle-run, or different mechanic? Note the game mechanic briefly.
    - Does either game have a "Home" navigation visible during play or on the game-over screen?
    - Any mobile layout concern at 375px (canvas sizing, touch controls)?

    Append to the "## Audit — Games Cluster" section already started in Task 1:

    ```
    ### games/jungle-run.html
    [entries]

    ### games/tower-stack.html
    [entries]
    ```

    Severity guidance:
    - scheduleMusic every frame (if confirmed): Low (performance / code quality)
    - sessionStorage silent redirect: High (broken path for direct URL / bookmark users)
    - Lives decrement duplication: Low (code quality, not a bug)
    - No home navigation during play: Medium (user can be stranded mid-game)
    - Canvas sizing at mobile: note as Medium if content is cut off, Low if canvas scales reasonably

    Do NOT fix any code. Write only to CONCERNS.md.
  </action>
  <verify>
    <automated>MISSING — manual check: open .planning/codebase/CONCERNS.md and verify "### games/jungle-run.html" and "### games/tower-stack.html" sub-sections exist under "## Audit — Games Cluster", each with at least one entry.</automated>
  </verify>
  <done>
    CONCERNS.md "## Audit — Games Cluster" section has sub-sections for all 4 games cluster pages. The jungle-run.html scheduleMusic open question is answered (confirmed same issue, or noted as not present). The sessionStorage pattern is confirmed across all three game files. Code duplication between game files is documented for Phase 3.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, CONCERNS.md must have:
- Section header: "## Audit — Games Cluster"
- Sub-sections: games.html, games/coin-dash.html, games/jungle-run.html, games/tower-stack.html
- The scheduleMusic open question is definitively answered for all three game files
- The sessionStorage direct-URL UX gap is documented for each game page with High severity
- The lives decrement duplication is documented for Phase 3 (Code Cleanup)
- Every entry has a severity rating
- No code files were modified
</verification>

<success_criteria>
- All 4 games cluster pages have audit entries in CONCERNS.md
- It is documented whether jungle-run.html has the same scheduleMusic-per-frame bug as coin-dash.html
- The sessionStorage silent-redirect UX gap is confirmed on all game pages
- Any mobile layout concerns with game canvases at 375px are noted
- Zero code changes made
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-03-SUMMARY.md` following the summary template.
</output>
