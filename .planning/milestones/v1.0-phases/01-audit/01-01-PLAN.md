---
phase: 01-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - AUDT-01

must_haves:
  truths:
    - "CONCERNS.md has a labelled audit section for each of: index.html, topic.html, sentences.html, conjugation.html, fill-blank.html"
    - "Every entry in that section has a severity rating (Critical / High / Medium / Low)"
    - "Known pre-existing issues for these pages are confirmed still present or noted as resolved"
    - "Any NEW issues found on these pages (beyond CONCERNS.md) are recorded"
  artifacts:
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Audit entries for all 5 learning pages"
      contains: "## Audit — Learning Pages"
  key_links:
    - from: "index.html inline scripts"
      to: ".planning/codebase/CONCERNS.md"
      via: "Audit entry confirming inline JS presence and coin gate logic"
    - from: "tapvocab.js btnQuizBack.onclick"
      to: ".planning/codebase/CONCERNS.md"
      via: "Audit entry confirming coin-refund bug still present"
---

<objective>
Audit the five learning pages (index, topic, sentences, conjugation, fill-blank) by reading their source files and recording all issues — new and confirmed — in CONCERNS.md.

Purpose: These five pages are the core learning experience. This audit produces the authoritative issue list that Phase 2 (Bug Fixes) and Phase 3 (Code Cleanup) will work from. Pre-existing CONCERNS.md entries are confirmed or updated; new findings are appended.

Output: A new "## Audit — Learning Pages" section in CONCERNS.md with one sub-section per page, each containing all discovered issues with severity ratings.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-audit/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit index.html and topic.html</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    Read the following files in full:
    - tap-to-vocab/tap-to-vocab/index.html
    - tap-to-vocab/tap-to-vocab/assets/js/tapvocab.js
    - tap-to-vocab/tap-to-vocab/assets/js/coins.js
    - tap-to-vocab/tap-to-vocab/assets/js/shared-utils.js

    For EACH page apply the 9-point checklist from RESEARCH.md:
    1. Functionality — every button, coin gate, game-lives sessionStorage write
    2. Navigation — is there always a way back/home?
    3. Data loading — TSV fetch; what happens on failure?
    4. Mobile layout — 375px: does 2-column grid wrap? Is content cut off?
    5. Tap targets — are buttons/cards at least 44px tall?
    6. Visual consistency — CSS variables used? Any raw hex colors?
    7. Script dependencies — load order: coins.js → shared-utils.js → page JS?
    8. Error states — graceful fallback when something goes wrong?
    9. Console errors — would normal use produce errors?

    Confirm the following known issues are still present (from RESEARCH.md):
    - index.html: inline `<script>` blocks (coin counter, reset button, game-lives sessionStorage set)
    - index.html: no favicon `<link rel="icon">` in `<head>`
    - topic.html / tapvocab.js: `btnQuizBack.onclick` decrements `correctCount` but does NOT call `CoinTracker.spendCoins(1)` or equivalent after a "Correct" answer is undone
    - tapvocab.js `savePracticeList`: `catch(e) { console.warn(...) }` with no user-visible feedback
    - tapvocab.js: `getVoices()` / `voiceschanged` pattern that fails on iOS/Safari
    - Quiz flip card animation reset: check the `inner.style.transition = "none"` / force-reflow pattern is present (this was a previously fixed bug — confirm fix is intact)

    Document any additional issues not yet in CONCERNS.md, including:
    - Whether index.html has a favicon (expected: missing)
    - Whether topic.html has a back/home button visible in browse AND quiz modes
    - Any elements that look like they could overflow on 375px (e.g., button groups, word tiles)
    - The `grid-two-col` 2-column category grid in index.html — does it force narrow buttons at 375px?

    Write a new section at the bottom of .planning/codebase/CONCERNS.md:

    ```
    ## Audit — Learning Pages
    *(Phase 1 audit — 2026-03-10)*

    ### index.html
    [entries]

    ### topic.html (tapvocab.js)
    [entries]
    ```

    Use the format established in CONCERNS.md:
    ```
    ### [Issue Title]
    **Severity: [Critical/High/Medium/Low]**

    [Description of broken behavior from the user's perspective]

    File: [path:line if applicable]
    ```

    For issues that were ALREADY in CONCERNS.md above, write a one-line confirmation:
    `(Confirmed present — see pre-existing entry above)`
    rather than duplicating the full description.

    Do NOT fix any code. Do NOT modify any HTML or JS files. Write only to CONCERNS.md.
  </action>
  <verify>
    <automated>MISSING — no automated test framework for this project. Manual check: open .planning/codebase/CONCERNS.md and verify "## Audit — Learning Pages" section exists with "### index.html" and "### topic.html (tapvocab.js)" sub-sections, each containing at least one entry with a severity rating.</automated>
  </verify>
  <done>
    CONCERNS.md has a new "## Audit — Learning Pages" section. index.html sub-section confirms or records: no favicon, inline scripts, grid-two-col mobile behavior. topic.html sub-section confirms or records: quiz back button coin-refund gap, localStorage silent failure, iOS/Safari voice bug. Every entry has a severity rating.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit sentences.html, conjugation.html, fill-blank.html</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    Read the following files in full:
    - tap-to-vocab/tap-to-vocab/sentences.html
    - tap-to-vocab/tap-to-vocab/assets/js/sentences.js
    - tap-to-vocab/tap-to-vocab/conjugation.html
    - tap-to-vocab/tap-to-vocab/assets/js/conjugation.js
    - tap-to-vocab/tap-to-vocab/fill-blank.html
    - tap-to-vocab/tap-to-vocab/assets/js/fill-blank.js

    For EACH page apply the 9-point checklist from RESEARCH.md (same as Task 1).

    Confirm the following known issues are still present:
    - sentences.js `saveEnabledSentences`: same `console.warn`-only catch pattern (silent localStorage failure)
    - conjugation.js: has its own `loadVerbs` TSV loader (not using SharedUtils.loadWords) — confirm the function exists and is not delegating to SharedUtils
    - fill-blank.js: has its own `loadSentences` TSV loader — confirm it exists; confirm the `(cols[i] || "").trim()` guard is present (previously fixed incomplete-row crash)
    - User state keyed by text strings in all three: practice list, enabled sentences, enabled verbs stored as text keys

    Investigate these OPEN QUESTIONS from RESEARCH.md:
    - sentences.html: Does the "back button with history tracking" pattern actually pop from the history array and re-render without re-fetching data? Or does it reload the full TSV? (document behavior, don't fix)
    - conjugation.html: Is the `max-height: 700px and max-width: 600px` media query present? Does it look correct at 375×667px (iPhone SE)?
    - fill-blank.html: Are both TSV categories (`hay_vs_estar`, `verb_conjugation`) loaded? Is there a category filter UI or does the page just show all exercises?

    Additional checks:
    - Does each page have a working back/home navigation control?
    - Are there any `innerHTML` assignments (XSS risk — note severity Low, acceptable for static site)?
    - Do script `<script src="">` tags appear in correct order: coins.js → shared-utils.js → page JS?
    - Are tap targets (buttons, word tiles) visually at least 44px tall based on CSS?

    Append to the "## Audit — Learning Pages" section already created by Task 1:

    ```
    ### sentences.html (sentences.js)
    [entries]

    ### conjugation.html (conjugation.js)
    [entries]

    ### fill-blank.html (fill-blank.js)
    [entries]
    ```

    Use the same format. For already-documented issues, write the one-line confirmation rather than duplicating.

    Do NOT fix any code. Write only to CONCERNS.md.
  </action>
  <verify>
    <automated>MISSING — manual check: open .planning/codebase/CONCERNS.md and verify "### sentences.html", "### conjugation.html", "### fill-blank.html" sub-sections exist under "## Audit — Learning Pages", each with at least one entry.</automated>
  </verify>
  <done>
    CONCERNS.md "## Audit — Learning Pages" section now has sub-sections for all 5 learning pages. Each sub-section has at least one entry with a severity rating. Known issues are confirmed or noted resolved. Open questions from RESEARCH.md are answered inline.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, CONCERNS.md must have:
- Section header: "## Audit — Learning Pages"
- Sub-sections: index.html, topic.html, sentences.html, conjugation.html, fill-blank.html
- Every entry has a severity (Critical / High / Medium / Low)
- Known pre-existing issues are either confirmed present or noted as resolved
- No code files were modified (only CONCERNS.md was written)
</verification>

<success_criteria>
- All 5 learning pages have audit entries in CONCERNS.md
- The quiz back button coin-refund bug is confirmed (or resolved) with file:line reference
- The localStorage silent failure pattern is confirmed in sentences.js and conjugation.js
- The duplicated TSV loaders in conjugation.js and fill-blank.js are confirmed
- Any mobile layout or tap-target issues at 375px are documented
- Zero code changes made
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-01-SUMMARY.md` following the summary template.
</output>
