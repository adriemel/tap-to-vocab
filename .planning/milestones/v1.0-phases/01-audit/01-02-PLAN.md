---
phase: 01-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - AUDT-01

must_haves:
  truths:
    - "CONCERNS.md has a labelled audit section for vocab.html and voices.html"
    - "Each entry has a severity rating"
    - "The open question about vocab.html reachability is answered (linked from app or orphaned?)"
    - "The open question about voices.html navigation is answered (intentional debug-only or needs back button?)"
  artifacts:
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Audit entries for vocab.html and voices.html"
      contains: "## Audit — Standalone Pages"
  key_links:
    - from: "vocab.html inline styles"
      to: ".planning/codebase/CONCERNS.md"
      via: "Entry documenting visual inconsistency with main CSS variable system"
    - from: "All HTML files"
      to: ".planning/codebase/CONCERNS.md"
      via: "Entry answering whether any page links to vocab.html or voices.html"
---

<objective>
Audit the two standalone/debug pages (vocab.html and voices.html) by reading their source files and all HTML files that might link to them, recording all issues in CONCERNS.md.

Purpose: These pages are not yet covered in CONCERNS.md at all. vocab.html uses a completely different visual system (inline styles, light theme). voices.html is a debug utility with no app navigation. Both need fresh inspection to close the gap in the issue registry before Phase 4 (UI Polish) can address them.

Output: A new "## Audit — Standalone Pages" section in CONCERNS.md with one sub-section per page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-audit/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit vocab.html</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    Read the following files in full:
    - tap-to-vocab/tap-to-vocab/vocab.html
    - tap-to-vocab/tap-to-vocab/index.html (check for link to vocab.html)
    - tap-to-vocab/tap-to-vocab/games.html (check for link to vocab.html)

    Also search all HTML files for any href or src referencing vocab.html:
    Read tap-to-vocab/tap-to-vocab/topic.html, sentences.html, conjugation.html, fill-blank.html headers/nav sections to look for vocab.html links.

    Apply the 9-point audit checklist from RESEARCH.md:
    1. Functionality — what does the page do? Does every interactive element work as coded?
    2. Navigation — is there a back button, home link, or any navigation at all?
    3. Data loading — does it fetch a TSV? Which one? What happens on failure?
    4. Mobile layout — does the inline CSS `max-width: 720px; margin: 2rem auto` work at 375px? Any overflow?
    5. Tap targets — are interactive elements at least 44px?
    6. Visual consistency — uses inline `<style>` block with light theme instead of styles.css CSS variables. Note ALL deviations.
    7. Script dependencies — does it load coins.js, shared-utils.js? If not, note what's missing.
    8. Error states — graceful fallback if TSV fails?
    9. Console errors — would normal use produce errors?

    Specific questions to answer inline in CONCERNS.md entries:
    - Is vocab.html linked from ANY other page in the app? (If not: mark as orphaned — users cannot reach it without knowing the direct URL)
    - Does vocab.html have any navigation element (back button, home link, header with nav)?
    - Does vocab.html use `CoinTracker`? If not, correct answers give no coins.
    - Does vocab.html use `SharedUtils` functions (shuffleArray, playSuccessSound, etc.)? Or does it have its own implementations?
    - What is the visual theme: dark CSS variable system or light inline styles?

    Write a new section at the bottom of .planning/codebase/CONCERNS.md:

    ```
    ## Audit — Standalone Pages
    *(Phase 1 audit — 2026-03-10)*

    ### vocab.html
    [entries]
    ```

    Use the standard format:
    ```
    ### [Issue Title]
    **Severity: [Critical/High/Medium/Low]**

    [Description of the problem from the user's perspective]

    File: [path:line if applicable]
    ```

    Severity guidance for this page:
    - Orphaned page (no links to it) = Medium (user cannot discover it)
    - No navigation back = Medium (user is stranded if they find it)
    - Not integrated with coin system = Low (missing feature, not broken)
    - Visual inconsistency with rest of app = Low (cosmetic)
    - Missing `(cols[i] || "").trim()` guard if it loads TSV = High (potential crash)

    Do NOT fix any code. Write only to CONCERNS.md.
  </action>
  <verify>
    <automated>MISSING — manual check: open .planning/codebase/CONCERNS.md and verify "## Audit — Standalone Pages" section exists with "### vocab.html" sub-section containing at least one entry with a severity rating.</automated>
  </verify>
  <done>
    CONCERNS.md has "## Audit — Standalone Pages" with a vocab.html sub-section. The open questions (reachability, navigation, coin integration, SharedUtils usage) are all answered with specific findings. Every finding has a severity.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit voices.html</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    Read the following file in full:
    - tap-to-vocab/tap-to-vocab/voices.html

    Also check all other HTML files for any link to voices.html (same sweep as Task 1 if not already done).

    Apply the 9-point audit checklist from RESEARCH.md:
    1. Functionality — what does the debug page do? List voices available? Test speech synthesis? Does the functionality work as coded?
    2. Navigation — is there any way back to the main app?
    3. Data loading — does it use Web Speech API? What if no voices are available?
    4. Mobile layout — does it display acceptably at 375px?
    5. Tap targets — any buttons at least 44px?
    6. Visual consistency — minimal/debug styling vs. main app CSS variable system. Note deviations.
    7. Script dependencies — does it load coins.js or shared-utils.js?
    8. Error states — what if `speechSynthesis` is unavailable?
    9. Console errors — would normal use produce errors?

    Specific questions to answer inline:
    - Is voices.html linked from any other page? (Expected: no — it's a debug utility)
    - Is it intentionally user-facing or intentionally developer-only? (Note based on what you observe in the file)
    - Does it have a back button or any navigation?
    - Would removing it or adding a back button be appropriate? (Note your finding — the fix goes in Phase 4)

    Append to the "## Audit — Standalone Pages" section:

    ```
    ### voices.html
    [entries]
    ```

    Severity guidance:
    - No back navigation = Low (debug utility, probably not user-facing)
    - No styling from main app = Low (intentional for a debug page)
    - speechSynthesis unavailable = Medium if no guard, Low if guarded

    Do NOT fix any code. Write only to CONCERNS.md.
  </action>
  <verify>
    <automated>MISSING — manual check: open .planning/codebase/CONCERNS.md and verify "### voices.html" sub-section exists under "## Audit — Standalone Pages" with at least one entry.</automated>
  </verify>
  <done>
    CONCERNS.md "## Audit — Standalone Pages" section has sub-sections for both vocab.html and voices.html. The open questions from RESEARCH.md about navigation and reachability are answered with specific findings. Every entry has a severity rating.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, CONCERNS.md must have:
- Section header: "## Audit — Standalone Pages"
- Sub-sections: vocab.html, voices.html
- The vocab.html reachability question is answered (is it linked or orphaned?)
- The voices.html navigation question is answered (back button needed or intentionally debug-only?)
- Every entry has a severity rating
- No code files were modified
</verification>

<success_criteria>
- vocab.html and voices.html have audit entries in CONCERNS.md with severity ratings
- It is documented whether either page is reachable from the main app
- The visual inconsistency of vocab.html (inline styles vs. CSS variable system) is documented
- Any functional gaps (no coins, no SharedUtils) are documented
- Zero code changes made
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-02-SUMMARY.md` following the summary template.
</output>
