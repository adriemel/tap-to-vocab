---
phase: 04-ui-mobile-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tap-to-vocab/vocab.html
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - MOB-03

must_haves:
  truths:
    - "vocab.html renders with the dark theme (dark background, light text) — no white page"
    - "vocab.html displays a visible Home link that navigates to /"
    - "vocab.html uses CSS variables for colors — no raw #ddd, #555 hardcodes"
    - "vocab.html coin counter is visible and updates on correct spelling answers"
  artifacts:
    - path: "tap-to-vocab/vocab.html"
      provides: "Dark-themed word card page integrated with design system"
      contains: "styles.css link, coins.js script, .btn.secondary home link, CoinTracker.addCoin()"
  key_links:
    - from: "tap-to-vocab/vocab.html"
      to: "assets/css/styles.css"
      via: "<link rel=\"stylesheet\" href=\"/assets/css/styles.css\" />"
      pattern: "link.*styles\\.css"
    - from: "tap-to-vocab/vocab.html"
      to: "CoinTracker"
      via: "coins.js script tag + CoinTracker.addCoin() in form submit handler"
      pattern: "CoinTracker\\.addCoin"
---

<objective>
Bring vocab.html into the design system. It is the only page that does not link styles.css and has no home navigation — a user who lands on it is stranded on a white-background page that looks like a different app.

Purpose: Close the two hardest UI consistency failures (UI-01, UI-02) with a single focused file edit.
Output: vocab.html with dark theme, home button, CSS variables, and coin integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ui-mobile-polish/04-RESEARCH.md

<interfaces>
<!-- Current vocab.html structure (read from file) -->
<!-- Head: has viewport meta, favicon, inline <style> block, NO styles.css link -->
<!-- Body: single .card div, no .container wrapper, no home navigation -->
<!-- Script: IIFE reading URL params ?w=, ?de=, ?lang=, ?voice=, ?sent=, ?mode= -->
<!-- The script block must NOT be touched — only the <head> styles and <body> HTML change -->

<!-- CSS variables available in styles.css :root -->
/* --bg: #0b1020 */
/* --card: #121831 */
/* --ink: #e8ecff */
/* --muted: #aeb6d9 */
/* --accent: #6ca8ff */
/* --ok: #3ddc97 */
/* --warn: #ffcc66 */
/* --error: #ff6b9d */

<!-- Standard page structure used by all other pages -->
<body>
  <div class="container">
    <div class="card">
      <div style="display:flex; align-items:center; justify-content:space-between; gap:10px; flex-wrap:wrap;">
        <h1>...</h1>
        <span class="badge coin-badge" id="coin-counter"><span class="coin-icon"></span> 0</span>
      </div>
      <!-- page content -->
      <div class="controls" style="margin-top:16px;">
        <a class="btn secondary" href="/">🏠 Home</a>
      </div>
    </div>
  </div>
</body>

<!-- Script load order used by all other pages -->
<script src="/assets/js/coins.js"></script>
<!-- shared-utils.js is NOT needed — vocab.html does not use SharedUtils -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline style block with styles.css link and add coins.js</name>
  <files>tap-to-vocab/vocab.html</files>
  <action>
In vocab.html's &lt;head&gt; section:
1. Remove the entire inline &lt;style&gt; block (lines 8-17, everything between &lt;style&gt; and &lt;/style&gt; inclusive).
2. Add these two lines before &lt;/head&gt;:
   &lt;link rel="stylesheet" href="/assets/css/styles.css" /&gt;
   &lt;script src="/assets/js/coins.js"&gt;&lt;/script&gt;

Do NOT touch the &lt;script&gt; block at the bottom of the file.

After this change vocab.html has no inline styles — all visual styling comes from styles.css.
  </action>
  <verify>Open vocab.html (with a local server at port 8000). The page should have a dark background (--bg: #0b1020). If it renders white, the styles.css link is missing or wrong path.</verify>
  <done>vocab.html &lt;head&gt; has styles.css link and coins.js script, no inline &lt;style&gt; block. Dark background renders at http://localhost:8000/vocab.html?w=hola&amp;de=Hallo</done>
</task>

<task type="auto">
  <name>Task 2: Add .container wrapper, coin badge, and Home navigation to body</name>
  <files>tap-to-vocab/vocab.html</files>
  <action>
Restructure the &lt;body&gt; content to match the standard page pattern:

1. Wrap the existing content in &lt;div class="container"&gt;&lt;div class="card"&gt;...&lt;/div&gt;&lt;/div&gt;.

2. Add a header row at the top of the card (before the existing h1):
   &lt;div style="display:flex; align-items:center; justify-content:space-between; gap:10px; flex-wrap:wrap;"&gt;
     &lt;h1 id="title" style="margin:0;"&gt;Tap-to-Vocab (Deutsch ↔ Español)&lt;/h1&gt;
     &lt;span class="badge coin-badge" id="coin-counter"&gt;&lt;span class="coin-icon"&gt;&lt;/span&gt; 0&lt;/span&gt;
   &lt;/div&gt;
   Remove the original standalone &lt;h1 id="title"&gt; tag (it is now inside the flex row).

3. Add a Home button at the bottom of the card (after the &lt;p id="sentence"&gt;&lt;/p&gt;):
   &lt;div class="controls" style="margin-top:16px;"&gt;
     &lt;a class="btn secondary" href="/"&gt;🏠 Home&lt;/a&gt;
   &lt;/div&gt;

The existing #prompt .big, #translation .hint, form, etc. remain unchanged. Only the wrapper divs and the header/footer rows are added.

The .hint class no longer exists in styles.css — replace it with an inline style or use the .muted class if available. Check: in styles.css there is no .hint class. The #translation element with class "hint" needs color: var(--muted) to show as secondary text. Add style="color:var(--muted);" to the div#translation element.

IMPORTANT: Do NOT add topic.html links to vocab.html. Do NOT add TSV loading. Do NOT add game features. This is styling only.
  </action>
  <verify>Visit http://localhost:8000/vocab.html?w=hola&amp;de=Hallo — page should show dark background, coin badge top-right, h1 title, and Home button at bottom. Coin badge auto-updates via coins.js.</verify>
  <done>vocab.html has .container &gt; .card structure, coin badge with id="coin-counter", Home link with class="btn secondary", and #translation uses color:var(--muted). Page matches dark theme of all other pages.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CoinTracker.addCoin() to correct spelling answers</name>
  <files>tap-to-vocab/vocab.html</files>
  <action>
In vocab.html's IIFE script block, find the form submit handler (the `form.addEventListener("submit", ...)` section).

Locate the line that sets feedback.textContent to "✅ Richtig!" — this fires only when `ok` is true (correct answer).

Add `CoinTracker.addCoin();` immediately after `feedback.textContent = ok ? "✅ Richtig!" : ...` inside the `if (ok)` block.

The exact insertion point is after the `if (ok) {` opening brace, before the speak() call:
```
if (ok) {
  CoinTracker.addCoin();   // ← add this line
  if (direction === "de-es") speak(w, lang);
  ...
}
```

coins.js is loaded before the IIFE script in the &lt;head&gt;, so CoinTracker is available.
  </action>
  <verify>Visit http://localhost:8000/vocab.html?w=hola&amp;de=Hallo. Type "hola" in the spelling input and submit. The coin badge should increment by 1. Open DevTools console — no errors.</verify>
  <done>Correct spelling answer increments coin badge. CoinTracker.addCoin() is called inside the if(ok) block of the form submit handler.</done>
</task>

</tasks>

<verification>
Manual verification steps after all tasks complete:

1. Open http://localhost:8000/vocab.html?w=hola&de=Hallo
2. Confirm dark background (#0b1020), light text, card with border-radius
3. Confirm coin badge visible top-right
4. Confirm "🏠 Home" button visible at bottom
5. Type "hola" and submit — confirm coin badge increments
6. Click Home — confirm navigates to /
7. Resize browser to 375px width — confirm no horizontal scroll, no content cut off
</verification>

<success_criteria>
- vocab.html renders with dark theme identical to other app pages
- vocab.html has a visible, working Home navigation link
- vocab.html uses CSS variables (no raw #ddd, #555 hardcodes remaining)
- Correct spelling answers award 1 coin
- No horizontal overflow at 375px
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-mobile-polish/04-01-SUMMARY.md`
</output>
