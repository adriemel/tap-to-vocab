---
phase: 04-ui-mobile-polish
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - tap-to-vocab/games/jungle-run.html
autonomous: false
requirements:
  - UI-01
  - MOB-01
  - MOB-02
  - MOB-03

must_haves:
  truths:
    - "jungle-run.html overlay buttons use var(--warn) for the primary gold color instead of hardcoded #f0a030"
    - "All pages pass visual inspection at 375px — no horizontal scroll, no cut-off content"
    - "All interactive learning elements visually appear large enough to tap on a 375px screen"
    - "Every page has a visible home or back navigation control"
  artifacts:
    - path: "tap-to-vocab/games/jungle-run.html"
      provides: "Overlay buttons using CSS variable --warn instead of hardcoded #f0a030"
      contains: "var(--warn)"
  key_links:
    - from: "tap-to-vocab/games/jungle-run.html"
      to: "CSS variable --warn (#ffcc66)"
      via: "Inline <style> block .btn background and .final-score color updated"
      pattern: "var\\(--warn\\)"
---

<objective>
Update jungle-run.html overlay button colors to use the --warn CSS variable instead of the hardcoded `#f0a030` amber, then run a full visual verification across all pages to confirm the phase is complete.

Purpose: Close the last UI-01 inconsistency (game overlay buttons using off-palette hardcoded color) and confirm all six phase success criteria hold across every page.
Output: Updated jungle-run.html inline style block; human sign-off on full app visual review.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ui-mobile-polish/04-RESEARCH.md

<interfaces>
<!-- jungle-run.html inline <style> block — current hardcoded colors to replace -->
/* .game-overlay h2 { color: #e8e0d6; } → use color: var(--ink) */
/* .final-score { color: #f0a030; } → use color: var(--warn) */
/* .score-label { color: #888; } → use color: var(--muted) — optional, muted is close */
/* .game-overlay p { color: #999; } → use color: var(--muted) — optional */
/* .btn { background: #f0a030; color: #1a1a2e; } → background: var(--warn); color: var(--bg) */
/* .btn.secondary { color: #ccc; border: 1px solid #555; } → color: var(--muted); border-color kept as-is */

<!-- PITFALL: Do NOT link styles.css in jungle-run.html -->
<!-- The page uses overflow:hidden on html,body and full-screen canvas layout -->
<!-- styles.css would apply padding/margin to body that would break the canvas game -->
<!-- Only update the hardcoded hex colors inside the existing inline <style> block -->
<!-- CSS variables declared in styles.css :root will NOT be available in jungle-run.html -->
<!-- without linking styles.css. Solution: declare the needed variables in a :root block -->
<!-- INSIDE jungle-run.html's inline &lt;style&gt; block. -->

<!-- Add at the TOP of jungle-run.html's inline &lt;style&gt; block: -->
:root {
  --bg: #0b1020;
  --ink: #e8ecff;
  --muted: #aeb6d9;
  --warn: #ffcc66;
}
<!-- Then replace #f0a030 with var(--warn), #e8e0d6 with var(--ink), #1a1a2e with var(--bg) -->
<!-- The .game-overlay background rgba(6,10,22,0.88) is fine as-is — not a named color -->
<!-- Do NOT touch the &lt;script&gt; block -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded hex colors in jungle-run.html inline style block</name>
  <files>tap-to-vocab/games/jungle-run.html</files>
  <action>
In jungle-run.html, edit only the &lt;style&gt; block inside &lt;head&gt;. Do NOT touch the &lt;script&gt; block.

**Step 1:** Add a `:root` block at the very top of the &lt;style&gt; block (before the existing `* { margin: 0; ... }` rule):
```css
:root {
  --bg: #0b1020;
  --ink: #e8ecff;
  --muted: #aeb6d9;
  --warn: #ffcc66;
}
```

**Step 2:** Replace the following hardcoded hex values throughout the rest of the &lt;style&gt; block:
- `background: #0b1a2e` on html,body → `background: var(--bg)` (note: #0b1a2e is close to --bg #0b1020 — use --bg for consistency)
- `.game-overlay h2 { color: #e8e0d6; }` → `color: var(--ink);`
- `.final-score { color: #f0a030; }` → `color: var(--warn);`
- `.score-label { color: #888; }` → `color: var(--muted);`
- `.game-overlay p { color: #999; }` → `color: var(--muted);`
- `.btn { background: #f0a030; color: #1a1a2e; }` → `background: var(--warn); color: var(--bg);`
- `.btn.secondary { color: #ccc; border: 1px solid #555; }` → `color: var(--muted);` (keep border as-is)

Leave these unchanged (they are layout-critical, not color variables):
- `rgba(6,10,22,0.88)` — overlay backdrop
- `rgba(0,0,0,0.5)` on hud-mute and hud-back
- `rgba(255,255,255,0.15)` border on hud buttons
- `background: rgba(0,0,0,0.5)` on hud elements

Do NOT add any new CSS rules. Do NOT link styles.css. Do NOT touch any &lt;script&gt; blocks.
  </action>
  <verify>Open http://localhost:8000/games/jungle-run.html — the start overlay should show the title and score in the same amber/gold color as before (--warn is #ffcc66, similar to the old #f0a030 orange-amber). Click Start to confirm the game runs correctly. The overlay button should be gold-colored.</verify>
  <done>jungle-run.html inline &lt;style&gt; block has a :root block defining 4 CSS variables, and all former #f0a030/#e8e0d6/#888/#999 hardcodes are replaced with var() references. Game still launches and runs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification across all pages at desktop and 375px</name>
  <what-built>
Full Phase 4 UI and mobile polish:
- Plan 01: vocab.html brought into design system — dark theme, home button, coin integration
- Plan 02: 44px tap targets on .word-btn, .choice-btn, .btn-wrong, .btn-correct; fill-blank.html header margin fixed; narrow grid media query for index.html
- Plan 03 (this plan): jungle-run.html overlay buttons use CSS variables
  </what-built>
  <how-to-verify>
Serve the app locally: `cd tap-to-vocab/tap-to-vocab && python3 -m http.server 8000`

**Desktop check (any window width):**
1. index.html — home grid loads, category buttons readable, coin badge visible
2. topic.html?cat=Essen — browse mode loads, flip card works, quiz mode loads
3. sentences.html — word bank tiles visible, home button present
4. conjugation.html — conjugation table visible, home button present
5. fill-blank.html — header row: h1 and coin badge on same horizontal line (no extra gap below h1)
6. vocab.html?w=hola&de=Hallo — dark background, coin badge top-right, Home button at bottom
7. games.html — game cards visible, Back to Learning link present
8. games/jungle-run.html — overlay has gold/amber button, game launches and runs

**Mobile check (resize browser to 375px width, or use DevTools device emulation):**
9. index.html at 375px — category buttons readable (smaller font), no horizontal scroll
10. sentences.html at 375px — word-btn tiles visually large enough to tap (inspect height in DevTools: should show >= 44px)
11. fill-blank.html at 375px — choice-btn buttons visually large enough to tap
12. topic.html quiz mode at 375px — btn-wrong and btn-correct are tall enough to tap comfortably
13. vocab.html at 375px — no horizontal scroll, all content visible
14. Any page at 375px — horizontal scroll test: try to scroll right; there should be no overflow

**Navigation check:**
15. Every page has a Home or Back button (vocab.html is the one that was missing — confirm it now has one)
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass. Or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
The checkpoint task IS the verification for this plan. All 6 phase success criteria from ROADMAP.md are tested in the checkpoint:
1. CSS variables — confirmed by visual dark-theme appearance on all pages
2. Navigation — every page has home/back control
3. No misalignment — header rows, buttons, content checked at desktop
4. 375px usable — confirmed by mobile resize check
5. 44px tap targets — confirmed by DevTools height inspection
6. No horizontal scroll at 375px — confirmed by scroll test
</verification>

<success_criteria>
- jungle-run.html overlay uses var(--warn) for gold color
- Human approves visual inspection of all 8 pages at desktop + 375px
- No issues reported on: dark theme, navigation, alignment, mobile layout, tap targets, horizontal scroll
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-mobile-polish/04-03-SUMMARY.md`
</output>
