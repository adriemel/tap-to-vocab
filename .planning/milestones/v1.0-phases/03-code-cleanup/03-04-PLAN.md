---
phase: 03-code-cleanup
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - tap-to-vocab/CLAUDE.md
autonomous: true
requirements:
  - STRCT-04

must_haves:
  truths:
    - "CLAUDE.md accurately describes SharedUtils and all its exports including loadTSV"
    - "CLAUDE.md accurately describes CoinTracker (coins.js) and its API"
    - "CLAUDE.md describes the correct script load order: coins.js → game-init.js (games only) → shared-utils.js → [page js]"
    - "CLAUDE.md lists home.js as the JS module for index.html logic"
    - "CLAUDE.md contains no references to old patterns (no mention of copy-paste loaders, no description of loadWords as the only shared utility)"
  artifacts:
    - path: "tap-to-vocab/CLAUDE.md"
      provides: "Accurate architecture documentation"
      contains: "SharedUtils"
---

<objective>
Update CLAUDE.md to accurately reflect the architecture after Phase 3 cleanup. The current CLAUDE.md describes the pre-SharedUtils state and is now misleading.

Purpose: CLAUDE.md is the primary context file read at the start of every Claude session. If it describes old patterns, future sessions will reintroduce the patterns this phase just removed.
Output: tap-to-vocab/CLAUDE.md updated with accurate module list, SharedUtils API, CoinTracker API, and script load order.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current CLAUDE.md state (the parts that need updating). -->

Current CLAUDE.md "JavaScript modules" section describes:
- tapvocab.js → window.TapVocabTSV
- sentences.js → window.SentenceBuilder
- States: "Both JS files contain their own copy of loadWords() (TSV parser) and shuffleArray() — there is no shared module" [OUTDATED - must be removed]

Current CLAUDE.md "Shared patterns" section says those two patterns exist in both files [OUTDATED].

Missing from CLAUDE.md:
- shared-utils.js and its exports (shuffleArray, loadWords, loadTSV, playSuccessSound, playErrorSound, showSuccessAnimation, confettiBurst)
- coins.js and CoinTracker API (addCoin, getCoins, spendCoins, resetCoins)
- home.js (new in Phase 3)
- game-init.js (new in Phase 3)
- conjugation.js → window.VerbConjugation
- fill-blank.js → window.FillBlank
- Script load order pattern

Current accurate state after Phase 3:
JS modules:
- assets/js/coins.js → window.CoinTracker — coin persistence in localStorage, auto-updates #coin-counter
- assets/js/shared-utils.js → window.SharedUtils — shuffleArray, loadWords (words.tsv), loadTSV (generic any-TSV), playSuccessSound, playErrorSound, showSuccessAnimation, confettiBurst
- assets/js/home.js — home page logic (practice count badge, games button, reset coins)
- assets/js/game-init.js → window.GameInit — requireLives(selector) guard for game pages
- assets/js/tapvocab.js → window.TapVocabTSV — Browse/Quiz mode
- assets/js/sentences.js → window.SentenceBuilder — sentence building game
- assets/js/conjugation.js → window.VerbConjugation — verb conjugation practice
- assets/js/fill-blank.js → window.FillBlank — fill-in-the-blank game

Script load order:
- All pages: coins.js first (if coin display needed)
- Game pages: game-init.js second
- Learning pages: shared-utils.js before the page's own JS
- index.html: coins.js → home.js (no shared-utils needed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md JavaScript modules section to reflect current architecture</name>
  <files>tap-to-vocab/CLAUDE.md</files>
  <action>
Read the current tap-to-vocab/CLAUDE.md in full, then rewrite the "JavaScript modules" and "Shared patterns" sections (and any other sections with stale architecture descriptions) to reflect the actual current state.

Specific changes required:

1. **Remove** the sentence: "Both JS files contain their own copy of `loadWords()` (TSV parser) and `shuffleArray()` — there is no shared module"

2. **Replace** the "JavaScript modules" section with a complete accurate list of all JS files:
   ```
   **JavaScript modules** (all use the IIFE pattern, export to `window`):
   - `assets/js/coins.js` → `window.CoinTracker` — coin persistence via localStorage; `addCoin()`, `getCoins()`, `spendCoins(amount)`, `resetCoins()`; auto-updates any `#coin-counter` element
   - `assets/js/shared-utils.js` → `window.SharedUtils` — `shuffleArray(arr)`, `loadWords(path)` (words.tsv-specific), `loadTSV(path)` (generic any-TSV, returns header-keyed row objects), `playSuccessSound()`, `playErrorSound()`, `showSuccessAnimation()`, `confettiBurst(count)`
   - `assets/js/home.js` — home page logic: practice count badge, Games button (spend 10 coins + set sessionStorage game_lives), Reset Coins button
   - `assets/js/game-init.js` → `window.GameInit` — `requireLives(selector)`: returns false and inserts error HTML when sessionStorage game_lives is missing or 0 (direct URL bypass guard)
   - `assets/js/tapvocab.js` → `window.TapVocabTSV` — Browse mode, Quiz mode with flip cards, practice list (localStorage), speech synthesis
   - `assets/js/sentences.js` → `window.SentenceBuilder` — sentence building game, word bank scrambling, sentence manager modal
   - `assets/js/conjugation.js` → `window.VerbConjugation` — verb conjugation practice (tap-to-fill table), Show mode, verb manager modal
   - `assets/js/fill-blank.js` → `window.FillBlank` — fill-in-the-blank game with multiple-choice answers
   ```

3. **Update** the "Shared patterns" section to say:
   ```
   **Shared patterns:**
   - All learning game pages (tapvocab, sentences, conjugation, fill-blank) load `shared-utils.js` before their own JS
   - Script load order: `coins.js` → (if game page: `game-init.js`) → `shared-utils.js` → `[page js]`
   - TSV loading: `SharedUtils.loadTSV(path)` for any TSV; `SharedUtils.loadWords(path)` for words.tsv specifically
   - Shuffle: `SharedUtils.shuffleArray(arr)` — Fisher-Yates, non-mutating
   - `coins.js` auto-updates any `#coin-counter` element via DOMContentLoaded + coinschanged CustomEvent listener
   ```

4. Keep all other sections unchanged (Project Overview, Development, Data flow, Pages and routing, Adding Vocabulary, Key Conventions).

Do not add any new sections or change the document structure beyond the updates listed above.
  </action>
  <verify>
    <automated>grep -c "SharedUtils\|CoinTracker\|home\.js\|game-init\.js" /home/desire/tap-to-vocab/tap-to-vocab/CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md references SharedUtils, CoinTracker, home.js, and game-init.js. No mention of "there is no shared module" or "both JS files contain their own copy". loadTSV is documented. Script load order is described.</done>
</task>

</tasks>

<verification>
1. `grep -n "no shared module\|their own copy" tap-to-vocab/CLAUDE.md` — should return no matches
2. `grep -n "SharedUtils\|CoinTracker\|loadTSV\|game-init\|home\.js" tap-to-vocab/CLAUDE.md` — should return multiple matches confirming all four are documented
3. Read CLAUDE.md and confirm it reads as an accurate description of the codebase a new Claude session would encounter
</verification>

<success_criteria>
- CLAUDE.md documents all 8 JS files with their exports and purpose
- Script load order is explicitly described
- No stale descriptions of the pre-SharedUtils architecture remain
- All other CLAUDE.md content (project overview, dev setup, data files, conventions) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-cleanup/03-04-SUMMARY.md`
</output>
